// HMR compatibility for running development sessions that imported the former `.tsx` module.
// New source imports resolve to the JSX-free `.ts` implementation.
export * from "./TreeProviderViewModel";
