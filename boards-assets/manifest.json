{
  "version": 1,
  "description": "Persephone Web Board — recommended third-party components and their Persephone skins. Each skin is written against the --p-* theme contract and is NOT bundled in the installer; an agent fetches the files it needs into the board folder as a frozen local copy. See README.md.",
  "components": [
    {
      "id": "tabulator",
      "name": "Tabulator",
      "purpose": "Data grid — sortable / filterable columns, Excel-style range select + clipboard copy/paste, virtualized rendering for large datasets, in-cell editing, grouping, and tree data.",
      "homepage": "https://tabulator.info/",
      "npm": "tabulator-tables",
      "testedVersion": "6.5.1",
      "vendor": {
        "css": "https://cdn.jsdelivr.net/npm/tabulator-tables@6.5.1/dist/css/tabulator.min.css",
        "js": "https://cdn.jsdelivr.net/npm/tabulator-tables@6.5.1/dist/js/tabulator.min.js",
        "note": "Download these INTO the board folder (e.g. lib/) and reference them with relative paths — the board CSP forbids remote network, so a CDN <script>/<link> will not load."
      },
      "skin": {
        "file": "tabulator.css",
        "tunedFor": "tabulator-tables@6.5.1",
        "tuned": "2026-06",
        "loadOrder": "board-base.css → lib/tabulator.min.css → tabulator.css",
        "cssVsJs": "The skin styles ALL grid chrome (frame, header, rows/cells, range selection, footer/pagination, header-filter inputs, in-cell editors, menus/popups, group/calc rows, tree toggles, resize guides, alerts, toggles) via the --p-* contract, so the grid follows the app theme live. Per-value colors that Tabulator sets INLINE from JS cannot be reached from CSS — the `progress` formatter bar fill + legend, and the `traffic-light` / `color` / `star` formatters. Pass those from the live palette (`persephone.getTheme()` or the `onThemeChange` argument) in your board JS and re-apply them in `persephone.onThemeChange` (see the demo board's app.js)."
      }
    },
    {
      "id": "chartjs",
      "name": "Chart.js",
      "purpose": "Charts & plots — line, bar, doughnut/pie, radar, scatter, area, mixed. Canvas-rendered, animated, responsive; good default for dashboards.",
      "homepage": "https://www.chartjs.org/",
      "npm": "chart.js",
      "testedVersion": "4.4.6",
      "vendor": {
        "js": "https://cdn.jsdelivr.net/npm/chart.js@4.4.6/dist/chart.umd.js",
        "note": "Chart.js ships NO stylesheet — it draws to <canvas>, so there is nothing to @import and no CSS skin. Download the UMD bundle INTO the board folder (e.g. lib/chart.umd.js) and reference it relatively; it exposes a global `Chart`. The board CSP forbids remote network, so a CDN <script> will not load."
      },
      "skin": {
        "file": "chart-theme.js",
        "tunedFor": "chart.js@4.4.6",
        "tuned": "2026-06",
        "loadOrder": "lib/chart.umd.js → chart-theme.js → app.js (no component CSS; chart-theme.js is a JS adapter, not a stylesheet)",
        "type": "js-adapter",
        "cssVsJs": "Chart.js is fully JS-colored — there is no CSS skin. `chart-theme.js` exposes `window.PersephoneChartTheme` (`applyDefaults(Chart, theme)`, `palette(theme)`, `paletteN(theme, n)`, `alpha`, `mix`). Pass it a --p-* palette: at init use `persephone.theme`; on a theme switch use the palette delivered to `persephone.onThemeChange` (or `persephone.getTheme()`) — NOT a cached `persephone.theme`, which is a load snapshot and goes stale after a switch. `applyDefaults` sets Chart.defaults.color (ticks/legend/point-labels) and Chart.defaults.borderColor (grid/angle lines), so scales follow the theme with no per-chart work; dataset colors come from `palette()`. The board chrome around the canvases is styled with --p-* in index.html as usual. See the demo board's app.js for the build + onThemeChange wiring."
      }
    },
    {
      "id": "flatpickr",
      "name": "Flatpickr",
      "purpose": "Date / time picker — single date, date + time (12/24h), time-only, date range, inline calendar, week numbers, month-dropdown nav, and min/max + disabled-date constraints. Lightweight, dependency-free.",
      "homepage": "https://flatpickr.js.org/",
      "npm": "flatpickr",
      "testedVersion": "4.6.13",
      "vendor": {
        "css": "https://cdn.jsdelivr.net/npm/flatpickr@4.6.13/dist/flatpickr.min.css",
        "js": "https://cdn.jsdelivr.net/npm/flatpickr@4.6.13/dist/flatpickr.min.js",
        "note": "Download BOTH INTO the board folder (e.g. lib/) and reference them with relative paths — the board CSP forbids remote network, so a CDN <script>/<link> will not load. The vendored CSS is the base/light theme (no theme-name suffix); that is the one the skin overrides — do NOT also vendor flatpickr's dark.css/material themes."
      },
      "skin": {
        "file": "flatpickr.css",
        "tunedFor": "flatpickr@4.6.13",
        "tuned": "2026-06",
        "loadOrder": "board-base.css → lib/flatpickr.min.css → flatpickr.css",
        "type": "css",
        "cssVsJs": "Flatpickr is fully CSS-styleable — there is NO JS-colored surface, so the whole component themes from this one stylesheet and a theme switch re-tints it with no JS (the calendar is appended to <body>, under the same <html> that carries the --p-* inline vars). The skin restyles the calendar container (incl. its pointer-arrow ::before/::after triangles + box-shadow border ring), month nav + year/month <select> spinners, weekday header, every day-cell state (hover, today=accent outline, selected/range endpoints=solid accent, in-range=accent tint via color-mix, disabled, prev/next-month, week numbers), and the time picker — all from --p-*. Flatpickr's three hardcoded accents (#569ff7 selection, #959ea9 today, #f64747 arrow-hover) are replaced with --p-accent. NOTE: flatpickr does NOT style the text <input> itself — the board owns that; style your input with --p-* in your own CSS (see the demo board)."
      }
    },
    {
      "id": "tom-select",
      "name": "Tom Select",
      "purpose": "Rich select / tags / autocomplete — searchable single + multi select, removable tag chips, create-on-the-fly values, option groups, remote/async options, and keyboard navigation. Dependency-free.",
      "homepage": "https://tom-select.js.org/",
      "npm": "tom-select",
      "testedVersion": "2.4.3",
      "vendor": {
        "css": "https://cdn.jsdelivr.net/npm/tom-select@2.4.3/dist/css/tom-select.css",
        "js": "https://cdn.jsdelivr.net/npm/tom-select@2.4.3/dist/js/tom-select.complete.min.js",
        "note": "Download BOTH INTO the board folder (e.g. lib/) and reference them with relative paths — the board CSP forbids remote network, so a CDN <script>/<link> will not load. Vendor tom-select.css (the default theme — the one the skin overrides) and tom-select.complete.min.js (the build that bundles all plugins: remove_button, dropdown_input, clear_button, etc.). Do NOT also vendor the bootstrap5/material theme CSS."
      },
      "skin": {
        "file": "tom-select.css",
        "tunedFor": "tom-select@2.4.3",
        "tuned": "2026-06",
        "loadOrder": "board-base.css → lib/tom-select.css → tom-select.css",
        "type": "css",
        "cssVsJs": "Tom Select is fully CSS-styleable — there is NO JS-colored surface, so the whole component themes from this one stylesheet and a theme switch re-tints it with no JS (the dropdown is rendered under the same <html> that carries the --p-* inline vars). The skin restyles the control box (mirrors the board's inputs: --p-bg + --p-border, accent focus ring), the text input + placeholder, selected-item/tag chips (neutral tint + accent on .active) with the remove_button, the dropdown panel + box-shadow, optgroup headers, options (the keyboard/hover .active row = solid accent, like the app's menus), the matched-substring .highlight, the loading spinner, and the dropdown_input/dropdown_header plugins — all from --p-*. Tom Select's hardcoded accents (#f5fafd/#495c68 active option, rgba(125,168,208,0.2) match highlight) are replaced with --p-accent. The control already matches the board's own inputs, so a <select>/<input> upgraded by Tom Select sits flush with the form."
      }
    },
    {
      "id": "markdown",
      "name": "marked + highlight.js",
      "purpose": "Markdown rendering + code highlighting — marked parses Markdown (GFM: tables, task lists, strikethrough) to HTML; highlight.js tokenizes fenced code blocks. Together they render README-style documents, release notes, AI/agent output, and code snippets.",
      "homepage": "https://marked.js.org/",
      "npm": "marked",
      "testedVersion": "marked@15.0.12 + highlight.js@11.11.1",
      "vendor": {
        "js": "https://cdn.jsdelivr.net/npm/marked@15.0.12/marked.min.js",
        "jsHighlight": "https://cdn.jsdelivr.net/npm/@highlightjs/cdn-assets@11.11.1/highlight.min.js",
        "note": "TWO libraries — download BOTH JS bundles INTO the board folder (e.g. lib/) and reference them relatively; the board CSP forbids remote network, so a CDN <script> will not load. marked.min.js exposes a global `marked` (use the bare `marked.min.js` UMD path — marked 16+ dropped the bundled UMD artifact, so that path serves the last UMD build, v15.0.12); highlight.min.js (the @highlightjs/cdn-assets 'common languages' bundle) exposes a global `hljs`. Do NOT vendor a highlight.js theme CSS — markdown.css IS the theme (highlight.js ships only class names; its colors live entirely in a theme stylesheet). marked emits no CSS of its own either."
      },
      "skin": {
        "file": "markdown.css",
        "tunedFor": "marked@15.0.12 + highlight.js@11.11.1",
        "tuned": "2026-06",
        "loadOrder": "board-base.css → markdown.css (no component CSS to load between them — neither library ships a usable stylesheet)",
        "type": "css",
        "cssVsJs": "Both halves are pure CSS skins — there is NO JS-colored surface, so a theme switch re-tints the rendered document AND the syntax colors with no JS re-render. markdown.css is ONE file with two parts: (1) rendered-markdown typography for a `.markdown-body` container — headings (h1/h2 ruled with --p-border), proportional system-font prose, lists + GFM task-list checkboxes (accent-color), blockquotes (accent left-border), GFM tables (--p-panel header, faint zebra stripes), inline code (--p-text tint pill), and the <pre> code frame (--p-panel + --p-border); (2) a highlight.js token theme mapping every .hljs-* class onto --p-* by role — keywords/tags/literals → --p-accent, titles/functions/types/built-ins/attr (e.g. JSON keys) → --p-link, strings/regexp/additions → --p-success, numbers/symbols/meta → --p-warning, comments → --p-text-muted (italic), deletions → --p-error, plain/operators/punctuation → --p-text. --p-* has no syntax palette, so the ~8 token hues fold onto the semantic colors; each is one the theme system already keeps legible on its own background (verified in both themes — the amber --p-warning is a dark amber in light themes, not an invisible yellow). Wrap marked's output in a `.markdown-body` element, then call hljs.highlightElement on each `pre code` (marked emits language-* classes that highlight.js reads)."
      }
    },
    {
      "id": "mermaid",
      "name": "Mermaid",
      "purpose": "Diagrams from text — flowcharts, sequence, class, state, ER, pie, gantt, gitgraph, and more. Renders Markdown-fenced diagram syntax to SVG; good for docs, architecture sketches, and process/state visualizations.",
      "homepage": "https://mermaid.js.org/",
      "npm": "mermaid",
      "testedVersion": "11.15.0",
      "vendor": {
        "js": "https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.min.js",
        "note": "Mermaid draws SVG colored from JS — it ships NO stylesheet, so there is nothing to @import and no CSS skin. Download the bundle INTO the board folder (e.g. lib/mermaid.min.js, ~3.2 MB) and reference it relatively; it exposes a global `mermaid`. The board CSP forbids remote network, so a CDN <script> will not load. v11 uses the async API: `mermaid.initialize(config)` (sync, call once) then `const { svg } = await mermaid.render(uniqueId, diagramText)`."
      },
      "skin": {
        "file": "mermaid-theme.js",
        "tunedFor": "mermaid@11.15.0",
        "tuned": "2026-06",
        "loadOrder": "lib/mermaid.min.js → mermaid-theme.js → app.js (no component CSS; mermaid-theme.js is a JS adapter, not a stylesheet)",
        "type": "js-adapter",
        "cssVsJs": "Mermaid is fully JS-colored — there is no CSS skin. `mermaid-theme.js` exposes `window.PersephoneMermaidTheme` (`config(theme)`, `themeVariables(theme)`, `palette(theme)`, `mix`, `alpha`). `config()` returns a Mermaid init config using the customizable `theme: 'base'` with a full `themeVariables` map built from the --p-* palette — node fill/border/text (primaryColor/mainBkg=--p-panel, nodeBorder=--p-border, text=--p-text), edges (lineColor=--p-text-muted), clusters, edge labels, the complete sequence-diagram set (actors, signals, notes=--p-warning tint, activation bars=--p-accent), state labels, pie slices (pie1..12 from the palette: --p-accent, --p-success, --p-warning, --p-error, --p-link, --p-text-muted), and the gantt set (task/active/done bars, sections, grid, today line). IMPORTANT — Mermaid is NOT a live-update library: it bakes colors into the SVG at render time, so a theme switch must RE-INITIALIZE and RE-RENDER every diagram from its source (NOT just re-init — and NOT a no-op like a pure CSS skin). Take the palette from the onThemeChange argument (or getTheme()), call `mermaid.initialize(T.config(theme))`, then re-run `mermaid.render(...)` for each diagram. See the demo board's app.js for the load + re-render-on-switch wiring."
      }
    },
    {
      "id": "split",
      "name": "Split.js",
      "purpose": "Resizable layout panes — drag a gutter to resize adjacent panes; horizontal (columns) and vertical (rows) splits, nestable for IDE-style layouts (sidebar | editor / console). Tiny, dependency-free; sets only inline sizes and inserts bare gutter <div>s.",
      "homepage": "https://split.js.org/",
      "npm": "split.js",
      "testedVersion": "1.6.5",
      "vendor": {
        "js": "https://cdn.jsdelivr.net/npm/split.js@1.6.5/dist/split.min.js",
        "note": "Split.js ships NO stylesheet — it only sets inline width/height on the panes and inserts bare `.gutter` <div>s; the gutter appearance is the author's job (that is what the skin provides). Download the UMD bundle INTO the board folder (e.g. lib/split.min.js, ~6.8 KB) and reference it relatively; it exposes a global `Split`. The board CSP forbids remote network, so a CDN <script> will not load. API: `Split(['#a', '#b'], { sizes, minSize, gutterSize, direction })`; splits nest (use one Split per axis)."
      },
      "skin": {
        "file": "split.css",
        "tunedFor": "split.js@1.6.5",
        "tuned": "2026-06",
        "loadOrder": "board-base.css → split.css (no component CSS to load between them — Split.js has no stylesheet)",
        "type": "css",
        "cssVsJs": "Split.js has no JS-colored surface, so this is a pure CSS skin — a theme switch re-tints the gutters with NO JS (they live under the same <html> that carries the --p-* inline vars). The skin styles the divider gutters Split.js inserts: the bar fill (--p-panel), a hover/drag tint (color-mix toward --p-accent, carried through the drag via :hover since Split.js adds no dragging class), the correct resize cursors per direction (.gutter-horizontal → col-resize, .gutter-vertical → row-resize), and a centered grip of dots drawn with a radial-gradient in --p-text-muted (brightening to --p-accent on hover) — NOT a fixed-grey PNG like Split.js's docs suggest, so the grip follows the theme. The board owns the *panes'* own look (bg/border/padding) and the *layout* (flex/float + `.gutter { flex: 0 0 auto }` so the fixed-size gutter does not shrink) in its own CSS — the skin is appearance-only. Gutters are plain non-focusable <div>s (no outline blind spot); the skin still ships a defensive `:focus-visible` accent ring (a no-op) in case a board makes a gutter keyboard-operable."
      }
    }
  ]
}
