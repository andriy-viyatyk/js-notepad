# Completed Tasks

Last 20 completed tasks, newest first. Older tasks are pruned.

| ID | Title | Epic | Notes |
|----|-------|------|-------|
| US-754 | "Open Folder" item in Tools & Editors | — | Added a quick way to open an empty page with the Explorer panel rooted at a chosen folder. **Single-file change — `src/renderer/ui/sidebar/tools-editors-registry.ts`:** new `"Open Folder"` `CreatableItem` (id `"open-folder"`, `category: "tool"`, `FolderIcon`) as the **first** `staticItems` entry — its `create()` shows `fs.showFolderDialog({ title: "Open Folder in Explorer" })` and, on a non-cancel, calls the existing `pagesModel.addEmptyPageWithNavPanel(folder)` (the same call the Explorer folder "Open in New Tab" context menu uses; wrapped in a `void (async…)()` IIFE since `create` is `() => void`). Cancelling the dialog opens nothing. Because both the Tools & Editors panel and the `+` new-page dropdown build from `getCreatableItems(...)`, the item appears in both and is pinnable with **no further wiring** (generic over the registry `id`). Also prepended `"open-folder"` to `DEFAULT_PINNED_EDITORS` so it's the **first** pinned item for **new** users (no migration — the default set only seeds profiles with no saved `pinned-editors`). Named "Open Folder" (not "Explorer") to name the action, not the resulting view, and to avoid reusing the Explorer editor's `editorId`. No new files, no new APIs. `tsc --noEmit` + lint clean. `/review` clean (0 must-fix; 2 optional suggestions left as pre-existing precedent — the `void (async…)()` IIFE is correct for a `() => void` slot; `FolderIcon`'s emoji rendering matches existing `MenuBar.tsx` usage). `/document` added a `tools-editors-registry.ts` row to `CLAUDE.md` Key Files (its siblings were listed; it wasn't) and added the new caller to `pages-architecture.md`'s `addEmptyPageWithNavPanel` line. `/userdoc` updated `docs/whats-new.md` (v4.0.6), `docs/tabs-and-navigation.md` + `docs/getting-started.md` (both had stale default-pinned-set listings). Task folder `doc/tasks/US-754-explorer-tools-editors-item/`. |
| US-753 | Rename "Web Board" → "Board" everywhere | — | Dropped the misleading "Web" qualifier from the feature name: the board frontend runs under CSP `connect-src 'self'` (no remote network from the page), so "Web" read as "online" when the page can't reach the web at all (networking is only via backend scripts through `persephone.execute()`). **Prose/string-only change — zero code-identifier churn:** the codebase already used plain `board` for every symbol (`BoardEditorModel`, `board://`, `board-view`, `board-manifest.json`, `createBoard`/`openBoard`, `create_board`/`open_board`, `IBoards`, `app.boards`), so "Web Board" lived only in docs, comments, and free-text strings. Replacement rule `Web Boards→Boards` / `Web Board→Board` / `Persephone Web Board→Persephone Board` applied across **35 files**: developer docs (`CLAUDE.md`, `folder-structure.md`, `overview.md` §9 `Board Subsystem`, `scripting.md`, `document/SKILL.md`), user docs (with **`docs/web-boards.md` → `docs/boards.md`** via `git mv` + all 8 cross-links updated), 14 code-comment/JSDoc files, and runtime/agent strings (the 4 agent-facing `mcp-http-server.ts` MCP tool/instruction strings, `mcp-res-boards.md`, `mcp-res-scripting.md`, `board-template/CLAUDE.md` + `app.js`, `boards-assets/README.md` + `manifest.json`). **Intentionally left:** descriptive lowercase tech phrases (`web app`, `mini web-app`, `web content` — these describe the HTML stack and stay accurate); historical epic docs (`doc/epics/` — point-in-time records); generated `assets/editor-types/*.d.ts` (regenerated from `src/renderer/api/types/`). `tsc --noEmit` + `eslint` clean; residual `"web board"` grep returns only the intended exclusions. `/review` clean (0 must-fix; applied 3 wording fixes it surfaced — `mcp-setup.md` "open Boards directly" → "drive board pages directly", `scripting.md` "the Board's" → "a board's", and a pre-existing stale per-project→per-board trust comment in `BoardEditorModel.ts`). `/document` + `/userdoc` satisfied by construction (the deliverable *was* the doc rename, applied + verified). Task folder `doc/tasks/US-753-rename-web-board-to-board/`. |
| US-733 | Standardized always-visible "Show in main view" zone-button in the sidebar panel header | — | Replaced four ad-hoc, hide-when-already-main "show main" IconButtons across sidebar panels with one standardized zone owned by the shared header. **`ui/secondary-views/SideBarPanelHeader.tsx`:** new local Emotion `ShowMainZone` styled.button rendered in the header portal **after** `actions` — right-edge, vertical-divider (`border-left` `color.border.light`), matches the header bar's own background (`color.background.dark`) + hover (`color.background.light`), icon color `icon.light`→`icon.default` on hover, and tints the chevron **blue** (`color.misc.blue`) via `data-active` when this editor is already the page's main view; wrapped in UIKit `Tooltip`, `stopPropagation` on click so it never toggles the panel. Three new props: `onShowMain` (opt-in render), `showMainTitle` (tooltip, default "Show in main view"), `showMainActive` (blue indicator). **`uikit/CollapsiblePanelStack/CollapsiblePanelStack.tsx`:** added `[data-type="sidebar-show-main"]` to the header's `pointer-events:auto` allowlist, and guarded the header hover-lighten with `:hover:not(:has([data-type="sidebar-show-main"]:hover))` so the header and the zone light up **independently** — only the hovered region changes (`:has` no-ops for headers without a zone, fully backward-compatible). **Four consumers** dropped their in-`actions` show-main IconButton + its `!isMainEditor` *visibility* gate (now always-visible via the zone), click guarded to a **no-op when already main**: Boards (`BoardListSecondaryView` — blue only when the list itself is main, i.e. `isMainEditor && !selectedBoard`; click promotes + deselects board), Git Branches & Tags (`GitBranchesSecondaryView` — dropped its `GitIcon` for the uniform chevron, click `showGitTree()` with nav-reuse; added the `isMainEditor` read it lacked), Collections (`LinkCategorySecondaryView` — Save button stays in `actions`), Mneme (`MnemeTreeSecondaryView`). `tsc --noEmit` + lint clean; `/review` clean (0 must-fix; applied all 3 suggestions — added missing `import type React` to Mneme, dropped the redundant hardcoded `data-name`, refreshed the stale allowlist comment); `/document` updated `CLAUDE.md` Key Files + `secondary-views.md` (§6 header layout + the zone, §11 add-a-panel props); `/userdoc` updated `docs/editors.md` (Collections + Branches & Tags panels) + `docs/whats-new.md` (v4.0.6 UI Polish). Task folder `doc/tasks/US-733-sidebar-show-main-zone/`. |
| US-729 | Paste image-bearing HTML into a new HTML viewer tab | — | Follow-up to US-684 (global image paste). A picture copied from PowerPoint/Office puts **no bitmap** on the clipboard (`clipboard.readImage()` empty) — only `text/plain` (whitespace) + `text/html` (an Office fragment with `data-slideid`/`data-shapeids`/VML and one `<img src="data:image/jpg;base64,…">`). Teams pastes it (reads the HTML); Persephone's old handler only looked for `image/*` clipboard **files**, so Ctrl+V did nothing. **Diagnosis** (via `execute_script` + Electron `clipboard`): confirmed empty `readImage`, formats `[text/plain, text/html]`, and the embedded JPEG (`ffd8ffe0`, ~58 KB). **Fix — `src/renderer/api/internal/clipboard-image.ts`:** new `getClipboardImageHtml(e)` (returns `text/html` only when it contains `<img src=…>`, regex `/(<img\b[^>]*\bsrc\s*=)/i`; else `null` so plain rich text is untouched), `isEditablePasteTarget(e)` (`true` for `INPUT`/`TEXTAREA`/`SELECT`/`isContentEditable` — covers Monaco's hidden textarea), `openPastedHtml(html)` (`pagesModel.addEditorPage("html-view","html","Pasted HTML",html)` + `showPage`). **`GlobalEventService.handlePaste`:** branch 1 (image file/bitmap → Image viewer, always) unchanged; then **`if (isEditablePasteTarget(e)) return;`** so a paste into any editor pastes as **text** (no new page); else image-bearing HTML → `openPastedHtml`. The new branch is a **fallback only** (per user requirement) — the HTML viewer was chosen over bare `<img>` extraction because `HtmlEditor` already exposes export-PNG / copy / open-in-Image-viewer, and it degrades gracefully when the fragment is richer than one image. 2 src files. `tsc --noEmit` + lint clean. `/review` clean (0 must-fix; 1 optional deferred — regex also matches empty/`cid:` srcs, harmless at current scope). `/document` updated `folder-structure.md` (the two stale `api/internal/` one-liners — paste now handles image-bearing HTML). `/userdoc` updated `docs/whats-new.md` (v4.0.6), `docs/editors.md` (Image Viewer paste cases), `docs/shortcuts.md` (global Ctrl+V). Task folder `doc/tasks/US-729-paste-html-image/`. |
| US-717 | Canonical `mneme://` href for Mneme tree nodes | — | Consolidated Mneme links to one canonical, openable form so a Mneme node dropped into a Link editor stores a working `mneme://` link (the no-op left open by US-716 Concern 3). **Before:** a Mneme node carried a scheme-less `{root}/{path}` href for the tree/drag/MCP layer and a qualified `mneme://{root}/{path}` only at navigation — so a stored/dropped Mneme link had no scheme and no parser matched it (silent no-op). **Fix — one canonical form + one translation boundary:** new `src/renderer/content/mneme-link.ts` (`MNEME_PREFIX`, idempotent `toMnemeHref` / `toMnemeAddress`). `MnemeTreeProvider` now **emits** the qualified `mneme://` href on every href it produces (`list`, `resolveLink`, `getNavigationUrl[ByHref]`) and **strips** the scheme to the plain address at every MCP tool call (`write`/`mkdir`/`rename`/`delete`/`upload`); `category`/`rootPath`/`getListPath` stay scheme-less so the generic tree-view path arithmetic is untouched (root node href stays `= rootPath` scheme-less while children are qualified — intentional, expand-only root; commented at `TreeProviderViewModel.tsx:183`). `mnemeLinkTraits.readMnemeBytes` uses `toMnemeHref` (no double-prefix on cross-root copy). `favicon-cache.getHostname` returns `""` for `mneme://` so a qualified href no longer buckets Mneme links under a bogus host or fetches a favicon. **Drop-dispatch fix (found in testing):** dropping on a **category** routes through `TreeProviderView.onTraitDrop`, which checked `FILE_LINK` (byte-copy) before `LINK`; a Mneme node carries both, so it hit `LinkTreeProvider.importFiles` which keeps only `filePath` items → no-op. Reordered so a catalog target (`importLinks`) is preferred over byte-copy; verified only `LinkTreeProvider` implements `importLinks`, so file-backed targets (File/Mneme/Archive) fall through to copy unchanged. 6 src files (+1 helper). `tsc --noEmit` + lint exit 0. `/review` clean (0 must-fix; applied 1 optional — root-href asymmetry comment; skipped Copy-Path-vs-Href label as pre-existing). `/document` corrected `trait-system.md` dispatch order + rationale (links-before-bytes; canonical `mneme://` href, no broken-href case), added `mneme-link.ts` to `folder-structure.md` + `CLAUDE.md` Key Files. `/userdoc` updated `docs/whats-new.md` (v4.0.5 ×3), `docs/editors.md` (Link editor drop-from-Mneme + Hostnames note), `docs/mneme.md` (Drag to Link editor / Copy Path). Task folder `doc/tasks/US-717-mneme-href-scheme/`. |
| US-718 | LinkEditor — no spurious save prompt on navigation; modified survives any navigation | — | Clicking a link inside the Link editor's Collections/Tags/Hostnames panels navigates the *same page*; the Link editor demotes to a sidebar panel (it is **not** released), yet `navigatePageTo` was firing `confirmRelease` ("Do you want to save changes?") before it knew the editor would survive — so a dirty collection always tripped the dialog. **Root cause:** `PagesLifecycleModel.navigatePageTo` (`:716`) prompted *before* the survival decision (made later in `setMainEditor` → `LinkEditor.beforeNavigateAway` → `_isOpenedFromMe`, which reads `sourceLink.sourceId`). **Fix (Option A + "modified always survives"):** new generic `EditorModel.survivesNavigation(sourceLink?)` hook (default `false`); `navigatePageTo` skips `confirmRelease` when `mainEditorInstance.survivesNavigation(options.sourceLink)` is true (survives check on the EditorModel subclass; `confirmRelease` still called on the unwrapped host). `LinkEditor.survivesNavigation` = `modified || own-source`, where own-source is a new shared `isOwnNavigationSourceId(sourceId)` predicate (own id / `link-category` / `link-tag` / `link-hostname`) also used by `_isOpenedFromMe`. Same `modified || own-source` predicate now governs `beforeNavigateAway` / `onMainEditorChanged` so a **modified** Link editor survives **any** navigation (incl. external Explorer opens) — unsaved collection edits are never lost; it stays in the sidebar until saved (then a later external nav evicts it) or its panel is closed. Since `modified ⟹ survives` and the prompt only fires when modified, the navigation prompt is structurally unreachable; the save dialog still appears on a genuine **close** (separate `confirmRelease(closing)` path). Also `LinkCategorySecondaryView.tsx`: **Save** button now shows whenever modified (main or demoted; was demoted-only); "Show links" stays gated on `!isMainEditor`. 4 src files. `tsc --noEmit` + lint exit 0. `/review` clean (0 must-fix; applied 1 optional — comment clarifying the `mainEditorInstance` vs `mainEditor` two-accessor asymmetry). `/document` updated `pages-architecture.md` (hooks box + navigation-flow Step 2 save-prompt gate), `editors.md` (EditorModel hooks sketch), `diagrams/4-class-hierarchy.mmd`, and collapsed two stale LinkViewModel-era `LinkEditor` rows in `secondary-views.md` into one accurate row (flagged the obsolete "standalone" duck-typing note for a future accuracy pass). `/userdoc` updated `docs/whats-new.md` (v4.0.5 Bug Fixes ×3) + `docs/editors.md` (Link Editor save/navigation). Task folder `doc/tasks/US-718-link-editor-navigation-save/`. |
| US-716 | Link editor drag-and-drop improvements (Collections panel) | — | Brought the Link editor "Collections" tree to drag-drop parity with the Mneme tree via the trait system, and fixed a Mneme "unknown root" drop bug. **Shared trait (Phase A):** added `FILE_LINK` to the **shared** `ILink` trait set (`linkTraits.ts`) — a link whose href is a local file now yields its bytes (`fs.readBinary`, lazy) via new `isLocalFileHref` (rejects URI schemes / curl; archive-entry hrefs pass and read through the archive service); every `ILink` producer (link list/tiles, Collections tree, File tree) thus becomes a file producer with no per-producer change. **Capability router (Phase A):** rewrote `TreeProviderView` `canTraitDrop`/`onTraitDrop` (the shared drop router for the Mneme / File / Archive / link trees) to dispatch on trait + source + **target capability**: (1) same-source → `moveItems` (file rename / category reassign); (2) file bytes present **and** target implements `importFiles` → copy; (3) cross-source link **and** target implements `importLinks` → catalog add/move by href; (4) else ignore. **Removed the old branch-3 cross-source LINK→`moveItems` fallback** — it fed a foreign href to `provider.rename` on Mneme, producing `MCP error -32603: unknown root D:`; now a non-file link dropped on Mneme is silently ignored and a file link copies in. File content is checked **before** links so a Mneme node (bytes, no usable link href) is consumed by `importFiles` rather than becoming a broken scheme-less link in a collection (kept a no-op; proper `mneme://` href fix tracked by **US-717**). **Catalog import (Phase A/B):** new `ITreeProvider.importLinks?(items, targetCategory)` (catalog upsert) beside `importFiles?` (byte copy); `LinkTreeProvider.importFiles` (stat each path → `ILink`, folders recurse) + `importLinks` (set category, dedupe-move); `TreeProviderViewModel.importLinksTo` wrapper; `LinkEditor.importLinks` gained `{ moveExistingToCategory }` so a dropped duplicate href is **moved** to the target category (toast "Moved N link(s)") instead of skipped — top-level drops only, folder-scan children keep skip-on-duplicate; `ILinkSource.importLinks` added to match. Gated the VM's title-overwrite confirm to file-backed providers (`provider.rename`) so the link catalog skips it. **Delivers:** OS file/folder drop from Windows Explorer onto a Collections category → links (folders import their files, 100-file gate); cross-window link drag (incl. `http`) onto a category → add or move-on-duplicate; Explorer file → Mneme copy / → Categories link; File-tree folder → Categories recurse. 6 src + type files. `tsc --noEmit` + lint exit 0. `/review` clean (0 must-fix; fixed 1 doc nit — archive-entry note in task README). `/document` updated `trait-system.md` (4-branch dispatch + `ILink` = `LINK`+`FILE_LINK` + registration map) and `folder-structure.md` (LinkTreeProvider / linkTraits notes). `/userdoc` updated `docs/whats-new.md` (v4.0.5 Improvements) + `docs/editors.md` (Link Editor drag-drop). Task folder `doc/tasks/US-716-link-editor-drag-drop/`. Spun off **US-717** (canonical `mneme://` href for Mneme nodes) as a placeholder. |
| US-715 | Remove auto-expand of editor secondary-view panels on open (Link / Todo / Notebook / Rest Client) | — | Clicking one of these files in the Explorer panel auto-switched the sidebar's active panel to the editor's own panel (e.g. Collections), collapsing the previously-expanded Explorer panel — confusing when reviewing files one-by-one. **Fix:** removed the private `_seedActivePanel()` method and both its call sites (`adoptHost` restore path + `setPage` fresh-open path) from `LinkEditor.ts`, `TodoEditor.ts`, `NotebookEditor.ts`, `RestClientEditor.ts`. The panels are still **contributed** (the removed `setPage` seed was already guarded by `contributesPanels()` being true, i.e. `secondaryView` was set earlier in the lifecycle) — only the auto-`page.expandPanel(...)` activation is gone, so the active sidebar panel (e.g. Explorer) stays put; the editor's own panels remain available to open manually. `ArchiveEditorModel`'s `expandPanel("archive-tree")` and Explorer's own `expandPanel` are intentionally untouched. `grep _seedActivePanel` → 0 matches after removal. `tsc --noEmit` + lint clean. Covered by the US-712..715 batch `/review` (0 must-fix) + `/document` + `/userdoc` (whats-new v4.0.5 "Sidebar stays put when opening files"). No task folder (small work). |
| US-714 | Link editor — Collections panel highlights the selected category | — | The Collections tree highlight (`selectedHref`) was derived **only** from `selectedLinkId` (the navigated link's href), so clicking a category folder never highlighted it. **Fix:** `LinkCategoryPanel.tsx` now derives `selectedItemHref` as the selected link's href **?? `selectedCategory`** — reading both fields via `vm.state.use(...)` so it re-renders on either change; this reuses the same `selectedCategory` state that drives the toolbar breadcrumb. `LinkEditor.setSelectedCategory` additionally clears `selectedLinkId` in the same `state.update`, keeping category and link selection mutually exclusive (last click wins) so a stale link selection can't outrank a freshly-clicked category. `tsc` + lint clean. Batch `/review` (0 must-fix) + `/document` + `/userdoc` (whats-new v4.0.5 bug fix). No task folder. |
| US-713 | Link editor — tag/category panel selection breaks after navigating to a link | — | Repro: select a tag → click a link to navigate → expand Collections → select a category; the center view + breadcrumb stayed stuck on the previous tag until a workaround. Root cause: the `panelExpanded` → `setExpandedPanel` bridge lived in a `useEffect` inside `LinkBody.tsx` (the **main-editor view**); after navigating to a link the LinkEditor is demoted to a sidebar and `LinkBody` unmounts, tearing down the subscription, so switching panels no longer updated `expandedPanel` — the single mode switch both the breadcrumb (`index.tsx`) and `applyFilters` key off. **Fix:** overrode `LinkEditor.onPanelExpanded(panelId)` (the `EditorModel` hook that `PageModel.setSecondaryViewsState` calls directly on the owning editor, regardless of view-mount state) to map panelId → `expandedPanel` via `setExpandedPanel`; removed the now-redundant `LinkBody` `useEffect`, its `panelExpanded` import, and the unused `pageId` local. `tsc` + lint clean. Batch `/review` (0 must-fix; flagged as the correct model-hook inversion) + `/document` (added an `onPanelExpanded` model-vs-view note to `secondary-views.md` §3) + `/userdoc`. No task folder. |
| US-712 | Tooltip singleton registry — one tooltip at a time + drag suppression | — | Two tooltip issues in the Link editor "Collections" tree: (1) a file-link row showed **two** tooltips at once — `TreeItem`'s row `tooltip={href}` (file path) **plus** the custom `LinkTooltipContent` on the nested label span; (2) starting a drag left a tooltip hovering over the drop targets, blocking the drop. **New `src/renderer/uikit/shared/tooltipRegistry.ts`** (module-level singleton, peer of `overlayRegistry`): (a) allows only one tooltip open at a time with **innermost-wins** — a nested/more-specific tooltip beats its ancestor regardless of which hover-delay fires first (`open(id, trigger, close)` refuses when the active trigger is a descendant of the new one, else closes it); (b) globally suppresses tooltips during any native drag via lazily-installed capture-phase `document` `dragstart`/`dragend`/`drop` listeners (closes any open tooltip on dragstart). `Tooltip.tsx` consumes it via `useSyncExternalStore` (same `subscribe`/`getVersion` contract as `overlayRegistry`): claims the slot on open (new `useEffect`), releases on close/unmount, and adds `suppressedByDrag` to its suppression set. Fix (1): innermost-wins makes the custom tooltip win; dropped the `LinkCategoryPanel` label tooltip's `delayShow={1200}` (→ default 800) so the swap is flash-free. Fix (2): drag suppression. `tsc --noEmit` + lint clean. Batch `/review` (0 must-fix; noted `Tooltip` nearing the uikit Rule-8 model-view threshold) + `/document` + `/userdoc`. No task folder. |
| US-698 | Mneme config — unprovisioned-model UX (Load model CTA + auto-open on start) | — | When no embedding model is provisioned, the Mneme config editor's **Embedding model** toolbar shows a warning (`Dot` + "Model not loaded — semantic search unavailable", `color="warning"`) between the label and the action button, and the button becomes `variant="primary"` labelled **"Load model"** (vs default **"Update model"** when ready, **"Downloading…"** while a download runs) — `src/renderer/editors/mneme-config/ModelPanel.tsx`, gated on `isModelReady(status)` + `isDownloadActive`. Also, `mnemeStatusModel` (`src/renderer/api/mneme-status.ts`) now **auto-opens** (focuses) the config editor ~500 ms after a definitive `status` probe shows no provisioned model while Mneme is active — routing the user to download it. One-shot per session via an `autoOpenedConfig` latch (re-armed when `mneme.enabled` flips off), so it pops once per Persephone start while unprovisioned and never once the model exists; uses existing `pagesModel.showMnemeConfigPage()` (fixed `MNEME_CONFIG_PAGE_ID`; `addPage` dedupes → focuses rather than duplicates); `openConfigEditor` lazy-imports `./pages` to avoid a startup cycle. 2 files. `tsc --noEmit` + lint clean; `/review` clean (0 must-fix; applied 1 optional — dropped the misleading `async` signature on `openConfigEditor`); `/document` updated `folder-structure.md` + `CLAUDE.md` Key Files (mneme-status role; also corrected a stale "throwaway connection per probe" note → shared connection); `/userdoc` updated `docs/mneme.md` (Embedding model + first-run prompt) + `docs/whats-new.md` (v4.0.4). No task folder (small work). |
| US-672 | Provider icon in the text-editor footer | — | The shared text-editor footer (`src/renderer/editors/base/TextChrome.tsx`, mounted by any editor whose content host is a `TextFileModel` — Monaco text, Git Diff, Grid, …) now renders a small **provider icon** immediately before the encoding label, so the source of an open document is visible at a glance: folder (local file), globe (HTTP), Mneme icon (`mneme://`). When the pipe also carries an `ArchiveTransformer`, an archive icon is appended after the base icon (an archive entry is a `FileProvider` + `ArchiveTransformer`, not a provider of its own). **Mechanism:** new `ProviderIcon` sub-component reads `host.pipe` (`pipe.provider.type` → `file`/`http`/`mneme`; `pipe.transformers.some(t => t.type === "archive")`); touches `host.state.use(...)` so it re-renders normally. A null pipe (untitled/temp) and `cache`/`data` providers render nothing. Icons keep their canonical hues (`MEMORY_ICON_COLOR`, `DEFAULT_BROWSER_COLOR` from `theme/palette-colors.ts`; file = `color.text.light`; archive = built-in multicolour) at `16×16`; hover shows a tooltip with the provider label + source URL. Standalone UI enhancement motivated by EPIC-032 (Mneme docs had no visual source cue). 1 file. `tsc --noEmit` + lint clean; committed `4954363d`. Review/document/userdoc skipped at user request (implemented earlier, low-risk single-file footer addition). Task folder `doc/tasks/US-672-provider-icon-footer/`. |
| US-697 | Markdown view — softer link color (distinct from search highlights) | — | Hyperlinks in the markdown view (and Mneme search result titles, which render via `MarkdownBlock`) reused the saturated blue accent (`misc.blue`) — the same blue as search-match highlights — so the Mneme search-results view read as "mostly blue." **Fix:** new per-theme semantic token `misc.link` (`var(--color-misc-link)`) in `src/renderer/theme/color.ts` + all 9 theme files under `src/renderer/theme/themes/`, a 50/50 RGB blend of each theme's `text.default` and `misc.blue` so links sit visually between body text and the accent; `MarkdownBlock.tsx`'s `& a` link color (and `strong`-in-link) repointed `misc.blue` → `misc.link`. Search-match highlights (`.highlighted-text` in `GlobalStyles.tsx`) intentionally left at `misc.blue` so matched words stay the visual anchor (that class is also shared app-wide with file search). **Abyss exception:** its `text.default` already equals `misc.blue` (`#6688cc`) so a blend would be invisible — its link token is instead **brightened** to `#9fc4f5` to stand out against the periwinkle body text. 12 files (color.ts + 9 themes + MarkdownBlock.tsx). `tsc --noEmit` + lint clean; `/review` clean (0 must-fix); `/document` + `/userdoc` found no doc updates needed (sub-threshold visual polish; no new file/API/pattern). Task folder `doc/tasks/US-697-markdown-link-color/`. |
| US-684 | Global image paste → open in Image viewer | — | Pressing **Ctrl+V** anywhere in the app when the clipboard holds an image (screenshot, "Copy image" from a browser, image copied from Teams) opens it in a new **Image viewer** tab titled "Pasted image"; non-image pastes are untouched and flow to the focused editor. **Mechanism:** a single **capture-phase** `paste` listener on `document` in `GlobalEventService` (`src/renderer/api/internal/GlobalEventService.ts`). Capture phase is essential — Monaco's `<textarea>` calls `preventDefault()` on every paste deep in the tree, so a bubble-phase listener never saw image pastes while a code editor was focused; at `document` capture the event is seen first (verified: `defaultPrevented:false`, image present as `Files`). For an image: `preventDefault()` + `stopPropagation()` (so Monaco/inputs never receive it) + open viewer; non-image returns immediately and flows through. An earlier attempt to scope the capture listener to Monaco's own DOM node (`ed.getDomNode()`) did **not** fire — that node wasn't on the paste's propagation path — hence the `document`-level listener. **Tradeoff (accepted):** Persephone claims all image pastes first; nothing consumes image paste today. New shared helpers `src/renderer/api/internal/clipboard-image.ts` — `getClipboardImageFile(e)` (first `image/*` file off `clipboardData`) + `openPastedImage(file)` (`pagesModel.openImageInNewTab(URL.createObjectURL(file), "Pasted image")`). No new IPC — the `ClipboardEvent` carries the bitmap synchronously (no permission prompt). `openImageInNewTab` gained an optional `title?` param (`PagesLifecycleModel.ts` + `PagesModel.ts` façade, backward-compatible). Pasted-image tab survives restart (existing `cacheBlobUrl` blob-URL path). Pairs with US-682's HTML image-export. 4 files. `tsc --noEmit` + lint clean; `/review` clean (0 must-fix after fixing stale module JSDoc that described an abandoned two-listener design); `/document` updated `folder-structure.md` (`api/internal/` listing); `/userdoc` updated `docs/whats-new.md` (v4.0.4), `docs/editors.md` (Image Viewer), `docs/shortcuts.md` (Ctrl+V global row). Task folder `doc/tasks/US-684-global-image-paste/`. |
| US-682 | HTML viewer — image-export toolbar (Copy + "…" menu) | — | The HTML viewer (`src/renderer/editors/html/`) gained an image-export toolbar so agent-built `.html` mockups can be sent as PNGs (e.g. pasted into Teams). **Toolbar** (`html/index.tsx` `HtmlToolbarBits`, wired via `<TextChrome rightToolbarContributions=…>`): a **Copy** `IconButton` (`CopyIcon`) + a "…" `WithMenu` (`MoreVertIcon`) with **Save as PNG** / **Open in Image View** / **Edit Image**. **Capture mechanism — WYSIWYG, the one non-headless `IImageExport`:** the page renders inside a sandboxed `<iframe srcDoc>` (cross-origin → cannot be canvas-rasterised), so `HtmlEditor.exportPng()` captures the **live on-screen iframe** pixel-for-pixel via a new `capturePageRegion` IPC endpoint — `webContents.capturePage(rect)` in main, rect scaled by `getZoomFactor()` for zoom alignment — returning a PNG buffer wrapped as a `Blob`. The view reports the iframe to the model through `setCaptureElement` (cleared on unmount + dispose); the model derives the rect from `getBoundingClientRect()`, so it requires a mounted/visible view and throws otherwise (documented `@throws` deviation from the headless contract). Captures only the visible region — intended ("match what I see"; user resizes the window to lay out the mockup first). **Actions** reuse existing helpers: Copy → `navigator.clipboard.write`; Save → `savePngViaDialog(model)`; Open in Image View → `pagesModel.openImageInNewTab(URL.createObjectURL(blob))`; Edit Image → `pagesModel.addDrawPage(dataUrl)` (Excalidraw), via new shared `blobToDataUrl` in `editors/shared/image-export.ts`. Menu actions defer ~250 ms (`MENU_CLOSE_DELAY_MS`) so the open menu repaints out before the screenshot. Transient `capturing` flag on `HtmlEditorState` guards Copy/Open/Edit (Save shows its own dialog). New IPC end-to-end: `api-types.ts` (`CaptureRect` + `Endpoint` + `Api`), `main/controller.ts` (handler + bind), `renderer/api.ts` (wrapper). 7 files. `tsc --noEmit` + lint clean; `/review` clean (0 must-fix after applying both concerns — `FileReader` runtime guard in `blobToDataUrl`, documented the `IImageExport` view-dependency; extracted `blobToDataUrl` to shared); `/document` updated `editors.md` (IImageExport section — HTML viewer + live-capture path) + `CLAUDE.md` (Key Files); `/userdoc` updated `docs/editors.md` (HTML Preview → image-export toolbar) + `docs/whats-new.md` (v4.0.4 New Features). Task folder `doc/tasks/US-682-html-viewer-copy-image/`. |
| US-667 | Render YAML frontmatter as a yaml code block in the markdown view | — | The markdown **preview** rendered a leading YAML frontmatter block (`---`…`---`) as broken CommonMark — two thematic-break rules with the key/value lines as a loose paragraph (and `title:` lines misread as setext headings). New `preprocessFrontmatter()` in `src/renderer/editors/markdown/MarkdownBlock.tsx` rewrites a document-leading `---`…`---` (or `…`…`...` YAML end-of-doc) block into a ```` ```yaml ```` fenced code block, chained into the existing `processedContent` `useMemo` ahead of the mermaid-container rewrite (`preprocessFencedContainers(preprocessFrontmatter(content))`). The resulting fence flows through the existing `CodeBlock` → `ColorizedCode` → `monaco.editor.colorize(…, "yaml")` path, so highlighting works with **no** new component/registry/highlighter changes. Anchored at document start (optional BOM stripped via `content.charCodeAt(0)===0xfeff`, BOM re-prepended from `content[0]`); empty frontmatter (`---`/`---` with blank body) left untouched; mid-body `---` thematic breaks unaffected (fast-bail unless the doc starts with `-`). **Render-only** — the Monaco text editor and the file on disk are never modified. 1 src file. lint clean; `/review` clean (0 must-fix; 1 optional readability note addressed — replaced the BOM string literal with a slice from source); `/document` found no dev-doc updates needed (no new file/API/pattern); `/userdoc` updated `docs/editors.md` (Markdown Preview → YAML frontmatter bullet) + `docs/whats-new.md` (v4.0.4 UI Polish). Task folder `doc/tasks/US-667-markdown-frontmatter-rendering/`. |
| US-650 | Settings & About pages — background Ornament pinned bottom-right | — | The Settings and About editor pages now render the decorative `Ornament` (`src/renderer/theme/Ornament.tsx`) in the bottom-right corner, behind the content, matching the empty page — and on the scrollable Settings page it stays **fixed** while the content scrolls. **Opt-in flag:** new `EditorModel.showBackgroundOrnament` (`src/renderer/editors/base/EditorModel.ts`, default `false`, next to `noLanguage`/`skipSave`; a non-reactive presentation hint); `SettingsEditor` + `AboutEditor` set it `true`. **Layout** (`src/renderer/ui/app/Pages.tsx`): when the main editor opts in, the scroll container (`PageEditorContainer`) is wrapped in a new non-scrolling `OrnamentPageArea` (`position:relative`, `flex 1 1 auto`, `overflow:hidden`) that hosts the existing `OrnamentWrapper`+`Ornament` pinned bottom-right; a `& > .scroll-container { position:relative; zIndex:1 }` rule raises the (transparent) scroll container above the ornament, so the ornament shows through the empty margins around the centered, opaque content card. A non-scrolling wrapper is required because an absolute child *inside* the scroll container would scroll away. 4 files. `tsc --noEmit` + lint clean; `/review` clean (0 must-fix; applied the 1 optional — moved the `position/zIndex` override out of an inline `style` into the styled component's `& > .scroll-container` selector); `/document` added `showBackgroundOrnament` to the `editors.md` EditorModel-flags snippet; `/userdoc` judged sub-threshold (pure background decoration) — no What's-New bullet. |
| US-649 | Todo toolbar — fixed-width right-aligned search input | — | The Todo editor's toolbar search box stretched the full toolbar width. Gave the `<Input>` in `TodoToolbarBits` (`src/renderer/editors/todo/index.tsx`) a fixed `width={200}`; it right-aligns automatically because `TextChrome` forwards `rightToolbarContributions` to `PageToolbar` after the auto-spacer. Matches the Grid editor's search-input precedent. Original input size preserved (no `size` change). 1 file. `tsc --noEmit` + lint clean. Cosmetic — below the What's-New threshold (no user-doc bullet). |
| US-648 | Explorer tree — restore row hover + active-file reveal | — | Two regressions in the Explorer sidebar tree. **(A) Hover:** the UIKit `Tree` drives its `[data-active]` hover background entirely from the caller's controlled `activeIndex`/`onActiveChange`, but `TreeProviderView` (`src/renderer/components/tree-provider/TreeProviderView.tsx`) never passed them, so `onItemMouseEnter → onActiveChange?.()` short-circuited and `data-active` was never set. Added a view-local `useState<number\|null>` and wired both props to `<Tree>` (same pattern as `GitBranchesSecondaryView`; visual-only transient state, correctly view-local per the model-view doc). **(B) Reveal/selection:** `ExplorerEditorModel.onMainEditorChanged` (`src/renderer/editors/explorer/ExplorerEditorModel.ts`) read the navigated path from `newMainEditor.state.get().filePath` — but for wrapped file editors (Monaco, Grid, …) the editor's own state is a fresh editor-shaped state with **no `filePath`** (it lives on the content host). So `filePath` was always `undefined` → always took the `_selectAndReveal(null)` branch → no highlight, no auto-expand. Added the `newMainEditor.contentHost?.state.get().filePath` fallback, mirroring `EditorModel.getNavigationSourceId`. The downstream reveal pipeline was intact — it just never fired. 2 files. `tsc` + lint clean; `/review` clean (0 must-fix); `/userdoc` added a `whats-new.md` (v4.0.3) bullet. |
| US-647 | Audio player Next/Random respects active tag filter | — | When a tag was selected in the Links editor's **Tags** panel and an audio link played, the player's **Next**/**Random** jumped to files outside the filtered (tagged) set. Root cause: `LinkEditor.openLinkFromPanel` (`src/renderer/editors/link-editor/LinkEditor.ts`) built the `openRawLink` `ILinkData` **without** `selectedTag`, so the player stored `sourceLink.selectedTag = undefined`; `VideoEditor.getSiblingTracks` then did `getTagItems("")` — the "All" path — and walked the full link list. The auto-advance `navigateToTrack` already re-propagated `selectedTag`, but only read it back from `sourceLink` (never seeded). **Fix:** seed `selectedTag` into `createLinkData` when `sourceId === "link-tag"`, mirroring `navigateToTrack`'s existing re-propagation. Realigns the code with the behavior already documented in `secondary-views.md` (the doc described the intent; the code had drifted — likely lost in the US-601 `openLinkFromPanel` refactor). 1 file. `tsc` + lint clean; `/review` clean; `/userdoc` added a `whats-new.md` (v4.0.3) bullet. |
| US-646 | Links "Collections" panel — "Show links" header button | — | After clicking a link in the **Collections** sidebar panel (which opens the target in the main view), there was no obvious way to bring the links list back as the main view. Added a **"Show links"** header `IconButton` (`ChevronRightIcon`, sized `14×14` to match the adjacent Save button) to `LinkCategorySecondaryView` (`src/renderer/editors/link-editor/panels/LinkCategorySecondaryView.tsx`), placed **after** the Save button and shown only when `!isMainEditor`. Calls `editor.page?.promoteSecondaryToMain?.(editor)` — the same already-documented mechanism the directory-click path in `LinkCategoryPanel` uses (no fresh re-open; the surviving instance is promoted). 1 file. `tsc` + lint clean; `/review` clean (applied the 1 optional: explicit `14×14` icon sizing); `/userdoc` added a `whats-new.md` (v4.0.3) bullet. |
| US-645 | Shared `SideBarPanelHeader` — truncating title + always-visible actions | — | Fixed a sidebar layout bug (narrowing the sidebar pushed panel action buttons off the right edge instead of truncating the title) by consolidating every secondary-view panel header into one shared component. **New `ui/secondary-views/SideBarPanelHeader.tsx`:** owns the `createPortal` into the panel-stack header and renders `[icon] [title group] [actions]`. Layout contract: the icon is the first **unwrapped** portal child (so the stack's `[data-part="header"] > svg { 14px }` rule still sizes it); the title group (`badge` + `title`) is a `Panel flex={1} width={0} overflow="hidden"` so the title (`<Text truncate size="md">`, 13px) and a `truncate` `Tag` badge **ellipsize**; the actions region is a `Panel shrink={false}` so buttons stay **pinned/visible**. **Icon ownership moved into the header:** `SecondaryViewProps` gained `icon?`; `SecondaryViews.tsx` still resolves it (registry override ?? `EditorIcon`) but passes it to `LazySecondaryView` (not `CollapsiblePanel`), which forwards to each panel → `SideBarPanelHeader` — a naive `model.getIcon()` would have regressed the Search override + language-icon panels. **UIKit:** `Tag` gained `truncate?` (`data-truncate` → `min-width:0` + label-span ellipsis), used by the GitBranches/GitChanges repo-name badge. **Migration:** all 13 secondary-view panels (GitBranches, GitChanges, GitDiffRevisions, Explorer, Search, Archive, LinkCategory + 6 text-only: Todo, RestPanel, NotebookTags, NotebookCategories, LinkHostnames, LinkTags) dropped their `createPortal`/`Spacer` and now forward `icon` + pass `title`/`badge`/`actions` (conditional/stateful buttons kept verbatim in `actions`). `tsc --noEmit` + lint clean; `/review` clean (0 must-fix; remaining suggestions were pre-existing US-644 items); `/document` updated `folder-structure.md` + `secondary-views.md` (§6 rendering, §11 add-a-panel) + `CLAUDE.md` (Key Files); `/userdoc` added a `whats-new.md` (v4.0.3 UI Polish) bullet. Task folder `doc/tasks/US-645-sidebar-panel-header/`. |
| US-644 | Page-tab context menu — per-editor `onGetMenuItems()` | — | Refactored the page-tab right-click menu into two tiers so non-text editors no longer show a wall of disabled text-file items. **Mechanism:** new concrete `EditorModel.onGetMenuItems(): MenuItem[]` whose default delegates to the content host (`this.contentHost?.onGetMenuItems?.() ?? []`) — text-bearing editors get the text-file menu for free, non-text editors override, an editor with nothing returns `[]`. `IContentHost` gained optional `onGetMenuItems?()`. **Single home of the menu items:** new `editors/shared/editor-menu-items.tsx` — `textFileMenuItems(host)` (Save / Save As / Rename / file-path items / encryption group) and reusable `filePathMenuItems(filePath)` (Show in File Explorer + Copy File Path). `TextFileModel` implements `onGetMenuItems()=textFileMenuItems(this)` and now owns `promptRename()` (the rename dialog, moved off the tab). **Tab:** `PageTab.handleContextMenu` builds only tab-level items (Close / Close Others / Close to Right / Open in New Window / Duplicate / Pin-Unpin) then appends `mainEditorInstance.onGetMenuItems()`, stamping `startGroup:true` on the first contributed item (tab owns the divider); old text-file block + `renameTab` removed (7 now-unused icon imports + `ui` dropped). **Git Tree:** `onGetMenuItems()` → "Open Git Root Folder" (`api.showItemInFolder(repoRoot)`) + "Copy Remote URL" (`git.getRemoteUrl`; prefers `origin`, falls back to first remote). New `gitRemoteUrl` IPC end-to-end: `api-types.ts` (Endpoint+map), `controller.ts` (handler+bind), `renderer/api.ts` (wrapper), `git-service.ts` (`remoteUrl()` via `git remote get-url`), `renderer/api/git.ts` (`getRemoteUrl` facade). **PDF/Image/Archive:** `onGetMenuItems()=filePathMenuItems(<source path>)`. `tsc --noEmit` + lint clean; `/review` clean (0 must-fix; 2 of 3 optional applied — dropped a redundant optional-chain, and prefer `origin` for multi-remote repos); `/document` updated `editors.md` (EditorModel snippet + new "Page-tab context menu" section) + `CLAUDE.md` (Key Files); `/userdoc` updated `whats-new.md` (v4.0.3 UI Polish) + `tabs-and-navigation.md` (two-tier menu tables). Task folder `doc/tasks/US-644-tab-menu-onget-menu-items/`. |
| US-643 | Sidebar panel headers — editor icon + shared icon resolver | — | Sidebar secondary-view panel headers now lead with an **editor-specific icon** so panels from different editors are distinguishable at a glance. **Shared resolver:** new `EditorIcon` (`components/icons/EditorIcon.tsx`) centralises the page-tab icon decision (`noLanguage ? editor.getIcon() : <LanguageIcon language title>`) behind a duck-typed `EditorIconSource` (no `EditorModel` import → `components/icons` stays decoupled); it forces no size/color so monochrome `currentColor` icons follow the header state (accent-blue when active) while colored icons keep their hue. **Single source of truth** — both `PageTab.tsx` and `SecondaryViews.tsx` use it. **Wiring:** `SecondaryViews.tsx` passes `secondaryViewRegistry.get(panelId)?.icon ?? <EditorIcon editor={model} />` to `CollapsiblePanel.icon` (renders in the header div's React tree → click-to-toggle works, no portal workaround). `SecondaryViewDefinition` gained an optional per-panel `icon?` override; the Explorer `search` panel registers `SearchIcon`. `ExplorerEditorModel` got `getIcon = FolderIcon`. **Storybook icon:** new `StorybookIcon` (book + bookmark, fixed Storybook pink) in `theme/icons.tsx`, wired into `StorybookEditorModel.getIcon` and swapped into the Tools & Editors list (was `ViewListIcon`). **Folded in (same session):** Git Tree Changes / Branches & Tags panels render the repo name as an outlined `Tag` badge (mirrors the toolbar) instead of `[name]`; `CollapsiblePanelStack` portal click-through rule extended from `[data-type="text"]` to also cover `[data-type="tag"]`/`[data-type="panel"]`, with a companion rule re-asserting `pointer-events:auto` on interactive descendants (the new `Tag`/`Panel` wrapper had swallowed the header toggle click). `tsc` + lint clean; `/review` clean (0 must-fix; 1 optional applied — documented the click-through selector-breadth constraint); `/document` updated `secondary-views.md` + `editors.md` (new "Editor icons" section) + `CLAUDE.md`; `/userdoc` updated `editors.md` + `whats-new.md` (v4.0.3). Task folder `doc/tasks/US-643-sidebar-panel-icons/`. |
| US-640 | Folder links open an empty page with the Explorer panel | — | Clicking/opening a link that resolves to a **directory** (e.g. a `file://` folder link in a Markdown preview, a Links list entry, anywhere `openRawLink` is used) previously fell through the content pipeline to a `"monaco"` fallback target and rendered a **blank Monaco editor** (the `FileProvider.readBinary()` read on a directory threw and was silently swallowed as "not deleted"). **Fix (single file, `src/renderer/content/resolvers.ts`):** the Layer-2 file resolver now `stat`s plain (non-virtual, non-archive) paths via `app.fs.stat()`; when `isDirectory`, it short-circuits — `pagesModel.addEmptyPageWithNavPanel(data.url)` + `closeFirstPageIfEmpty()`, sets `data.handled`, and bypasses Layer 3 (directories never produce a content pipe). Result: a folder link opens an empty page (no main editor) with the **Explorer** sidebar panel rooted at the folder — identical to "Open Folder" / the folder-tree context menu. Mirrors the HTTP-resolver browser short-circuit pattern. Decisions: always-new-page (matches existing folder entry points) + stat every plain link (correct even for dot-named folders). `tsc --noEmit` + lint clean; `/review` clean after 2 fixes (missing `closeFirstPageIfEmpty()` → spurious leftover empty tab; `.lifecycle.` → flat `pagesModel.` API) + 1 optional applied (skip stat for archive inner-paths); `/document` updated `content-pipeline.md` (Layer 2 directory short-circuit) + `pages-architecture.md` (`addEmptyPageWithNavPanel` callers); `/userdoc` updated `whats-new.md` (v4.0.3). No task folder (small work). |
| US-639 | Expose & target browser **profile** through the MCP interface | — | Browser profiles are now first-class over MCP — agents no longer reverse-engineer partitions via `execute_script` to act on the right login session (the motivating case: a named work profile holding the only authenticated Outlook/SharePoint session). **Metadata:** `list_pages`/`get_active_page` include `profileName` ("" = default) / `isIncognito` / `isTor` / `url` (ACTIVE internal tab's URL; omitted for incognito/Tor — privacy) for `browser-view` pages, read via structural state cast keyed on `editorId` (deliberately NO static `BrowserEditor` import in `mcp-handler.ts` — loads at startup, browser chunk stays dynamic); `list_windows` exposes the three persisted identity fields (works for closed windows; no `url` — not persisted); `get_app_info` gained `browserProfiles` + `defaultBrowserProfile` (two settings reads — discovery instead of a new tool). **Targeting:** `getTarget(params)` in `automation/commands.ts` (single choke point) resolves `pageId` → `profileName` ("" = default; never matches incognito/Tor; active page preferred, else first) → existing active/first fallback; resolved page is activated (webview needs `display != none`), privacy blocks stay after resolution; all 14 `browser_*` tool schemas gained shared `pageId`/`profileName` zod params. **Key finding:** `open_url` profile-matched reuse already existed (`openUrlInBrowserTab` matches + focuses by profile) — only its description was stale; fixed text only, `PagesLifecycleModel` untouched. New "Browser Profiles" section in `assets/mcp-res-pages.md`. 4 files (3 code + 1 guide). Implemented by an Opus subagent from the task doc (cost saving); diffs reviewed + tsc/lint re-verified independently. `/review` clean (0 concerns, 2 optional suggestions left as-is); `/document` updated `browser-editor.md` (target resolution + profile visibility) + `overview.md` (MCP bullet); `/userdoc` updated `mcp-setup.md` (tools table, Browser Profiles section, profile example) + `whats-new.md` (v4.0.3). Task folder deleted on completion. |
| US-633 | Editor image export — "Save to file" + agent-readable PNG (Mermaid / SVG / Image) | — | Gave the three image-rendering editors a save-to-file path and an agent-readable PNG export. **Core:** new `IImageExport` capability (`editors/base/IImageExport.ts` — `exportPng()`/`suggestedImageName()`) implemented by `MermaidEditor`/`SvgEditor`/`ImageEditor`; shared canvas helpers `editors/shared/image-export.ts` (`rasterToPngBlob` loads an `image/svg+xml` data URL / blob / http URL into an offscreen `<img>`, draws to canvas, encodes PNG — browser rasterisation keeps fonts/text, fixing the broken external mermaid→png conversions), plus `savePngViaDialog` (toolbar, notifies on failure) and `writePngToFile` (facade). Export is **model-level / headless** (works off-tab); Mermaid renders on demand via `renderMermaid` when `svgUrl` is empty. `BaseImageView.copyToClipboard` refactored onto the shared `imageElementToPngBlob`. **UI:** Mermaid & SVG editors got a toolbar Save (PNG, 1×) button; the Image viewer's save button became a `WithMenu` dropdown — "Save as .png" (re-encode) / "Save original" (source bytes via `pipe.readBinary()`, falls back to `fetch`). **Scripting/agent:** `page.asMermaid()/asSvg()/asImage()` expose `savePngToFile(filePath)` (new `ImageEditorFacade` + `PageWrapper.asImage()`, guarded by the `editorId` check like `asBrowser`/`asMcpInspector`); no new MCP tool — an agent calls it via `execute_script` and reads the temp file. Types: `mermaid/svg/page.d.ts` + new `image-editor.d.ts`; `assets/mcp-res-scripting.md` documents it so an agent discovers it via `read_guide("scripting")`. **Verified live via MCP**: agent wrote a 179 KB valid PNG of an open mermaid diagram, text/fonts correct. `tsc`+`eslint` clean; `/review` clean after 2 fixes (asImage pattern, centralised save-failure toast); `/document` updated `editors.md` (new `## IImageExport`) + `scripting.md` + `CLAUDE.md`; `/userdoc` updated `editors.md` + `api/page.md` + `whats-new.md` (v4.0.3). Task folder deleted on completion. |
| US-615 | Browser reload respects `beforeunload` (unsaved-changes) guards | — | Repro: a local React dev page (Vite, `localhost:5173`) with an unsaved-changes `beforeunload` guard — clicking Reload / pressing F5 did nothing (no reload, no prompt, React state preserved); GitHub reloaded fine. **Root cause:** Electron's default for the `will-prevent-unload` event (fired when a page's `beforeunload` handler tries to cancel an unload) is to **silently cancel the unload** — no Chrome-style confirm. `browser-service.ts` never handled the event, so every reload of a dirty page was silently aborted (the brief did-start/did-stop-loading icon flip was the only visible sign). The earlier "stuck loading → Stop button" theory was a red herring. **Fix:** added a `will-prevent-unload` handler — soft reload (toolbar / F5 / Ctrl+R) shows a native confirm ("You have unsaved changes. Leave the page and discard them?"), **Leave** allows the unload (reload proceeds), **Cancel** stays (default/cancel button = Cancel, so a stray Enter won't discard); hard reload (Ctrl+F5 / Ctrl+Shift+R) bypasses the prompt and force-reloads. Bypass uses a one-shot `bypassUnloadGuard` flag on the per-webContents `RegisteredWebview` — armed by a hard reload, consumed+cleared in `will-prevent-unload`, cleared defensively in `did-stop-loading` so it can't leak. **Two keyboard layers** both arm it in main: Layer 1 (`before-input-event`, focus inside page) directly; Layer 2 (`globalKeyDown`, focus on browser chrome) via a new `BrowserChannel.hardReload` IPC (`BrowserWebviewModel.hardReload()` → main arms flag + `reloadIgnoringCache()`) — the first `/review` caught that Layer 2 originally called `reloadIgnoringCache()` directly and skipped the flag. 4 src files (`browser-service.ts`, `browser-ipc.ts`, `BrowserWebviewModel.ts`, `BrowserEditor.ts`). Lint clean; `/review` clean (re-run after Layer-2 fix; sync `showMessageBoxSync` is the only option since `will-prevent-unload` requires synchronous `preventDefault()`); `/document` updated `browser-editor.md`; `/userdoc` updated `browser.md` + `whats-new.md` (v4.0.3). No task folder. |
| US-614 | Settings — "Bookmark file:" label in Browser Profile tiles | — | One-line cosmetic clarification. The `BookmarksFileLine` component (`src/renderer/editors/settings/SettingsView.tsx`) — shared by the Default / custom-profile / Incognito / Tor tiles in the Browser Profiles section — previously showed `📁 <filename>` with no context. Added a `"Bookmark file:"` label span (reusing the existing `labelTextStyle` token) **before** the 📁 icon, rendered only when a file is set (the empty state still reads `📁 No bookmarks file`). Placed before the icon per user request; a brief experiment normalizing the emoji's vertical line box was reverted ("if it is an emoji, then let it be"). 1 file changed. `tsc --noEmit` + lint clean; `/review` clean; `/document` + `/userdoc` found no doc updates needed (cosmetic, below What's-New threshold). |
| US-347 | CategoryView / CategoryEditor breadcrumb | — | Added a clickable breadcrumb to the CategoryEditor toolbar showing the path from the linked TreeProvider's root to the current folder; clicking a chip navigates the same page to that ancestor. **Core complexity:** the three tree providers use two different category-path conventions — Link & Archive store `/`-separated **relative** paths (`rootPath===""`), File stores **absolute** OS paths (`rootPath===sourceUrl`). Solved by adding a required `getCategorySegments(category): ICategorySegment[]` ({label, category}) method to `ITreeProvider` (`src/renderer/api/types/io.tree.d.ts`) so each provider owns its own path semantics; the editor stays provider-agnostic. Shared `relativeCategorySegments()` helper in `tree-provider-link.ts` covers Link (`LinkTreeProvider`) + Archive (`ArchiveTreeProvider`); `FileTreeProvider` strips the `rootPath` prefix for labels but keeps `/`-joined absolute paths as nav categories (readdirSync accepts `/` on Windows). Navigation reuses the existing `encodeCategoryLink` + `openRawLink({pageId, sourceId: hostId})` mechanism (same as folder double-click, so the secondary panel survives). **UIKit:** added opt-in `clipStart` prop to `Breadcrumb` (`uikit/Breadcrumb/Breadcrumb.tsx`) — `flex-direction:row-reverse` + reversed children + `overflow:hidden` + `min-width:0` clips the START (root) side on overflow keeping the current folder visible; default off so LinkBreadcrumbBits/BookmarksDrawer/notebook callers are unchanged. Verified on all 3 provider types (File/Link/Archive) incl. Windows separators. `tsc --noEmit` + lint clean; `/review` clean. 7 files. Task doc: `doc/tasks/US-347-category-breadcrumb/`. |
| US-594 | Browser & Link-editor UI fixes (4 items) | — | Four small post-release UI fixes. **(1) Blank browser page link controls** — `BrowserView.tsx` `BlankPageLinks` rendered only `<LinkBreadcrumbBits>` + a flex spacer; the Add Link / view-mode / search controls (`LinkActionBits`, exported from `link-editor/index.tsx`) had been dropped during the EPIC-027/028 refactors. Restored by importing `LinkActionBits` and adding it after the spacer, matching `BookmarksDrawer.tsx`'s toolbar. **(2) Add/Edit Link dialog input stretch** — in `EditLinkDialog.tsx` `FormRow`, the value slot was `<Panel flex minWidth={0}>` (default row direction). `Input`/`PathInput`/`Select` filled it because they carry internal `width: 100%`, but `Textarea` (Title) and `TagsInput` (Tags) sized to content. Added `direction="column"` to the slot so default `align-items: stretch` stretches any child to the slot width. **(3) Webview context menu delayed while page loads** — `BrowserWebviewModel.handleContextMenu` awaited `webview.executeJavaScript()` (SVG probe) unconditionally before building/opening the menu; `executeJavaScript` queues on the page renderer's main thread, so a still-loading page blocked the menu for up to the full load time (user saw 30 s delays). Wrapped the probe in `Promise.race` with a 250 ms timeout — on idle pages the "Open SVG in Editor" item still appears; on busy pages it's dropped and the menu opens immediately. **(4) tabs-panel content width** — `BrowserTabsPanel.tsx` `BrowserTabsRoot` had `height="100%"` but no width inside its row-flex host `tabs-panel-host`, so it snapped to ~146px content width and didn't track the splitter. Added `width="100%"` (symmetric with the existing `height="100%"`). 4 src files + docs. `/review` clean; `/document` updated `browser-editor.md` SVG-extraction note; `/userdoc` updated `whats-new.md` (v4.0.2) + `browser.md`. |
| US-593 | PageTab — clear drag-over state on drop so lighter background does not stick | — | Repro: drag one page tab and drop on another to reorder; the drop-target tab kept the lighter "active" background (from `data-drag-over`) permanently, surviving subsequent activations and reorders. Root cause: `PageTab.tsx` tracks drag-over via local `isOver` state + `dragEnterCount` ref (standard counter idiom for nested-child dragenter/leave); `&[data-drag-over]` styles the lighter `color.background.default` background. `onDragEnter` increments the counter and sets `isOver=true`; `onDragLeave` decrements + clears when counter reaches 0. But `onDrop` was wired straight to `tabModel.handleDrop` with no local cleanup — per the HTML5 DnD spec, `dragleave` does NOT fire on the drop target when a drop occurs, so the counter stays at ≥1 forever and `isOver` is stuck. Subsequent enter/leave cycles only decrement back to 1, never 0. **Fix:** added local `handleDrop` that resets `dragEnterCount.current = 0` + `setIsOver(false)` before delegating to `tabModel.handleDrop(e)`; wired `onDrop={handleDrop}`. 1 file changed. Review/document/userdoc skipped at user request. |
| US-592 | LinkEditor — restore save-prompt on close after demote-to-sidebar | — | Repro: open `.link.json`, edit a link, click "Demote to sidebar only" on Categories panel, close the page → closed silently, unsaved edits lost. Root cause: `PageModel.close()` (`PageModel.ts:491-507`) iterates `this.editors` and reads `editor.modified` on each — only the main-editor branch goes through `unwrapToHost`. Text-bearing editors (Monaco, Grid, Markdown, …) override `confirmRelease` to delegate to `_host.confirmRelease()` but inherit base `get modified()` which reads the editor's own `state.modified`; they get away with it because they're never in `editors[]` as non-main (no panels → can't be demoted). `LinkEditor` is the only editor that wraps a `TextFileModel` host AND contributes sidebar panels AND can be demoted while remaining in `editors[]`. It already overrode `confirmRelease` (`LinkEditor.ts:1170`) but not `modified` — so as a demoted secondary it reported `modified=false` and the close loop skipped the prompt. **Fix:** added `get modified()` override in `LinkEditor` returning `this._host ? this._host.modified : super.modified` — same delegation pattern as the adjacent `confirmRelease` override. 1 file changed. **Known caveat (not addressed here):** the page-tab "modified" dot still disappears the moment you demote because `PageTab.tsx:516,528` reads `page.mainEditor.state.modified` and `mainEditor` is `null` after demote — close prompt fires correctly but the user has no visual indicator between demote and close. Bug never shipped (introduced and fixed in `upcoming-v4.0.1` working branch); review/document/userdoc skipped at user request. |
| US-591 | Video/Audio Player URL input — restore submit-on-Enter | — | Regression introduced during the UIKit migration of the Video/Audio Player URL bar: input required Ctrl+Enter to submit and plain Enter inserted a newline. Switched the call site (`src/renderer/editors/video/VideoView.tsx`) to use UIKit `Textarea`'s built-in `singleLine` prop + `onKeyDown` hook (designed for this URL-bar pattern — see `Textarea.tsx:36,62-67`). Plain Enter calls `preventDefault()` and triggers `model.submitUrl(inputText)`; Shift/Ctrl/Alt+Enter neither submit nor insert a newline (`singleLine` strips them). Long URLs still wrap visually because Textarea root has `white-space: pre-wrap` + `overflow-wrap: anywhere` unconditionally; outer Panel wrapper kept for `flex={1}` toolbar width. Placeholder updated `"Ctrl+Enter to play"` → `"Enter to play"`. Bug never shipped (introduced and fixed in `upcoming-v4.0.1` working branch); review/document/userdoc skipped at user request. 1 file changed. |
| US-590 | Drop stale "v4" naming from registry, persistence schema file, and related identifiers/comments | — | EPIC-028 introduced "v4"-suffixed identifiers (`v4EditorRegistry`/`v4Registry` import aliases, `editorRegistryV4`, `editorV4`, `getV4EditorModule`, `restoreV4`, `private get v4()`) and the file name `src/shared/persistence-v4.ts` as strangler-pattern scaffolding to distinguish the new editor architecture from the legacy one. After US-582 stripped the legacy/strangler narrative, the "v4" qualifier no longer contrasts with anything and was just noise. **Three-phase cleanup:** **Phase 1 (symbols, ~30 sites)** — unwrap `editorRegistry as v4EditorRegistry`/`v4Registry`/`editorRegistryV4` aliases in 19 files; rename `getV4EditorModule`→`getEditorModule` + drop `"v4:"` cacheKey prefix + drop `"v4 editor"` from error string (`RenderEditor.tsx`); `restoreV4`→`applyState` (`PagesPersistenceModel.ts`); `editorV4`→`editor` in 3 functions of `mcp-handler.ts` (also renamed local string `editor`→`editorId` in `createPage` to avoid a collision); `private get v4()`→`private get mainEditor()` + 14 local `v4` vars→`editor` (`PageWrapper.ts`). **Phase 2 (comments, ~35 sites)** — collapsed 9 uniform `/** v4 editor identity. Matches the legacy registry id so v4 ... */` docstrings to `/** Editor identity. Matches \`EditorDescriptor.editorId\`. */`; stripped `v4 X` / `v4-native` / `v4-specific` / "v4 vs legacy" qualifiers from ~25 contextual comments across `PageModel.ts`, `PagesLifecycleModel.ts`, `TextEditorModel.ts`, `ArchiveEditor.ts`, etc.; simplified "Both legacy `LinkViewModel` and v4 `LinkEditor` satisfy…" historical framing where the legacy class no longer exists. **Phase 3 (file rename)** — `git mv src/shared/persistence-v4.ts → src/shared/persistence.ts` with 28 importers updated. Also updated 2 stale references in `/doc/architecture/scripting.md` + `folder-structure.md`. Historical entries in `completed.md` / `epics/completed.md` left intact (archival records reflect terminology at the time). `npx tsc --noEmit` clean. 49 modified + 1 renamed via `git mv` (history preserved). |
| US-589 | TextChrome — restore chrome for v4 editors (regression from US-559) | — | `isTextFileHost(host)` in `src/renderer/editors/base/TextChrome.tsx` discriminated `TextFileModel` from `NoteItemEditModel` by duck-typing on `setEditorToolbarRefFirst`. US-559 ("Strangler-fig retirement") removed that method from `TextFileModel` but left it on `NoteItemEditModel`, silently inverting the check: TRUE for the inner per-note fake host, FALSE for the real TextFileModel. Symptoms (all silent, all v4 editors — notebook, monaco, grid, markdown, html, todo, link, log-view, mermaid, svg, graph, draw, rest-client): footer toolbar gone, ScriptPanel never rendered, Run/Compare/ShowResources buttons gone, editor-overlay portal target never mounted (surfaced as "Expand note doesn't work" because `NotebookBody` portals `ExpandedNoteView` into `editor.host.editorOverlayRef` which was null). F5 still worked because it's handled in `handleRootKeyDown`, not gated on `textHost`. Fix: duck-type against `setEditorOverlayRef` instead (present on TextFileModel, absent on NoteItemEditModel) + comment explaining the regression for future maintainers. 1 file changed (commit `4ce4a1f`). |
| US-587 | Post-EPIC-028 doc / type audit | — | Whats-new v4.0.1 announced changes that were never executed in code. **W1** unified `EditorView` into a single canonical site at `src/renderer/api/types/common.d.ts` (synced 3 missing members `rest-client`/`video-view`/`storybook-view`); `src/shared/types.ts` now re-exports instead of duplicating, so drift can't recur. Extended `EditorView` IntelliSense across the registry surface — `IEditorInfo.id`, `IEditorRegistry.getById/resolveId`, `ISwitchOptions.options/getOptionLabel`. **W2** added `IEditorInfo.hasContentHost: boolean` (the field whats-new promised but never shipped); `toEditorInfo()` now forwards it from the internal `EditorDefinition`. **W3** doc-only fixes — `docs/api/page.md` `asText()` Promise return types, `force?` parameter blurb, `type` row removal; `docs/scripting.md` facade-lifecycle paragraph aligned with `page.md`; `docs/whats-new.md` false breaking-change bullet replaced with positive "TypeScript improvements" entry, editor count 22 → 25. **Dev docs** — `doc/standards/editor-guide.md` Step 6 rewritten to describe the canonical-in-`api/types/` + re-export-from-`shared/types.ts` pattern; `doc/architecture/folder-structure.md` clarifies `assets/editor-types/` is generated by the Vite plugin (never hand-edit). 11 files changed. |
| US-577 | Decode percent-encoded `file://` paths in `will-navigate` (Cyrillic/Unicode filenames in Markdown links) | — | Markdown links to files with Cyrillic / Unicode names were broken: the markdown renderer built `<a href>` via `url.pathToFileURL()` which percent-encodes non-ASCII bytes; the `will-navigate` handler in `open-window.ts` stripped `uri.pathname` manually and passed the still-encoded form to the renderer, so a path like `D:\info\todo\active\2026-05-22-закриття-ФОП\task.md` reached the open pipeline as `D:\info\todo\active\2026-05-22-%D0%B7…%D0%9F\task.md` and failed to resolve. **Fix:** switched to `url.fileURLToPath(url)` — the symmetric counterpart of the `pathToFileURL()` call that built the link; handles percent-decoding, slash conversion, and Windows drive-letter handling correctly. 1 file changed (`src/main/open-window.ts`, commit `dba97c6`). Implemented during EPIC-028 window; covered by epic-level `/review` + `/document` + `/userdoc`. |
| US-586 | MCP Inspector — block relative-link clicks in resource markdown | — | MCP resources are keyed by URI, not by filesystem path, so a sibling reference like `[X](structure.md)` inside resource markdown had no anchoring base. The browser resolved it against the renderer origin (in dev: `http://localhost:5173/structure.md`), Vite answered with the SPA-fallback `index.html`, and Persephone opened a junk markdown page with stuck Text Editor/Preview toggles. Added `onClickCapture` to the resource-content `<Panel>` in `ResourceContentView.tsx`: relative hrefs (no scheme, no `#`, no `//`) are `preventDefault`'d and surfaced via `ui.notify("info")`; absolute schemes still flow through `openRawLink` as before. 1 file changed. || US-541 | Fix UIKit `CategoryList` missing custom scrollbar styling | — | `CategoryList`'s `Root` styled.div had `overflow: auto` (intrinsic to the primitive) but never applied `className="scroll-container"`, so the native browser scrollbar appeared instead of the global VSCode-style fade-in scrollbar that the `.scroll-container` CSS rule paints. Surfaced during US-540 LinkEditor testing once the Hostnames panel started scrolling correctly. UIKit `Panel` auto-applies the class when its `overflow` prop is `"auto"`/`"scroll"` (see `Panel.tsx:403`); `Menu`'s `ListRoot` also hardcodes it. CategoryList's Root now hardcodes `className="scroll-container"` since its overflow is baked into the styled definition (same idiom as Menu). UIKit `Textarea` has the same gap (`overflow-y: auto` without the class) — flagged as a potential consistency follow-up. |
| US-526 | Fix external https link ignoring active browser-page profile | — | Two bugs combined. (1) `resolvers.ts` `browserMode === "internal"` branch called `openUrlInBrowserTab` with `{ profileName: "" }`, hardcoding the search to profile-less pages instead of signalling "external reuse". Now passes `{ external: true }`. (2) `PagesLifecycleModel.openUrlInBrowserTab` matcher for `external === true` still constrained the reuse search to `browser-default-profile`; matcher now reuses any non-incognito/non-tor browser regardless of profile. New-page fall-through still uses `browser-default-profile`. |
| US-510 | Fix TreeProviderView chevron click also triggering row navigation | — | Custom `renderItem` wired `onChevronClick={ctx.toggleExpanded}` directly, but `ctx.toggleExpanded` has signature `() => void` and never stops propagation, so the click bubbled up to Tree's row `onClick` and fired `onItemClick`. Wrapped to call `e.stopPropagation()` first. |
| US-494 | Fix Open Tabs list — clicking current-window document page does not activate it | — | `OpenTabsList` was passing `mainEditor.state.id` (editor UUID) instead of `page.id` (page UUID), so `pagesModel.showPage()` lookup silently failed for any page with a `mainEditor`. List item now spreads the editor state then overrides `id` with the page id so `showPage(item.page.id)` resolves correctly. |
| US-493 | Fix Explorer panel not refreshing on `navigateUp` / `makeRoot` | — | `ExplorerSecondaryEditor` read `rootPath` from `model.state.get()` (non-reactive snapshot) instead of `model.state.use()` (subscribed). Switching to `model.state.use()` makes the rendered tree re-mount via `key={rootPath}` when `navigateUp` / `makeRoot` mutate the state. |
| US-545 | Markdown viewer — Azure DevOps `::: mermaid` container support | — | Markdown preview now renders Azure DevOps wiki `::: mermaid ... :::` fenced container blocks as diagrams. Added `preprocessFencedContainers()` regex pre-processor in `MarkdownBlock.tsx` that rewrites the containers into ```mermaid``` fences before ReactMarkdown sees the content, so the existing mermaid renderer handles them. Memoized via `useMemo(content)`. Scope limited to `mermaid`; other `:::` names pass through. 1 file changed. |
| US-425 | Fix infinite-loop when Persephone is the OS default browser | — | Fixed recursive launch loop caused by Persephone handling its own protocol URLs when registered as OS default browser. Released in v3.0.8. |
| US-424 | Links & Tags Panel — Enhancements and Bug Fixes | — | 5 items: (1) Save button in Links sidebar panel (visible when modified and not main editor). (2) SaveIcon updated to floppy-disk shape. (3) Tag editing in LinkTooltip — toggleable badges + inline input (Enter to add). Wired into LinkItemList and LinkTagsSecondaryEditor. (4) PageNavigator + Links panel bugs — first open event fix (`toggleNavigator` now fires `pageNavigatorToggled`), always-3-panels when main editor is Links, standalone mode shows Links always + Tags if data + Hostnames hidden, `addSecondaryEditor` bumps version on re-register, demote microtask no longer strips panels. (5) Archive `!` separator false-positive fix — `isArchivePath` now validates archive extension on left side. 10 files changed. |
| US-423 | Tags Panel — Player Integration & Tag-Based Navigation | — | Tags panel visible in sidebar when Player is main editor. Bottom resizable pane (Splitter + LinksList) shows links filtered by selected tag; "All" shows all items. Clicking opens via `openRawLink` with `sourceId: "link-tag"` + `selectedTag`. Player next-track/shuffle works within the selected tag's set. `LinksList` converted to `forwardRef` for scroll-into-view on auto-advance. Fixed `ContentViewModelHost` race condition (concurrent `acquire` calls now share the same vm). Fixed `RenderGridModel.setProps` to pass `{ all: true }` so cell cache invalidates on prop change. `LinkTreeProvider.getTagItems("")` returns all items. 8 files changed. |
| US-422 | Audio Player — Next Track & Shuffle Mode | — | Auto-plays next/random track from Explorer (filesystem folder) or Links (link collection category) panel when a track ends. Provider discovery via `sourceLink.sourceId` + duck-typed `treeProvider`. Navigation goes through full `openRawLink` pipeline. Shuffle bag (Fisher-Yates) stored in new `PageModel.getTransient/setTransient`. Shuffle setting persisted to `app.settings`. Links panel selection unified with `LinkViewModel.selectLink()` — panel click and auto-navigation both update the same state, so promote-to-main shows the correct highlight. 11 files changed. |
| US-421 | Custom Audio Controls Bar | — | Replaced native `<audio controls>` with custom overlay bar: play/pause, current time, seek (CSS gradient fill), duration, mute toggle. Bar fades out at rest and reappears on hover using CSS `opacity` + `pointer-events` via parent `:hover`. `isSeekingRef` suppresses timeupdate re-renders during drag. 3 files changed (1 new: `AudioControls.tsx`). |
| US-420 | Bars Audio Visualizer — Iterative Improvements | — | Bars rewritten: centered vertically + horizontally (symmetric growth from center), per-bar HSL rainbow colors with horizontal 3D gradient, OffscreenCanvas stamp optimization for particles, mini-bar particles emitted at peaks (top+bottom pairs), particle emission rate tuned. Added "No effect" mode with track info overlay (ID3 + filename fallback). Explorer panel now highlights video/audio files (`filePath` set in `newEditorModel`). File Explorer button added to Player toolbar. Circular effect trail fade fixed (destination-out compositing). 7 files changed. |
| US-420 | Audio Visualizer — effects + switcher UI + theme support | — | Added circular effect (3 RGB rings with cross-band modulation, OffscreenCanvas stamping), effect switcher buttons (bars/circular), dark/light theme support, selected effect persisted to settings, click-to-play/pause on visualizer. Removed wave/rings effects. 6 files changed. |
| US-419 | Audio Visualizer for Audio-Only Files in Video Player | — | Audio files (.mp3/.wav/.aac/.flac/.m4a) open in video editor with AudioPlayer component + spectrum visualizer. Web Audio API AnalyserNode feeds switchable effects (BarsEffect, CircularEffect). 11 files changed (7 new). |
| US-418 | Browser Cinema Mode — Expand `<video>`/`<iframe>` to Full Page | — | Preload script detects `<video>` and `<iframe>` elements via `MutationObserver`. On hover, injects a fixed-position expand button (built with DOM API — `innerHTML` blocked by Trusted Types CSP on sites like YouTube). Expand: resizes entire ancestor container chain to `100vw × 100vh` (not the video element itself — YouTube continuously overwrites its inline styles); hides sibling elements via `visibility: hidden`; dispatches `window.resize` to trigger site's own resize handlers. Collapse restores all saved styles. Native video controls enabled on expand; `MutationObserver` restores them if site removes the attribute. Button repositions on scroll. 1 file changed (`preload-webview.ts`). |
| US-395 | Links Editor Secondary Panel Enhancements | — | 3 enhancements: (1) Swap button to promote/demote LinksEditor between main and sidebar with panel save/restore. (2) Editable Target dropdown in Edit Link dialog. (3) Centralized link opening through openRawLink pipeline — all paths now respect ILink.target; browserMode/browserPageId metadata for browser routing; removed dead selectedBrowser code and BookmarksDrawer monkey-patch. ~20 files changed. |
| US-325 | Fix preload script error in HTML preview | — | Disabled `nodeIntegrationInSubFrames` in BrowserWindow config. Electron was injecting the preload script into sandboxed HTML preview iframes, causing `module not found: node:path` console errors. Nothing depends on Node.js in iframes — browser automation uses CDP, webviews have their own preload. 1 file changed. |
| US-392 | Paste Rich Text as Markdown in Monaco Editor | — | Ctrl+Shift+V in markdown/html pages reads clipboard HTML and pastes as converted Markdown (or raw HTML). Uses Monaco `addAction` keybinding + Turndown (dynamic import) for HTML→Markdown. Regular Ctrl+V unchanged. 2 files changed (1 new: `paste-rich-text.ts`). |
| US-393 | Interactive Resource Templates in MCP Inspector | — | Resource templates (parameterized URIs) are now interactive: clickable in sidebar, detail panel shows name/URI/description/MIME, `TextAreaField` per `{param}`, "Read Resource" expands template and calls `readResource`, results shown via `ResourceContentView`. Mutual exclusion with static resources. 2 files changed. |
| US-394 | ClaudeSession Helper Class for Scripting | — | Exposes `ai.ClaudeSession` global to scripts wrapping `@anthropic-ai/sdk`. Auto-manages message history, tool-call loop (maxToolRounds=20 safety limit), events (tool-call, tool-result, assistant-message), and toolChoice per send(). 3 new files + 6 modified. |
| — | Browser tab UX improvements | — | Dark favicon glow (drop-shadow filter), tab activation history (close returns to previous tab), auto tab grouping with visual left border indicator, new tab (+) appends at end. 4 files changed. |
| US-360 | View Actual DOM — include iframe content | — | Moved DOM collection to main process via `collectDom` IPC channel. Uses `WebFrameMain.framesInSubtree` to collect all frame DOMs (incl. cross-origin), cheerio to inject content inside `<iframe>` elements. Both "View Actual DOM" and "Show Resources" updated. 3 files changed. |
| US-361 | Adopt libarchive-wasm for multi-format archive support | EPIC-018 | Replaced jszip (read) with libarchive-wasm. Supports RAR, 7z, TAR (+gz/bz2/xz), cab, ISO. jszip kept for ZIP writes. Added `ITransformer.writable`; `ContentPipe.writable` checks transformers. Renamed Zip* → Archive* (classes, files, IDs). New helpers: `getArchiveExtension()`, `isZipBasedArchive()`. WASM loaded from `assets/libarchive/`. ~20 files changed. |
| US-359 | Links panel improvements | EPIC-018 | Rich tooltips with copy-as-JSON, "Edit Link" context menu, navigation fixes (pipe passthrough, target resolution), ILink.target + ILinkMetadata.fallbackTarget/title, DataUrlProvider for data: URLs, inline script/style extraction, language detection from title, deduplication. 14 files changed (1 new). |
| US-358 | HTML resource extraction | EPIC-018 | `extractHtmlResources()` utility using cheerio via `require()`. "Show Resources" toolbar button on HTML pages + Browser context menu. WebScraperIcon. 4 files (1 new utility + 3 modified). |
| US-357 | LinkEditor as secondary editor — fixes | EPIC-018 | 5 fixes: cache write order in openLinks(), secondary editor restore for pages without mainEditor, page-level cache cleanup on close, link highlight in Links panel, CategoryEditor discovery via treeProvider duck-typing. 5 files changed. |
| US-356 | Multi-file drop handler | EPIC-018 | Single file drop: unchanged. Single folder or multiple items: `expandDroppedPaths()` recursively enumerates folders into ILink items with categories, then `pagesModel.openLinks()` creates a link collection page. 1 file changed (`GlobalEventService.ts`). |
| US-355 | Standalone link collection page | EPIC-018 | Added `app.pages.openLinks((ILink \| string)[], title?)` API. Creates TextFileModel with .link.json content as Pattern A secondary editor (never mainEditor). Categories panel in sidebar; clicking a link navigates the page. Passes pageId in RawLinkEvent metadata for in-page navigation. 7 files changed. |
| US-353 | Replace CategoryTree with TreeProviderView in LinkCategoryPanel | EPIC-018 | Replaced CategoryTree with TreeProviderView + LinkTreeProvider. Added getLabel and rootLabel props to TreeProviderView. Added hasSubDirectories/hasItems to ILink for accurate expand arrow visibility. categoriesOnly prop controls showLinks. 7 files changed. |
| US-354 | Consolidate ILink drag-drop into LinkDragEvent | EPIC-018 | Replaced LINK_DRAG + LINK_CATEGORY_DRAG + tree-provider-item with single LINK_DRAG_TYPE and LinkDragEvent carrying ILink[] + sourceId. 9 files changed. |
| US-352 | Clean up and unify link actions | EPIC-018 | Unified double-click → open link. Removed separate "open" buttons. Deleted dead ItemTile.tsx. PinnedLinksPanel: selection highlight, TreeProviderItemIcon. 7 files changed, 1 deleted. |
| US-351 | Secondary editor registration | EPIC-018 | LinkEditor registers panels in PageNavigator via pageNavigatorToggled event. Panel expansion sync. Pattern B guard. Conditional inline panels. 4 files changed. |
| US-348 | LinkEditor refactoring — browser removal, context menus | EPIC-018 | Removed browser selector. Renamed treeProviderContextMenu → linkContextMenu. Registered HTTP link handler. Refactored context menus to event channel pattern. 6 files changed. |
| US-349 | CategoryView uses LinksList/LinksTiles | EPIC-018 | Replaced CategoryViewRow/ItemTile in CategoryView with shared LinksList/LinksTiles. No type mapping (ILink unification). 7 files changed. |
| US-350 | ILink type consolidation | EPIC-018 | Consolidated ITreeProviderItem + LinkItem into unified ILink type. Renamed name → title. ~15 files changed. |
| US-346 | Extract LinksList and LinksTiles | EPIC-018 | Extracted view-only LinksList and LinksTiles from LinkItemList/LinkItemTiles. Callback-based API. 2 new files, 2 refactored. |
| US-345 | Shared panel components | EPIC-018 | Extracted Categories/Tags/Hostnames panels into shared components. 6 new files in panels/. 8 files changed. |
| US-344 | LinkTreeProvider | EPIC-018 | ITreeProvider adapter over LinkViewModel. list() enumerates sub-categories + leaf links. 1 new file, 1 modified. |
| US-343 | Make folder editor provider-agnostic | EPIC-018 | CategoryEditor scans secondaryEditors[] for matching treeProvider by type+sourceUrl. PageModel notifies mainEditor on secondary changes. 10 files changed. |
| US-342 | Test in Explorer — fixes and adjustments | EPIC-018 | Toolbar layout fix, root collapse guard, click behavior, ".." parent navigation, first tile switch fix. 5 files changed. |
| US-341 | Rename CategoryEditor → ExplorerFolderEditor + view mode | EPIC-018 | Full rename. FolderViewModeService with hierarchical inheritance. Per-folder view mode persistence. 14 files changed. |
| US-340 | CategoryView tile modes | EPIC-018 | Tile rendering for all view modes. Responsive grid, view mode toggle, favicon preloading. 1 file changed. |
