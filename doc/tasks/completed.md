# Completed Tasks

Last 20 completed tasks, newest first. Older tasks are pruned.

| ID | Title | Epic | Notes |
|----|-------|------|-------|
| US-920 | Fix the `QuestionIcon` dot vanishing at toolbar size | — | **Reported symptom:** the "?" info button that appears on the Browser toolbar only in Tor mode looked cut off at the bottom. **Not clipping.** The icon drew its dot as a zero-length stroke segment (`M12 19V18.99`) relying on `strokeLinecap="round"` to render a blob — an idiom that yields a dot of radius `strokeWidth/2`, here 0.75 units. The button renders the icon at 16px against a 24-unit viewBox, so the dot came out ~1px and antialiased away to almost nothing, leaving a bare hook that reads as truncated. The old path also ended mid-arc at `(8.12598, 8)` and left a 3-unit gap before the dot, which added to the truncated look. **Diagnosis method:** rendered the icon at 160/48/24/16px side by side against a candidate fix in a throwaway fixed-position div injected into the running app, then screenshotted — that is what showed the dot surviving at large sizes and disappearing at 16px specifically. Reasoning from the path data alone would have suggested the geometry was fine, since the glyph is vertically centred in its box. **Fix** (`src/renderer/theme/icons.tsx`): the dot is now a real `<circle cx="12" cy="18.5" r="1.25" fill="currentColor">`, the same idiom `WarningIcon` and `InfoIcon` already use for this feature — slightly larger than their `r="1"` because those render at dialog size while this one lives in a toolbar. The hook was redrawn as a properly closed arc feeding into a vertical stem. Also dropped a stray `xmlns` attribute on the `<path>`, leftover from an external SVG paste and meaningless on a child element. **Other consumer:** `AVGrid/HeaderCell.tsx` uses the same icon at 16px as the frozen-rows indicator, so it becomes legible there too; that is the only other usage. `tsc --noEmit` and `eslint` clean. **Closed without `/review`, `/document` or `/userdoc` at the user's explicit request.** **Untested at close:** the fix was verified as standalone SVG rendered at four sizes, not by reopening a Tor browser page and looking at the real toolbar button. |
| US-919 | Render archive-entry images in the folder-view tile modes | — | **Reported symptom:** opening a `.pptx` in the Archive panel, navigating to its `ppt/media` folder and switching the center view to a tile mode showed tiles with no images — only the fallback globe glyph. The user's hypothesis was that the tiles component doesn't support reading files from archives, and their suggested remedy — build a pipe and read the file through it — was exactly right. **Root cause:** `ArchiveTreeProvider.list()` already sets `imgSrc` for image entries, but it sets the archive href (`Evergreen-Iconography.pptx!ppt/media/image1.jpeg`). `LinksTiles` puts `imgSrc` straight into an `<img src>`, and Chromium has no idea what the `!` means, so the load fails and the tile falls back. **Why plain folders were never affected:** verified empirically in the running renderer rather than assumed — a raw absolute Windows path (`C:\…\icon.svg`) *does* load in this Electron `<img>`, so archives were the only broken shape. **Fix:** new `src/renderer/editors/link-editor/pipe-image-src.ts` exporting one hook, `usePipeImageSrc`. An archive-entry src (detected with `isArchivePath`, which checks for a known archive extension left of the `!` so a file merely *named* `foo!bar.png` isn't misrouted) is read through the existing `pipeFromSourcePath` — `FileProvider` + `ArchiveTransformer` — into a blob URL. Everything else (`http(s)`, `data:`, `blob:`, `file://`, plain local paths) passes through untouched. **Two deliberate scope decisions:** (1) plain local files are NOT piped — the DOM loads them natively with `loading="lazy"`, and routing them here would force a full in-memory read of every visible image for no gain; (2) the blob-URL cache is not incidental — tile grids are virtualized, so scrolling remounts the same cell repeatedly and every scroll would otherwise re-read the archive. The cache is capped at 200 entries with oldest-first eviction and `URL.revokeObjectURL` on the entry it drops, plus an in-flight map so two tiles sharing a source share one read and a failed-set so a broken entry isn't retried on every scroll. **Ordering:** the hook runs BEFORE `resolveTorSrc` in `LinksTileCell`, so the resulting `blob:` matches `tor-src.ts`'s `LOCAL_SRC_RE` and is never routed through the Tor proxy. **Verification:** live in the running dev app over MCP against the user's own `Evergreen-Iconography.pptx` — 20 blob-backed images on screen, 0 broken, natural dimensions correct (`image1.jpeg` 524×570, `image10.jpeg` 1056×567). `tsc --noEmit` and `eslint` clean. **Review:** no concerns. Took its one optional suggestion — `needsPipeImageSrc`, `getPipeImageSrcSync` and `resolvePipeImageSrc` were exported with no consumers outside the module, so they are now module-private and the public surface is the single hook. **Untested at close:** cache eviction past the 200-entry cap was not exercised — a scroll test to drive it landed on the wrong page because tabs were switched mid-run, so only the first screenful is confirmed. An entry evicted while still displayed would break that tile without recovering, which needs 200+ distinct archive reads while the cell stays mounted; the virtualized grid makes that unlikely but not impossible. |
| US-918 | Fix `Panel` dropping the `overflow` and `flex` shorthands | — | **Reported symptom:** in the MCP Inspector's Tools tab, selecting `execute_script` and widening the window let the parameter form grow, but narrowing it again left the content cut off on the right; `list_pages` resized correctly in both directions. The user's hypothesis was that the difference came from a "text area" field. **Investigation:** done live against the running dev app over MCP (`browser_evaluate` with `pageId: "app"`), measuring the real DOM rather than reading CSS. `mcp-tools-panel` measured **1587px inside a 1425px parent** — already overflowing by 162px. Both it and `mcp-tools-detail` declare `overflow="hidden"` in source, yet computed `overflow: visible` with `min-width: auto`, and both carried the *identical* Emotion class with a style attribute of only `flex-grow: 1; flex-basis: auto;` — the prop was never reaching the DOM. **Root cause — in `Panel`, not the MCP Inspector.** `Panel` builds one inline-style object holding both the `overflow` **shorthand** and its `overflowX`/`overflowY` **longhands**. React does not skip a style key whose value is `undefined`; it *clears* that property by writing `""`. So `{ overflow: "hidden", overflowX: undefined, overflowY: undefined }` applies the shorthand and then removes both longhands, which erases the shorthand entirely — confirmed in-page: setting `overflow: hidden` then clearing the two longhands leaves an empty style. The same collision hits `flex` + `flexShrink`, which is why the live DOM showed `flex-grow: 1; flex-basis: auto;` with **no** `flex-shrink` — two independent symptoms corroborating one cause, and a real defect on its own since a string form like `flex="0 0 auto"` silently became shrinkable. **Why only some tools reproduced it:** with `overflow` erased the panels computed `overflow: visible`, which is precisely the condition under which a flex item's `min-width: auto` resolves to a real min-content floor instead of 0. Monaco writes its own pixel width inline, pinning that floor at the widest size it ever reached — hence grow-but-never-shrink. `execute_script`'s `script` argument matches `CODE_FIELD_PATTERNS` in `ToolArgForm`, so it renders a Monaco editor, not the `Textarea` the user assumed; `list_pages` has no such field, and `Textarea` sets `overflow-wrap: anywhere`, making its min-content contribution negligible. **Fix:** a `compactStyle` helper in `src/renderer/uikit/Panel/Panel.tsx` strips `undefined` entries before the object reaches React — an absent key is never cleared, so the shorthands survive. Written as a `for-in` loop rather than `Object.entries` to avoid an entry-array allocation per instance per render in a primitive used on nearly every screen. React's update path still clears a property that disappears between renders and re-applies the new styles afterwards, so a shorthand that is still set wins on the next commit. **Verification:** live over MCP — forcing the inspector to 600px gave detail 394px with Monaco relaying out to 358px, and restoring the width returned it to 1193px (both directions, the exact failure the user reported). A post-fix sweep found 0 panels with an erased shorthand and 0 panels overflowing their parent. `tsc --noEmit` and `eslint` clean. **Blast radius — the change is ~10 lines but its reach is not:** **152 `overflow="…"` props across 68 files** had been silently no-ops, so panels meant to clip were spilling and panels meant to scroll were not scrolling. All of that is now live. Longhand `overflowX`/`overflowY` usages were never affected (only the shorthand collided). **Closed without `/review`, `/document` or `/userdoc` at the user's explicit request.** **Untested at close:** not visually inspected across the affected screens — any layout that was accidentally relying on the spill may change appearance, so a pass over the busier screens (Rest Client, Git panels, Link editor, Notebook) remains outstanding. |
| US-917 | Make Explorer file search truly background | — | **Reported symptom:** searching a large tree (`C:\projects\EverGreen`) froze the whole app. The panel's "Searching… N files" counter kept climbing, so the search was clearly progressing, but nothing else responded — the search couldn't be stopped from its own close button, pages couldn't be switched, and closing the app did nothing until Windows painted the window "Not Responding"; the only exits were waiting it out or killing the process. **Root cause 1 — main was blocked end to end.** `executeSearch` was declared `async` but contained **no `await`**: `readdirSync`, `statSync`, `readFileSync` and the `openSync` binary sniff are all synchronous, so the entire `while (dirStack.length)` walk ran inside one tick of the main-process event loop — which owns the window message pump, the `close` event, and every `ipcMain` handler. It also made cancellation *structurally impossible*: `isCancelled()` polled `activeSearches`, a map written only by the `SearchChannel.cancel` handler, which could not run until the loop it was meant to interrupt had already finished. Every cancel check in the walk was dead code. The counter still ticked because `sender.send` writes to the IPC pipe and the **renderer** drains it independently — which is precisely why the feature looked like a background job while being nothing of the kind. **Root cause 2 — the renderer was quadratic.** `TOneState.update` runs Immer `produce`, so `s.results.push(...)` copied the whole accumulated array on every arriving file; the new array identity then invalidated the view's `useMemo`, rebuilding the filtered rows and firing a full grid update and re-render. Three O(n) passes per file, unbatched. Fixing either half alone leaves the other symptom, so both were in scope. **Fix:** new `src/main/search-worker.ts` holds the walk, matchers, batching and cap and runs in a `worker_thread`; `src/main/search-service.ts` is now only the host — one worker per sender `webContents`, relaying messages. Cancellation is `worker.terminate()`, so the poll disappeared entirely rather than being repaired. A `sender.once("destroyed")` teardown was **added**, closing a pre-existing leak nobody could see: before this, a window closed mid-search left the walk running to completion. **Packaging — the one genuinely tricky part.** The worker is a fifth build entry emitted to `.vite/build/search-worker.js` (added to both `build-prod.mjs` and `dev.mjs`), but it is loaded as **source** via `fs.readFileSync` + `new Worker(src, { eval: true })` rather than `new Worker(path)`: `asar: true` packs `.vite/**` into `app.asar`, and a worker bootstraps its own Node environment whose module loader cannot be relied on to resolve an entry inside the archive, while `readFileSync` in main *is* asar-aware. This mirrors `board-protocol-service.ts`'s `getShimSource`, including caching only when `app.isPackaged` so a dev rebuild applies without restarting Electron. Two constraints follow from `eval`: the worker must never import `electron`, and the bundle must externalize only node builtins so `picomatch` stays inlined — verified on the emitted file, whose only `require`s are `node:fs`, `node:path`, `node:worker_threads`. **Batching:** results now stream as `SearchResultBatch` flushed at 100 ms or 50 files, whichever first, with the progress count riding the same flush — one IPC per flush instead of two per file. The renderer keeps accumulated rows in a plain field on the model and bumps a `resultsVersion` counter in state as the view's change signal. **Result cap (user's call):** 10 000 matched lines, hardcoded, no setting — beyond that a result list isn't reviewable by a human anyway, and it removes the memory risk that smooth batching would otherwise make *easier* to reach. Enforced in the worker (stop walking, `truncated: true`) rather than the renderer, so no messages are paid for and discarded; checked **between** files, never mid-file, so a file's `matchedLinesCount` badge can never disagree with the rows beneath it; surfaced in the status line, because a silently short list reads as a bug. **Added mid-implementation — the `search-exclude` setting.** Asked to check how files are filtered, investigation found the exclude defaults (`node_modules`, `.git`) already existed but were hardcoded and invisible: unlike `search-extensions` they had no settings key, and the panel's placeholder (`"Exclude (e.g. node_modules)"`) implied the field was what excluded them. The root-exemption behavior requested — searching *inside* `node_modules` should work while nested ones stay skipped — was verified to **already hold**, and to still hold after the change, because exclusion only ever sees child directory names (`matchDir`; the root is seeded into the stack directly) and root-relative paths (`matchFile`). `defaultExcludePatterns` became a `string[]` seeding the new setting, sent as `SearchRequest.excludePatterns` and merged ahead of the panel box. **Verification:** the built worker was driven from a plain Node harness over the same `readFileSync` + `eval` path the service uses, against the real tree — rooted at `…\web\node_modules`: 11 354 files examined, 264 matched, **0** from the 5 nested `node_modules`; rooted one level up at `…\web`: 106 examined. Both numbers identical before and after the settings change (the point: routing defaults through a setting altered no behavior), and a differential run proved the setting live (5 matched files → 2 when `src` was added to the excludes). `tsc --noEmit`, `eslint` and `npm run dist`'s bundle step all clean. **Review:** no concerns. **Deliberately not changed:** `matchFile` uses a substring test (`relPath.includes(name + "/")`), so a folder named `my_node_modules` also matches; `matchDir` does the real pruning with exact equality, so this only bites on odd names, and altering match semantics for user-supplied patterns deserves its own decision. **Untested at close:** not exercised in the running app — verified by inspection, typecheck, lint, a production build, and the out-of-app worker harness only. The packaged-asar load path in particular is the one thing dev mode cannot prove and remains unverified. |
| US-916 | Remember the last folder per dialog kind, globally | — | **Reported symptom:** the native open/save/folder dialogs started somewhere different each time — saving several files into one folder meant re-navigating there on every dialog. A few dialogs seemed to remember; most didn't. **Cause:** the starting folder was whatever the call site passed as `defaultPath`. A path carrying a directory pins that folder; a bare file name leaves the choice to the Windows shell's own per-executable MRU — so the "remembering" users saw was Windows', not ours: invisible, unresettable, shared across dialog kinds, with per-extension rules we don't control. **Fix:** new `src/main/dialog-folder-memory.ts` remembers the last-used directory per dialog kind (`open`/`save`/`folder`) in `electronStore` under `dialog.lastDir.<kind>`. `resolveDefaultPath` owns the precedence: an explicit caller directory wins → remembered dir → the new weak `location?: CommonFolder` param → the caller's value; a bare file name is treated as a suggested name and placed into whichever directory wins. `src/ipc/main/dialog-handlers.ts` — the one place all three dialogs are opened, reached by both the renderer `app.fs` API and the board bridge — resolves and records through it, so ~25 call sites were fixed with no call-site edits. **Why `location` is symbolic** (`CommonFolder`, the union already declared in `api-param-types.ts` and already resolved behind `Endpoint.getCommonFolder`): correct cross-platform by construction, callers needn't know the OS path, and it stays safe to expose to scripts/boards later because it can't smuggle an arbitrary path. **Why a weak param instead of "memory always wins":** every directory-bearing caller except downloads passes a *meaningful* location — text Save As passes the file's own path, the settings and board-path pickers pass the configured value — so a global precedence flip would have broken all of them. `download-service.ts` switched to `defaultPath: filename` + `location: "downloads"`, and calls the memory inline rather than via the handlers because `will-download` requires `setSavePath` before the handler returns and so must use `showSaveDialogSync`. Stale memory (folder deleted or drive detached) is ignored via a `statSync().isDirectory()` guard; a cancelled dialog never records. **Not in scope:** per-purpose slots (would need a `memoryKey` at each call site — the opposite of the goal), a literal `defaultDir` for computed weak defaults, and exposing `location` on the script-facing `app.fs` API or the board bridge. **Untested at close:** runtime behavior across a restart was not exercised — the change is verified by inspection, typecheck, and lint only. |
| US-915 | Light theme style fixes | — | **Reported symptom:** in the Light Modern theme, selecting a folder in the Explorer panel painted the row dark blue and the label white as expected, but the expand/collapse chevron stayed dark and nearly invisible, and the vertical level guides to the left of the row kept their light-gray color — subtle in the tree at rest, glaring against the blue. **Cause:** the focused-selection override lives on the `Tree` container and changes the row's background and `color`; the label follows because it inherits, but `TreeItem`'s `Chevron` declares `color: icon.default` and `Indent` declares `borderLeft: 1px solid border.light`, so both silently opt out of the state change. This is the general hazard, not a tree quirk — any descendant that sets a color of its own escapes a row-level override. **Fix:** one rule on `TreeItem`'s own styled block (`src/renderer/uikit/Tree/TreeItem.tsx`), scoped with the same `[data-focus-selection]:focus-within &[data-selected]` ancestor selector `rowFocusSelectionOverride` uses, so it activates in exactly the states the container override does — chevron takes `icon.selection` (hover included), guides take `borderLeftColor: transparent`. New `.tree-chevron` / `.tree-indent` child hooks target them; they express structure, not state, so Rule 1 holds. No new color token: a guide color readable against both the row and the selection background would need defining per theme, and the guides carry no information on the row the user is already looking at. **Scope:** the shared primitive, so every focus-aware tree is fixed — Explorer, Archive, Mneme, script library, link categories. **Deliberately not covered:** the blurred (unfocused) selection leaves the label at its default color, so both elements already read correctly there; `data-drop-active` repaints the row and has the same latent issue, but is transient drag feedback and was not reported. Opened as a running collection for a light-theme pass; closed with one finding, no others surfaced in use. |
| US-914 | Bookmark star must not wait on a busy page | — | **Reported symptom:** clicking the browser's star icon to bookmark a page sometimes did nothing — the "Add Bookmark" dialog never appeared. It coincided with the page still loading. The user kept clicking, kept surfing, and roughly a minute later *every* queued dialog opened at once and all had to be closed. **Root cause:** `handleStarClick` awaited `discoverImages()`, which awaited `webview.executeJavaScript(...)`. That call queues on the **page's** renderer main thread, not the app's, so a page mid-load leaves it pending for as long as the load takes and the whole handler stalls behind it; each extra click started an independent handler, and they all reached the dialog together when the page finally settled. The identical hazard had already been found and fixed once for the context menu's SVG probe (raced against 250 ms) — the bookmark path simply never received the same treatment, which is what made it worth generalizing rather than patching in place. **Fix (the user prescribed both the approach and the budget):** new `withTimeout(promise, ms, fallback)` in `core/utils/utils.ts` — clears its timer and maps a rejection to the fallback whether it arrives before or after the deadline, so an abandoned probe can never surface as an unhandled rejection. `discoverImages()` is bounded at `IMAGE_DISCOVERY_TIMEOUT = 1000`; on timeout the dialog opens with the URL, title and favicon (all already held in the browser's own tab state) and only the *suggested image* list is lost. The context menu's "Add to Bookmarks" had the same unbounded probe for its suggested title/image — bounded at `LINK_PROBE_TIMEOUT = 1000`, with a comment noting that `linkURL` comes from the context-menu params rather than the page, so the URL guarantee never depended on the probe. The pre-existing inline 250 ms `Promise.race` was refactored onto the shared helper as `SVG_PROBE_TIMEOUT`, so all three probes now read the same way. **Second half of the bug:** the timeout alone would still have produced three dialogs from three clicks, one second later — so `handleStarClick` became a `starClickBusy` guard around a new private `runStarClick()`. This is the part that actually fixes "I need to close them all". **Review:** no concerns; its one suggestion (document that `linkURL`, not `linkInfo`, carries the URL guarantee) was taken. **Docs:** `browser-editor.md` gained a "Bounding page probes" subsection that the three probe descriptions now link to, plus notes on the star guard and on image discovery being the only source that reads the live page; `coding-style.md` gained "Bound Awaits on a Thread You Don't Own" under Error Handling, since a `try`/`catch` protects against failure but not against never answering — with the re-entrancy guard as its companion rule. **Files:** `core/utils/utils.ts`, `editors/browser/BrowserBookmarksUIModel.ts`, `editors/browser/BrowserWebviewModel.ts`. |
| US-913 | Collapsing a tree folder collapses its expanded subfolders | — | **Reported symptom:** in the Explorer tree, collapse a folder that has an expanded subfolder, then click a file so the page navigates, then re-expand the folder — the subfolder shows an open chevron (down) with **no contents**, and only a collapse + re-expand reloads it. Notably it did *not* reproduce when merely collapsing/expanding other folders; the navigation click was the trigger. **Root cause — two layers disagreed about what "expanded" means.** `TreeProviderViewModel.buildTree()` re-lists children **only** for currently-expanded paths (deliberately: it refreshes them inline before publishing, so an FS-watcher tick can't shrink the tree to root-only rows and reset `RenderGridModel` scroll to 0). A collapsed ancestor is therefore not in that set, its children are never re-listed, and a grandchild is not even reachable via `findNode` — its `items` reverts to `undefined`. Meanwhile `TreeModel.state.expanded` still held `true` for that grandchild, since collapsing the parent never touched it. Result: an open chevron over nothing. The rebuild that exposed it fires on the navigation click, which is why the pure expand/collapse sequences looked fine. **Fix — chosen deliberately over the alternative.** Keeping the collapsed subtree's children loaded was rejected in favor of the inverse: collapsing a row now closes its whole subtree, so re-expanding always reveals closed subfolders. New opt-in `TreeProps.collapseDescendants`; when a toggle collapses a row, `TreeModel.toggleAt` collects every descendant `value` via the new private `collectDescendantValues(source)` (walks the resolved SOURCE subtree — so collapsed-but-loaded descendants are reached — excludes the row itself, includes leaves) and writes `expanded = false` for all of them in the **same** `state.update` as the toggled row: one render, no intermediate state. This makes the persisted `expandedPaths` set self-consistent with what `buildTree` can actually reach. `onExpandChange` still fires **once**, for the toggled row only; consumers persisting expansion must read `getExpandedMap()` (already updated when the callback runs) rather than accumulate per-node callbacks — `TreeProviderViewModel.getState()` already does exactly that. **Scope:** opt-in, enabled only on `TreeProviderView`. Review confirmed the other `Tree` consumers (Boards, Tools, Git refs, Notebook categories, Rest Client) hand `Tree` a fully-materialized array every render, so a collapsed row's children are never dropped and the bug cannot occur there — opting them in would be pure UX preference. Mneme's tree renders *through* `TreeProviderView`, so it inherits the fix with no separate opt-in. Review also traced the inflight-load race: a descendant mid-`loadChildren` when its ancestor collapses settles collapsed-with-loaded-children, because `runLoadAndExpand`'s resolve path only clears `loading` and bumps `revision` — it never forces `expanded` back to `true`. **Note for testers:** expansion state persists across restarts, so a folder already left in the stale state keeps it until collapsed once more. Files: `uikit/Tree/types.ts`, `uikit/Tree/TreeModel.ts`, `components/tree-provider/TreeProviderView.tsx`. |
| US-912 | Tor pages must fail closed (no direct-network fallback) | — | **Reported symptom:** when the Tor daemon failed, a Browser (Tor) page kept loading pages over the normal network — caught three times in one day. The captured "Tor connection" dialog read `Could not reach check.torproject.org: net::ERR_SOCKS_CONNECTION_FAILED`. **Ruled out first:** the daemon in use on that machine is `tnet` (`C:\projects\tnet`, an Arti-based drop-in that `tor.exe-path` points at). It is **not** at fault — it fails closed by design, replying SOCKS `0x01` on a Tor connect failure (`tnet/src/main.rs:220-223`), which is exactly the error above, and it hands domains to Tor unresolved (`atyp 0x03`), so there was no local-DNS leak either. **Root cause — the partition was fail-OPEN.** `setProxyForPartition` was called only from the **success** branch of `startForPartition`, so until `Bootstrapped 100%` — and forever after a failed bootstrap — the page's Electron session carried **no proxy**, and an unproxied session in Chromium is DIRECT. Absence of a proxy is not a neutral state. Three distinct behaviors followed: (1) **every Tor page opened with a URL leaked its opening navigation** — `showBrowserPage` called `addPage(model)` and only *then* `initTorProxy()` un-awaited, while `addPage` mounts `<webview src={initialUrl.current}>` which fetches immediately, against a cold Arti bootstrap of 5–30 s; the Tor overlay hid this visually but stopped nothing; (2) **a failed bootstrap left the page permanently unproxied** — the URL bar, bookmarks and every new tab went out direct, since no navigation site consulted `torStatus`; (3) **a daemon dying after bootstrap kept a green dot** — `child.on("close")` cleared `running` but never broadcast, as only `restart()` called `broadcastStatus`; this case does *not* leak (the proxy rules stay on the session, so requests fail closed — which is precisely the captured screenshot) but it is why the failure was only discoverable by opening the dialog. **Fix — apply the proxy *before* the daemon exists,** rather than gating every navigation site. `src/ipc/tor-ipc.ts`: new `tor:arm` channel. `src/main/tor-service.ts`: `armPartition(socksPort, partition)` points the session at a SOCKS port nothing is listening on yet, turning the bootstrap window into `ERR_SOCKS_CONNECTION_FAILED`; the two start-outcome branches were collapsed into `settleStart`, which applies the proxy on **both** outcomes (on success that puts traffic on Tor, on failure it keeps the partition failing closed); `child.on("close")` now broadcasts `"error"` when `isCurrent(child)` still holds. `src/renderer/editors/browser/BrowserEditor.ts`: `armTorProxy()`. `src/renderer/api/pages/PagesLifecycleModel.ts`: arming awaited before `addPage`, bootstrap left un-awaited after it. **Deliberate decision — no navigation gating.** Refusing `navigateWebview`/`addTab` while `torStatus !== "connected"` is redundant once the partition is armed, and it would create a second source of truth for "may this load happen" across several call sites. The armed proxy is the single enforcement point — but that reasoning only holds if arming is *guaranteed*, which is what `/review` caught. **`/review` found one must-fix, and it was correct:** `reconnectTor` called only `initTorProxy()`, which reaches `setProxyForPartition` only *after* the daemon settles — so on a cold daemon it reopened the same 5–30 s bare-partition window. Following that thread found the worse half the reviewer's scenario implied: the **session-restore path never armed at all**. `applyRestoreData` resets a restored Tor page's tabs to `about:blank` and waits for an explicit Reconnect (correct, and long-standing), but the URL bar is never disabled and `TorStatusOverlay` renders only inside the webview panel, not over the toolbar — so typing a URL before reconnecting navigated a completely unproxied partition. **Resolution:** arming moved into `BrowserEditor.restore()`, which runs on *every* path that produces a Tor page, restore included, and always before `addPage`. `armTorProxy` became **idempotent** (flag set only on success, so a failed attempt is retried rather than remembered as done) because three callers must each guarantee it without being able to see whether another did: `restore()` always runs and records failure into `torStatus` rather than throwing, so a failed arm never drops a page from a session restore; `showBrowserPage` re-asserts it and refuses to open the page on failure, since a Tor page that cannot be proxied would browse normally and no page is the correct outcome; `reconnectTor` covers a partition whose earlier arming failed. **Verified by the reviewer, not assumed:** `armPartition` correctly stays out of `activePartitions` (`isActiveTorPartition` means "live *and bootstrapped*" for the `tor-src://` handler and `checkIp`, and an armed partition is neither), `settleStart`'s failure branch cannot reject the invoke (the `setProxyForPartition` failure is caught and logged), and both deliberate-stop paths (`stopTorProcess`, `stopTorProcessAndWait`) null out `torProcess` synchronously before the killed child's `close` fires — so the new unexpected-death broadcast cannot produce a spurious red dot during a normal restart or shutdown. The reviewer's one optional note, that `this.socksPort` is written by three entry points, was answered with a comment: it is sound only because the port is a single global setting feeding a single shared daemon, and per-partition ports would need a `Map<partition, port>`. **Known gap, left by design:** a daemon that stays *alive* while its circuits fail still keeps a green dot — that is the captured screenshot's exact case. Detecting it needs active probing; it is a status-honesty gap, not a leak, since such requests already fail closed. **Accepted behavior change:** a Tor page opened with a URL now *fails* that first navigation behind the overlay and needs a reload once the dot turns green. A visible failure is strictly better than the silent success that was the leak. **Not verified at runtime by me:** the only decisive test navigates a Tor page to a real URL, and had the fix had a hole the test itself would have leaked the user's real IP, so it was left to the user's manual pass. Testing also requires a **full app restart** — three of the changed files are main-process or reach it over IPC, and the dev server's HMR only reloads the renderer, so until a restart every Tor page open fails with the new "Could not secure the Tor session" notification, which is the fail-closed path working. `/document`: `doc/architecture/browser-editor.md` (new **"The partition fails closed"** subsection — the arming order and why it is load-bearing, the idempotency and its three callers, and the remaining live-daemon gap; plus `tor:arm` in the channel list and two Key Files rows), root `CLAUDE.md` (`tor-service.ts` row carries the invariant). `/userdoc`: `docs/browser.md` (new **"Nothing loads outside Tor"** subsection, written around what the user sees), `docs/whats-new.md` (v4.0.18 Bug Fixes, leading with the security framing and stating the behavior change), `docs/api/pages.md` (`showBrowserPage({ tor })` now documents refuse-to-open and that a `url` will not load until Tor connects). `tsc --noEmit` + `eslint` clean. Task folder deleted on completion. |
| US-903 | Theme-cycling hotkeys (Ctrl+Alt+[ / ]) work while focus is inside a board frame | — | **Symptom:** with focus anywhere inside a board, `Ctrl+Alt+]` / `Ctrl+Alt+[` did nothing — the theme did not change and nothing was logged. Clicking out into the app first made them work again. It matters more than a stray shortcut normally would, because cycling themes is precisely what an author does *while* looking at a board, to check its `--p-*` styling. **Cause:** not the shortcut handler — the keystroke never arrives. The app's global `KeyboardService` listens on the **host document**, and a board is a cross-origin `board://` iframe, so SOP means its `keydown` never propagates to the host. Nothing was broken; the event simply cannot cross that boundary. **Fix:** forward the two keys the same way `Ctrl+S` was already forwarded — a `window`-level bubble-phase listener in `src/board-shim.ts` posts `{ __persephone: "board:cycleTheme", direction: ±1 }` to the parent, and `BoardWebview`'s existing message handler (already origin- and source-validated for every branch) calls the theme cycler. Reusing the established Ctrl+S shape also inherits its opt-out contract: a board that binds either combination itself calls `preventDefault()` and the forwarding stands down. **Two deliberate choices.** The cycling logic was extracted from `KeyboardService` into `api/cycle-app-theme.ts` rather than duplicated, so the host path and the forwarded path cannot drift on ordering, wrap-around, or persistence to settings. And unlike `board:setStatusText` — main-frame-only, because the footer is main-view state — `board:cycleTheme` is accepted from **any** board frame, including a secondary sidebar panel: the app theme is global UI state, not something a secondary view could improperly drive. **Files:** `src/board-shim.ts`, `src/renderer/api/cycle-app-theme.ts` (new), `src/renderer/api/internal/KeyboardService.ts`, `src/renderer/editors/board/BoardWebview.tsx`, `src/ipc/board-bridge-channels.ts`. Reviewed clean; the only note was the absence of automated coverage for the new message branch, which has no precedent in this repo. |
| US-902 | Markdown opens in Preview by default; board editor-switch list no longer changes shape | — | **Reported symptom (a different bug than the one that got fixed first):** with a trusted test board registered for `DASHBOARD.md`, opening that file showed three switch segments — *Text Editor*, *Preview*, *Dashboard (mask test)* — but selecting the **board** made **Preview disappear**, leaving only *Text Editor* + the board; switching back to Text Editor brought Preview back. So there was no way to go board → Preview directly. **Root cause of the widget instability — two different questions being asked.** The switch widget (`SwitchWidget`, `editors/base/PageToolbar.tsx`) asks the **active** editor for the built-in options via `model.findCompatibleEditors()` and then appends board segments. A built-in text editor answers with `editorRegistry.findEditorsAccepting(host)`, which evaluates the **live content host** and therefore knows the file's `language` — the only way a **language-only** matcher can ever be named. `md-view`'s matcher was exactly that: `switchOption: (lang) => lang === "markdown" ? 10 : -1` plus `validForLanguage`, and **no `acceptFile` at all**. A *simple* (non-content-host) board never loads the file into a host, so `BoardEditorModel.findCompatibleEditors()` returned only `[editorRegistry.resolveId(filePath) ?? "monaco", boardEditorId(root)]` — and `resolveId` consults `acceptFile` **only**, so it could not return `md-view` under any circumstance; monaco's priority-0 floor always won. Preview was therefore structurally unnameable from the board. **Options weighed:** (A) derive the language from the extension inside `findCompatibleEditors` and take `getSwitchOptions` too — local, no resolution-semantics change; (B) give `md-view` a real `acceptFile`; (C) remember the previous editor's option list in board state — rejected as fragile (empty again after restart or a direct board open). **The user chose (B) and widened the scope deliberately:** they had independently wanted Markdown files to *open* in the rendered Preview, since they use Persephone as a docs viewer and let AI agents do the editing. Both (A) and (B) were then implemented — (B) for the default-open behavior, (A) because it is still needed for every *other* language-only editor (`html-view`, `mermaid-view`, `svg-view`) and for markdown while the board is active. **Fix — 2 files.** `editors/base/editor-matchers.ts`: `md-view` gains `acceptFile: (fn) => (isMarkdownFile(fn) ? 10 : -1)`, where the new module-local `isMarkdownFile` takes the file's last extension and asks the shared Monaco language table (`getLanguageByExtension`, `core/utils/language-mapping.ts`) whether it maps to language id `markdown` — so `.md`, `.markdown`, `.mkd`, `.mdown`, `.mkdn`, `.mdwn` are all covered with **no duplicated extension list**. `editors/board/BoardEditorModel.ts` `findCompatibleEditors()` now derives `language` from `fpExtname(filePath)` via the same table, takes `editorRegistry.getSwitchOptions(language, filePath).options`, appends the `resolveId` winner if the language pass missed it, then the board id. **Why priority 10:** above monaco's 0 floor, below the compound-name 20-tier (`*.grid.json`, `*.note.json`, `*.rest.json`, `*.link.json`, `*.fg.json`, `*.log.jsonl`, `*.env.json`) and below the viewer tiers (draw 50, pdf/image/archive/video 100, category 200) — so a compound name always still outranks markdown. **Consequence for board authors (the one breaking-ish detail):** `resolveEditorIdForFile`'s `builtinPriority` for a markdown file is now 10 rather than 0, so a board claiming a `.md` file needs `editorPriority` **> 10** — not merely > 0 — to open by default; ties still go to the built-in. All seven published catalog boards were checked, none claims markdown. **Non-obvious details confirmed rather than assumed:** `getSwitchOptions` returns a **fresh** array per call (`results.map(...)`), so the in-place `builtins.push` mutates nothing shared; it deliberately returns `[]` when fewer than two options apply, which the `resolveId` append covers; it sorts **ascending** (monaco leads) whereas `findEditorsAccepting` sorts **descending** — the board list is now monaco-first, matching what the user sees on the built-in editors, and the only consumers are membership checks (`PageWrapper.compatibleEditorIds`) and the widget's id→segment map, whose sole order-sensitive step (moving the Board Info `+` segment last) is untouched. `md-view` has `hasContentHost: true`, so `resolveId` now returning it keeps `newTextFileModel`/`buildEditorById` on the correct text-host path — that is in fact the mechanism that makes markdown open into Preview. `getPreviewEditor` is unchanged in output for markdown (both `switchOption` and the new `acceptFile` yield 10). **Verified live over MCP on the dev build:** `resolveId` returns `md-view` for `DASHBOARD.md` and `notes.markdown`, still `monaco` for `.txt`, `grid-json` for `*.grid.json`, `pdf-view` for `.pdf`; on the fixture `dev/tasks/DASHBOARD.md` all three segments stay present and in stable order on **each** of the three editors (snapshotted via the `pageId: "app"` automation target); board → Preview renders the markdown (`editor` = `md-view`); and a fresh open of the board-unclaimed `dev/DASHBOARD.md` lands in `md-view`. `/review` clean (0 concerns, 0 suggestions — it independently traced every `resolveId`/`resolve`/`getSwitchOptions`/`findEditorsAccepting` consumer). `/document`: `editors.md` (**rewrote** the Editor Resolution priority list as an accurate tier table — it had claimed content detection scores 90 and participates in `resolve`, when `resolve` is `acceptFile`-only and detection scores 60 inside `accepts()`; added the markdown-10 tier and its rationale; updated the `editorPriority` bullet; corrected the `monaco` and `md-view` catalog rows; new "The switch list while the board is active" paragraph recording the `findEditorsAccepting`-sees-language vs `resolveId`-does-not asymmetry; fixed three EditorRegistry API signatures), `standards/editor-guide.md` (added the missing `acceptFile` row + how it differs from `switchOption`; replaced the Priority Guidelines list, stale on three of six entries), `CLAUDE.md` Key Files (new `editor-matchers.ts` row — the ladder had no pointer). Authoring docs: `assets/board-template/CLAUDE.md` (full ladder on `editorPriority`; also fixed its `folderMasks` example, which said a non-matching `DASHBOARD.md` falls to Monaco — now Preview), `assets/mcp-res-boards.md` (same facts, condensed, kept in agreement). `/userdoc`: `docs/editors.md`, `docs/boards.md`, `docs/whats-new.md` (v4.0.18). `tsc --noEmit` + `eslint` clean. No task folder (small change, auto-tracked). |
| US-901 | Markdown anchor navigation (`#fragment` links) | — | **Symptom:** in the Markdown (Preview) view, every link carrying a `#fragment` was broken. A cross-document link such as `[#RTB.RUL.2](../entities/RTB-Routing-Table.md#rules)` raised *"File not found: RTB-Routing-Table.md#rules"*, and a same-document `[x](#details)` link did nothing at all. **Two independent root causes.** (1) `resolveRelatedLink` (`core/utils/path-utils.ts`) deliberately splits the fragment off, resolves the path, then **re-appends** it to the `file://` URL; the US-784 in-page click interceptor (`MarkdownBody`) forwarded that whole href into `openRawLink`, and the Layer 1 file parser ran `normalizeFileUrl` → `decodeURIComponent` over it, so `#rules` became part of a *file path* that then failed `navigatePageTo`'s existence check. Note the asymmetry that made this look inconsistent: a **Ctrl+click** was never intercepted, fell through to `will-navigate`, and `fileURLToPath()` discarded the fragment — so it always opened fine, just without scrolling. (2) `MarkdownBlock` ran only `remark-gfm` + `rehype-raw` + `rehypeHighlight` — there was no `rehype-slug` equivalent and **no `id` on any rendered heading**, so even a correct fragment had no target. **Design — fragment as pipeline metadata,** reusing the `revealLine`/`highlightText`/`diffFrom` precedent rather than patching the click site. The load-bearing safety rule: the fragment is split off **only for real URLs** (`file://`, `mneme://`), never a bare path — in a URL a literal `#` must be `%23` (and `pathToFileURL` encodes it), whereas `#` is legal in Windows names (`C:/notes/C#/readme.md`). **Fix — 11 files + 1 new.** `api/types/io.link-data.d.ts` (+ the auto-copied `assets/editor-types/` twin) adds ephemeral `fragment`; `shared/link-data.ts` adds it to `EPHEMERAL_FIELDS`; `content/parsers.ts` gains `splitUrlFragment`, applied in the file, archive and `mneme://` parsers (**not** HTTP — there the fragment belongs in the URL); `content/open-handler.ts` threads it into both open paths; `editors/base/EditorModel.ts` declares optional `revealFragment?(fragment)` (same opt-in shape as `onNavigationReuse?()`); `api/pages/PagesLifecycleModel.ts` accepts it on `openFile` + `navigatePageTo` and calls it at **all five exits** — two fresh builds plus the three reuse short-circuits (`matchesNavigationTarget`, `findEditorByFilePath`, `openFile`'s existing-page dedupe), because an anchor into an already-open document is still a jump request; `api/pages/PagesModel.ts` for option parity; `editors/markdown/MarkdownEditor.ts` implements the hook by queueing an `anchor` `ComponentQueue` event (buffers until the body mounts); **new** `editors/markdown/rehypeHeadingIds.ts` emits GitHub-style slug ids with `-1`/`-2` dedupe and exports `slugifyHeading`; `MarkdownBlock.tsx` registers the plugin and adds `scrollToAnchor` to its imperative handle; `MarkdownBody.tsx` handles the queue event with a `requestAnimationFrame` retry ladder and intercepts same-document `#` links (scroll only — no `openRawLink`, no back-stack push). **Deliberately NOT added to `skipPreview`:** `revealLine`/`highlightText` force Monaco because they address lines; a fragment must keep the preview editor that renders the headings. **Tolerant resolution — 3 passes:** exact id, case-insensitive id, then `slugifyHeading(fragment)` vs `slugifyHeading(heading.textContent)`. The third absorbs the dialect gap — Azure DevOps writes `#rtb.rul.2` where GitHub slugs `rtbrul2`; `slugifyHeading` also collapses hyphen runs (where github-slugger keeps them) precisely so both sides meet. Queries are scoped to the block's root, never `document`: several Markdown views mount at once **and** the Minimap clones the rendered content, so ids are not document-unique. **Bug found only by live testing — the PV4 clobber.** The scroll ran, then was silently undone: every navigation sends `onFocus` immediately after `revealFragment`, and `MarkdownBody`'s scroll-restore reset `scrollTop` to its remembered `0`. Instrumented tracing (queue → attempt → success, yet final `scrollTop` 0) pinned it. Two adjustments: `scrollToAnchor` now scrolls **synchronously and instantly** (unlike `scrollToMatch`'s smooth microtask — an anchor is a starting position, and the caller needs the final value), and on success the body records the resulting `scrollTop` into the restore ref so PV4 reinstates the anchor instead of the top. **Verified live over MCP on the dev build:** cross-document jump lands the heading at container-top offset 0; the reuse path (second anchor in the already-open doc) scrolls; `#RULES` resolves case-insensitively; `#rtb.rul.2` resolves via the slug pass; same-document `#details` and `#rtb.rul.2` dispatched as real clicks scroll in place; a nonexistent fragment opens at the top with no error; heading ids emitted as `system`/`rtbrul2`/`details`; and the regression case `C:/.../C# notes/read#me.md` opened by bare path extracted no fragment and loaded full content. A 16-case `slugifyHeading` harness passes (dialects, accents, emoji, dedupe). **Note for future debugging:** HMR re-evaluates `PagesLifecycleModel` but the live `pagesModel` singleton keeps its old lifecycle instance, so changes there need a renderer reload to take effect. `/review` clean (0 concerns). `/document`: `content-pipeline.md` (Layer 1 fragment extraction + **corrected** the now-false claim that hints are never consumed on a reuse path), `pages-architecture.md` (`revealFragment?()` in the optional-hook family; section renamed "Markdown in-page **navigation**" — its "`#anchors` left untouched" statement was false — plus resolution/timing prose), `browser-editor.md` (inbound link), `folder-structure.md`, `CLAUDE.md` Key Files. `/userdoc`: `docs/editors.md` (Markdown Preview — replaced the stale "anchors open as before" claim), `docs/whats-new.md` (v4.0.18). `tsc --noEmit` + `eslint` clean. Task folder deleted on completion. |
| US-900 | `folderMasks` — folder scope for a board's custom-editor `fileMasks` | — | **Goal:** register a custom-editor board for a *specific* path shape rather than a bare file name — the motivating case was a dashboard editor for `*/tasks/DASHBOARD.md` that must not claim every `DASHBOARD.md` on disk. **Finding:** mask matching was **basename-only by design** (`fpBasename` before `matchesFileMask` in both `custom-editor-registry.ts` and the published-catalog matchers), so a path-shaped mask could never match — but the two points that actually *decide* which editor opens a file (`resolveEditorIdForFile`, the editor-switch widget) already hold a **full path**, which is what made the fix small. **Design (user's call):** a separate `folderMasks` axis that only **narrows** `fileMasks`, rather than teaching `fileMasks` about paths. **Fix — 11 files.** `editors/board/board-manifest.ts` holds the whole mask surface: `folderMasks?` on the manifest + `BoardEditorAssociation`, `normalizeFolderMasks` (lowercase, `/`-unified, leading `./` and outer slashes stripped, deduped; no bare-name coercion — a plain `tasks` is already meaningful), `folderMaskToRegExp`, `matchesFolderMask`, and `matchesBoardMasks` — the single predicate every consumer now calls instead of testing masks itself. Semantics: a folder mask is a path **suffix** (so it need not spell out the drive), matched case-insensitively against the file's parent folder, and separator-aware — `*`/`?` stop at `/` while `**` crosses, which is what makes `*/tasks` mean exactly one segment above `tasks`, `tasks` any depth, `**/dev/tasks` span segments, and `c:/projects/acme/**` scope a board to one tree (the tree root itself is not matched — add it as a second mask). Narrowing only: `getBoardEditorAssociation` still returns null on empty `fileMasks` even when `folderMasks` is present. Consumers: `custom-editor-registry.ts` (both matchers, `CustomEditorMatch.folderMasks`) and `api/published-boards.ts` (new module-level `matchesCatalogMasks` shared by both catalog matchers). Catalog/IPC parity: `ipc/api-param-types.ts`, `main/published-boards-service.ts` (same `Array.isArray` + `typeof` guard as `fileMasks`), `api/boards.ts`, `api/types/boards.d.ts` (+ the auto-copied `assets/editor-types/` twin). Display: `board-info/BoardInfoEditorModel.ts` + `BoardInfoEditorView.tsx`, where the chip markup was extracted into a `MaskChip` so the "Editor for" row reads *file chips → in → folder chips → (editorKind)*. **Accepted trade (user's explicit decision):** the file **icon** stays file-mask-only — icon surfaces receive a display *name*, not a path, so `matchesBoardMasks` **skips** (rather than fails) the folder gate when given a bare name. Every name-matching file therefore shows the board's icon even outside the folder scope; an icon is cosmetic and the editor-deciding paths always honor the scope. **Implementation notes:** `**` is parked on a NUL sentinel before the single-`*` pass so the passes can't chew through each other — an earlier draft used a space, which would have corrupted masks containing spaces (legal in folder names, and covered by a test case). Folder-mask *examples* inside the doc comments must be written `*\/tasks`, since a bare `*/` closes a block comment. **Verified** with a 19-case glob harness (all pass), including the real path `C:/projects/EverGreen/TelephonyApi/dev/tasks`, trailing slashes, `\` separators, case-insensitivity, `mytasks` correctly *not* matching, `*` not crossing separators, `**` spanning, and spaces. `/review` clean (0 concerns). `/document`: `editors.md` (Custom-Editor Boards — the `folderMasks` field plus the mask-predicate, glob-semantics and icon-trade paragraphs), `folder-structure.md`, `CLAUDE.md` Key Files. `/userdoc`: `docs/boards.md` (field table + new "Scoping to a folder" subsection), `docs/whats-new.md` (v4.0.18). Authoring docs updated in-task: `assets/board-template/CLAUDE.md`, `assets/mcp-res-boards.md`. `tsc --noEmit` + `eslint` clean. **Follow-on fix, found while testing this in-app:** the feature's own motivating mask `fileMasks: ["DASHBOARD.md"]` matched **nothing**. `normalizeFileMasks` coerced *every* wildcard-free entry into a suffix mask, so `"DASHBOARD.md"` became `"*.dashboard.md"` — an exact-filename association was impossible to express, and the example had been written into five docs as if it worked. Pre-existing (not introduced here), but it invalidated the documented case, so it was fixed in the same change set: a wildcard-free entry is now read **by shape** — starts with a dot or has no dot at all → an extension (`"drawio"` / `".drawio"` → `*.drawio`, `".grid.json"` → `*.grid.json`, unchanged); a dot **inside** it → a whole file name kept exact (`"DASHBOARD.md"` → `dashboard.md`). Accepted behavior change: a starless compound extension (`"grid.json"`) now means the exact file name rather than `*.grid.json`; all seven published catalog boards were checked and every one already uses an explicit `*.ext` mask, so none is affected. An extension-less bare word (`"Makefile"`) stays ambiguous and is still read as an extension — recorded as a known limit in the function's doc comment. Docs corrected in `editors.md`, `docs/boards.md`, `assets/board-template/CLAUDE.md`, `assets/mcp-res-boards.md`. **Verified live over MCP** with a purpose-built board (`fileMasks: ["DASHBOARD.md"]`, `folderMasks: ["*/tasks"]`, `editorPriority: 200`) against a fixture tree: `dev/tasks/DASHBOARD.md` → `board-editor:<root>` (claimed), `dev/DASHBOARD.md` → `monaco` (folder gate excluded it), `dev/tasks/MYDASHBOARD.md` → `monaco` (exact-name mask not a suffix), and the real `EverGreen/TelephonyApi/dev/DASHBOARD.md` → `md-view` (correctly unclaimed — its folder is `dev`, not `tasks`). A 30-assertion mask harness passes, including that every catalog mask round-trips unchanged. No task folder. |
| US-899 | "Copy board path" context-menu item in the board lists | — | **Goal:** a board's root folder path was only reachable by opening the board and reading the in-board toolbar, or by hunting for the folder in the Explorer — nothing copied it to the clipboard. Add a **Copy board path** item to the context menu of both board lists. **Change — 2 files.** `src/renderer/editors/explorer/BoardsSecondaryView.tsx` (Explorer-sibling **Boards** panel): new item between *Open in New Tab* and *Delete Board*, with `CopyIcon` — that menu's items all carry icons. `src/renderer/ui/sidebar/TrustedBoardsList.tsx` (**Tools & Editors** / trusted-boards tab, which also backs the Tools & Editors hub page): new item after the conditional *Update to vX* and before *Remove*, icon-less to match that menu's existing items; *Remove* also gained `startGroup: true` so the destructive action now sits below a separator, as in the other menu. Both use `navigator.clipboard.writeText(root)` — the same pattern as `filePathMenuItems`' "Copy File Path" in `editors/shared/editor-menu-items.tsx` and ~15 other call sites. Both menus ride the shared `BoardsTree` `getBoardContextMenu` slot, so `BoardsTree.tsx` itself is untouched; the third `BoardsTree` consumer (the in-board toolbar board-switcher popover) passes no board menu and is unaffected. `/review` clean (0 concerns). `/document`: no changes — no new pattern, no new file, and the panels' Key Files rows describe their trust-affecting actions rather than enumerating menu items; the `assets/` board docs cover the board-authoring surface, which host-side panel menus are not part of. `/userdoc`: `docs/boards.md` ("Managing boards" quick-reference row), `docs/whats-new.md` (v4.0.18 Upcoming — New Features). `tsc --noEmit` clean. No task folder (small enhancement). |
| US-895 | `browser_click { ref }` threw on a StaticText ref | — | **Symptom:** every `browser_*` ref action — click, hover, type, select_option, focus — failed with `TypeError: this.scrollIntoView is not a function` (`at Text.<anonymous>`) when the ref came from a `StaticText` line of an accessibility snapshot. It read as "the automation tools are broken" precisely when an agent did the documented thing: snapshot, then click the ref it got back. **Root cause:** a ref is a CDP `backendDOMNodeId`, and a `StaticText` AX node's id backs a DOM **text node**, which has none of the `Element` methods every command body calls. `DOM.resolveNode` resolves a text node happily and `Runtime.callFunctionOn` binds `this` to it. Not an edge case: in list-style UI built from roleless `<div>`/`<span>` rows the ancestors resolve to the `generic` role and are dropped by `SKIP_ROLES`, so the StaticText ref is often the **only** ref on the row — making such rows unclickable by ref at all (sidebar panels, custom trees, tag chips). The CSS-selector branches were never affected — `querySelector` can only return an `Element`. **Fix — 1 file:** `src/renderer/automation/ref.ts`. `callOnRef` now wraps the caller's function declaration so `this` is coerced (`this.nodeType === 1 ? this : this.parentElement`) and invokes it via `(fn).call(el)`. Acting on the element that displays the text is what the snapshot line denotes, and since DOM events bubble, dispatching on the inner `<span>` still reaches a handler bound to the surrounding row. A ref with no element parent now throws a message naming the ref and node type instead of a `TypeError`. **Why that location:** `callOnRef` is the sole chokepoint — all five element-assuming paths route through it (`commands.ts` click/hover/selectOption, `input.ts` focus-by-ref/fill-by-ref), `resolveRef` has no callers outside `ref.ts`, and a repo-wide grep for `backendNodeId`/`parseRef` outside `ref.ts` returns nothing. No call site changed; the wrapper costs **no extra CDP round trip** (it rides inside the existing `Runtime.callFunctionOn`), and `returnByValue`/`awaitPromise` survive because the inner call's value is returned — which `focusElementByRef` depends on. **Rejected alternative:** minting the ref from the nearest element ancestor in `snapshot.ts` would break ref uniqueness — sibling text nodes under one element collapse onto the same id, and ordinary prose with inline markup (`first text <b>bold</b> second text`) produces exactly that, so all but one of those lines would become unaddressable. Ref minting and the `SKIP_ROLES`/semantic-ancestor logic are deliberately untouched. **New contract:** `callOnRef`'s `fn` must be a plain `function(){}` expression — it is `.call`-bound, so an arrow function would keep its lexical `this` and silently act on the wrong object; recorded in the doc-comment, and all five callers comply. **Verified live over MCP** on the running dev build with a minimal repro (`<div onclick><span>text</span></div>`): the StaticText click fires the handler on the outer row `div` (page log went `none` → `Session info` → `Session info, Table sizes`), sibling StaticText refs stay independently addressable, hover no longer throws, and element refs are unregressed — `browser_type` into a textbox ref still filled it, exercising the `returnByValue` + native-setter fill path through the wrapper. `tsc --noEmit` + lint clean. |
| US-894 | `app.boardVars` unreachable from `execute_script` | — | **Symptom:** the agent-facing board environment-variables admin namespace, shipped and announced as a 4.0.17 feature, was `undefined` in every script — the documented `read_guide("boards")` recipe failed with *"Cannot read properties of undefined (reading 'namespaceFor')"*. `app.boards` and `app.settings` in the same script worked, so the `app` object was initialized; only this namespace was absent. **Root cause:** the `app` a script receives is not the real singleton but `AppWrapper`, which whitelists sub-interfaces one explicit getter at a time. `app.ts` was complete (`:44`, `:96-98`, `:178`), but `AppWrapper.ts` had no `get boardVars()` and `IApp` (`api/types/app.d.ts`) never declared the member or imported `./board-vars` — so the property did not exist on the object scripts see, and a `language: "typescript"` script could not type it either. Five doc locations described the working behaviour correctly; only the two surfaces between the real app and a script were missing. **Fix — 2 code files:** `api/types/app.d.ts` imports `IBoardVars` and declares `readonly boardVars: IBoardVars` after `boards`; `scripting/api-wrapper/AppWrapper.ts` adds the `get boardVars()` passthrough. **Recurrence guard:** the class comment claimed *"Implements the IApp interface"* but carried no `implements` clause, so nothing ever checked it — which is why a member could be added to the real app and silently missed. A real `implements IApp` proved impossible (verified with `tsc`): the wrapper deliberately returns richer concrete types — `PageCollectionWrapper.all` is `PageWrapper[]` not `IPage[]` (facades are narrower, e.g. `GraphNode` lacks the index signature `IGraphNode` declares), and the `events` getter is typed `unknown`, not `IAppEvents`. So a **type-only member-name** check was added instead: `type _AppWrapperCoversIApp = AssertNever<Exclude<keyof IApp, keyof AppWrapper>>` — fully erased at runtime, eslint-clean, and it names the offender (`Type '"boardVars"' does not satisfy the constraint 'never'`). Verified by deleting the getter and re-running `tsc`. It checks names, not shapes; the comment records that limit and the upgrade path. **Audit:** `Exclude<keyof IApp, keyof AppWrapper>` is `never` today, so `boardVars` was the only gap — and the sole `App`↔`IApp` difference is also `boardVars` (`runAsync` is wrapper-only by design). **Nothing needed for Monaco typings:** `editorTypesPlugin` in `vite.renderer.config.ts` auto-copies `api/types/*.d.ts` → `assets/editor-types/` and regenerates `_imports.txt`, so `board-vars.d.ts` was already bundled — merely orphaned. **Docs:** five false "implements X" claims corrected to "mirrors X" in `doc/architecture/scripting.md` + `diagrams/5-wrapper-architecture.mmd`, with new prose on why no `implements` clause is possible and what the guard covers; the stale `IApp` snippet in `scripting.md` (missing `boards`/`boardVars`/`events`/`fetch`/`openRawLink`, wrong `IEditors`/`IRecent` names) brought in line; new CLAUDE.md Key Files row for `AppWrapper.ts`. **Concern resolutions:** name-only check accepted and documented in code; the absent runtime test logged to `backlog.md` (the project has no test framework at all, so choosing one is a separate task) along with extending the check to `PageCollectionWrapper`/`PageWrapper`, which have the identical gap. **Verified live over MCP** on the running dev build: all six methods present; `namespaceFor` on the real Postgres Viewer root resolved to the manifest identity `Evergreen/Postgres Viewer` rather than the folder path (matching what the board sees via `persephone.var.*`); `listNamespaces`/`list`/`set`/`get`/`show` all correct, without opening the board. `tsc --noEmit` + lint clean. |
| US-898 | Link editor view-mode switch didn't re-render | — | **Symptom:** in the Link editor (including a browser blank tab's bookmarks view, in **any** browser mode) switching the view mode — List / Landscape / Portrait — left the content rendered in the *old* mode. It only corrected itself after re-selecting a category or typing in the search box. **Root cause:** a state-reactivity bug, not a rendering bug. `state.use(selector)` re-renders only when the selector's **result** changes (structural compare via `compareSelection` in `core/state/state.ts`). `LinkBody` subscribed with a selector returning `searchText`/`selectedLinkId`/`error`/`filteredLinks`/`allLinks`/`pinnedLinksRaw`/`pinnedPanelWidth`, then read `const viewMode = model.getViewMode()` **outside** the selector. The mode is stored under `data.state.categoryViewMode` / `tagViewMode` / `hostnameViewMode` — keys that selector never reads — so `setViewMode` updated the state and notified subscribers, the selector produced an equal object, and React skipped the render. The two workarounds mapped exactly to the two selected fields that *do* change: re-selecting a category changes `selectedCategory`/`filteredLinks`, typing changes `searchText`. **Fix — 3 files:** `src/renderer/editors/link-editor/LinkEditor.ts` — `getViewMode(snapshot?: LinkEditorState)` now takes an optional state snapshot so it can be called purely from inside a selector; `LinkBody.tsx` — `viewMode: model.getViewMode(s)` moved into the `state.use` selector so it participates in the comparison; `index.tsx` (`LinkActionBits`) — same non-reactive read fixed, so the toolbar button's own icon and label are no longer stale either. **No change needed in `LinksTiles.tsx`:** its `useEffect(..., [links, viewMode])` already re-runs `scrollToRow(0)` + `update({ all: true })` on a mode change, so the grid repaints correctly once the render actually happens. `/review` clean (0 concerns — noted the fix is applied consistently at both selector sites). `/document`: `doc/architecture/state-management.md` — generalized into **Best Practice #2, "Derive Computed Values Inside the Selector"** (renumbered 3–5), since this is a whole class of bug in this state primitive that fails deceptively: the value self-corrects on the next unrelated render, so it reads as a working feature. `/userdoc`: `docs/whats-new.md` (v4.0.18 Bug Fixes). `tsc --noEmit` + `eslint` clean. **Not verified at runtime** — reasoned from the state primitive's compare semantics; the case worth a manual check is Landscape → Portrait, where the component stays mounted rather than being swapped. No task folder (small fix). |
| US-897 | Tor connection info dialog ("?" button) — exit IP, location, Reconnect | — | **Goal:** a Tor-only toolbar "?" button opening a dialog that shows the exit IP a remote server actually sees, its approximate location, and a **Reconnect** button that gets fresh circuits. **Why the existing Reconnect wasn't it:** `TorStatusOverlay`'s button → `BrowserEditor.reconnectTor()` → `tor:start`; since `TorService.running` is already true that only re-applies the proxy — **no circuit or exit-IP change**. It serves a different purpose (recovering a dead/never-established proxy, e.g. after session restore) and was left untouched. **Why the lookup runs in main:** same constraint as US-896 — the SOCKS proxy is bound to the *page's* partition while renderer code runs in the unproxied `appPartition`, so a renderer `fetch()` would hand the checker the user's **real IP**. Reusing `tor-src://` was rejected: registered without `corsEnabled`, so a renderer fetch is CORS-blocked, and enabling CORS would widen it to "any app-session document can read arbitrary cross-origin content through Tor". **Reconnect design — user chose restart over control port:** rejected adding `ControlPort` + `CookieAuthentication` to `torrc` + `SIGNAL NEWNYM` (faster, ~1 s, but opens permanent loopback control surface and requires appending to a possibly user-authored `torrc`). Restart touches **no** `torrc`, adds no control port, and needs no new setting. Accepted costs: several seconds per reconnect, and it affects every open Tor page. **Two pre-existing latent bugs had to be fixed first** — both only looked safe because nothing had ever spawned a second `tor.exe`: (1) `startTorProcess`'s `close`/`error` handlers unconditionally did `running = false; torProcess = null`, and the *old* child's close event arrives **after** the replacement is assigned — nulling out a healthy process and leaving `running` false while Tor is up, which would have silently broken `isActiveTorPartition` (and therefore `tor-src://` images and this dialog); fixed with an `isCurrent(child)` guard. (2) `stopTorProcess` fired `kill()` and returned immediately, but Tor holds a `lock` file in its `DataDirectory` — a replacement racing the exit fails with "Could not lock data directory"; added `stopTorProcessAndWait()` (awaits `close`/`exit`, 5 s cap then proceeds so a hung process surfaces as a normal spawn error rather than a deadlock). **Fix — 6 files + 1 new:** `src/ipc/tor-ipc.ts` (`TorStatus`/`TorIpInfo` types; `tor:check-ip`, `tor:restart`, `tor:status` channels); `src/main/tor-service.ts` (retains `torExePath` for the respawn; `restart()` = synchronous-guard + `runRestart()` so a concurrent caller can't slip through the in-flight check during an `await`, `startPromise` cleared in `finally` so one failed restart can't wedge every later start, re-applies the proxy to **every** active partition since they all lost it, `broadcastStatus`; `checkIp()` with a 20 s exit-IP timeout and a tighter 8 s per-geo-provider timeout — they run sequentially, so sharing the budget let two slow providers alone spin the dialog ~40 s; `normalizeGeo` folding both provider shapes); `src/renderer/editors/browser/BrowserEditor.ts` (`torStatusListener`, `showTorInfoDialog` via dynamic import; also fixed a **pre-existing duplicate-listener bug** — `initTorProxy` called `ipcRenderer.on(TorChannel.log, …)` unconditionally and `reconnectTor()` re-enters it, so every overlay Reconnect stacked another log listener and duplicated later log lines; now `removeListener`-then-`on` for both); `BrowserEditorModel.ts` (`torStatus` uses the shared `TorStatus`); `BrowserView.tsx` (`QuestionIcon` button, `isTor`-gated); **new** `src/renderer/ui/dialogs/TorInfoDialog.tsx` (`TDialogModel`; loads on `postCreate`; Reconnect compares before/after IP and reports an unchanged exit honestly rather than implying failure; states the restart is app-wide). **Endpoints (hardcoded):** `check.torproject.org/api/ip` for IP **+ an `IsTor` verdict** (Tor-Project-run, never blocks exits), then `ipinfo.io/json` → `freeipapi.com/api/json` for geo; geo failure is non-fatal and never overwrites `error`, so a dead provider still leaves the IP visible. **Status broadcast is the plumbing this design requires:** the daemon is shared but `torStatus` lives in renderer state main can't see, so without `tor:status` a second Tor page would keep showing a green dot while Tor is down. **Deliberately not done:** no UA/fingerprint spoofing (consistent with US-896); country shown as the provider's code (`DE`), no code→name table. `/review` clean (0 concerns; applied its geo-timeout suggestion). `/document`: `browser-editor.md` (new **"The proxy covers the webview, not the renderer"** subsection — the architectural fact behind both Tor tasks, previously undocumented; plus `tor-src://` guard contract, the connection-info dialog, and both restart invariants, since each looks like removable defensiveness to anyone unaware a second daemon can exist), `folder-structure.md`, `CLAUDE.md` Key Files. `/userdoc`: `docs/browser.md` ("Connection Info and Reconnecting"), `docs/whats-new.md` (v4.0.18 New Features). `tsc --noEmit` + `eslint` clean. **Not verified at runtime** — needs a configured `tor.exe`; the acceptance criteria in the task doc are the user's manual test, notably Reconnect **three times in a row** (what would expose a lock-file or stale-child failure) and a second Tor page's dot following the restart. Task folder `doc/tasks/US-897-tor-info-dialog/`. |
| US-896 | Link editor images must be fetched through Tor in a Tor browser page | — | **Symptom:** opening a new tab in a **Tor** browser page renders the bookmarks `LinkEditor`; in tile mode its images were fetched **directly, not through Tor** — leaking the user's real IP to every bookmark's image host. **Root cause is architectural, not a code bug:** Tor is a per-page session-partition proxy covering only that page's `<webview>`. The blank-tab Link editor is plain React in the **app renderer** (`appPartition`), which is unproxied, so its `<img>` requests never touched the proxy. **Fix — new `tor-src://` scheme + a renderer-side rewriter.** `src/main/tor-src-protocol.ts` (new) handles `tor-src://<partition>/?u=<encodeURIComponent(url)>` and fetches via `session.fromPartition(torPartition).fetch(target)` so the SOCKS proxy applies. **Three guards, load-bearing together:** partition must match the `browser-tor-<uuid>` shape; `TorService.isActiveTorPartition()` must confirm a live bootstrapped session (new method — the scheme is inert whenever no Tor page runs); target must be `http(s)`. Registered on **`appPartition` only** (least privilege — first draft wrongly registered it inside `registerAssetProtocol`, which also covers `fileAccessPersistPartition`), with `standard`/`secure`/`supportFetchAPI` but deliberately **no** `bypassCSP`/`corsEnabled`. **The `?u=` query is not cosmetic:** a `standard: true` scheme's *path* is canonicalized by Chromium, which can rewrite percent-escapes and corrupt a target URL carrying escapes of its own (`%25` → literal `%`); `searchParams` decodes exactly once. Verified round-trip against `https://ex.com/a%20b/img.png?w=100&h=2`, `https://ex.com/p%25%2Fq/img.png#frag`, `http://ex.com/img+plus.png`. Responses carry `Cache-Control: no-store`; `appPartition` is `'nopersist'` (no `persist:` prefix ⇒ in-memory session, no on-disk HTTP cache) so nothing lands on disk either way. **Renderer side:** `src/renderer/editors/link-editor/tor-src.ts` (new) — `resolveTorSrc(src, proxy)` passes local schemes (`data:`/`blob:`/`file:`/`safe-file:`/`app-asset:`) through, returns **null** when the circuit isn't connected (so nothing is fetched direct), else rewrites. Placed in `link-editor/`, not `browser/`, to keep the dependency arrow one-way (`browser` already imports `link-editor`). **Wiring:** `LinkEditor.imageProxySource` + `imageProxy`/`isTorPage` getters; `BrowserEditor.configureBookmarks` supplies a closure reading `torStatus` **live at render time** — the plan's pushed-field approach was broken, since the bookmarks model outlives every blank tab and a field set at init would be frozen at `ready:false` forever, defeating even the user's expected reopen-the-tab workaround. Threaded `imageProxy` through `LinksTiles` (+ an `onError`/`failedSrc` fallback to the globe/favicon placeholder), `LinkTooltip`, `LinksList`, `LinkItemTiles`, `LinkItemList`, `PinnedLinksPanel`, `panels/LinkCategoryPanel`, `EditLinkDialog` (preview + discovered-image thumbnails; `width`/`height` moved onto the wrapping `Panel` so tiles stay sized/clickable when a thumbnail is suppressed), `BrowserBookmarksUIModel`. **User decisions:** ignore `.onion`; **no** `torStatus` reactivity (an unconnected circuit renders no image; it appears on the next new tab); **no** UA/Referer spoofing; "Open Image in New Tab"/"Copy Image URL" deliberately **not** disabled in Tor mode (an unproxied fetch there is acceptable); scheme named generally (`tor-src://`, not `tor-img://`) since it fetches any URL; incognito out of scope. **Dropped from the first draft:** a `Content-Type: image/*` response gate — it constrained the *response* while the Tor fetch happened regardless, i.e. cost without benefit. **Follow-on feature (same task):** disable favicon caching in Tor mode. Full audit found exactly two `saveFavicon` writers (both now Tor-guarded — `BrowserBookmarksUIModel` `!isIncognito && !isTor`, and `BrowserView`'s favicon handler already checked both), `cacheFavicon` is an in-memory `Map` only, and `requestFaviconSave` writes to `saveForHosts`, a **module-level set shared by every browser page** — a marker armed from a Tor page would be honoured later by a non-Tor page, so the new `isTorPage` getter gates all 3 call sites (`LinkItemTiles`, `LinkItemList`, `PinnedLinksPanel`). Severity stated accurately: not a Tor de-anonymisation leak (the write only occurred on a genuine non-Tor visit), but a Tor page shouldn't reach into another page's behavior. **Corrected a factual error in the task doc's own Background:** `PinnedLinksPanel` does **not** reuse `LinksTiles` (it's a `ListItem` list rendering only a `LinkTooltipContent`), which is why the tooltip — 3 call sites — was the fix there and pulled `LinksList.tsx`/`LinkItemList.tsx` into the change set. Verified no remaining unproxied image sites (grep for `src={link.imgSrc}`/`state.imgSrc`/`item.imgSrc` → zero) and no CSP change needed (`index.html` declares no `default-src`, so `img-src` is unrestricted). `/review` clean (0 concerns; verified all three guards present and documented as required together). `/document`: `browser-editor.md`, `folder-structure.md`, `CLAUDE.md` Key Files. `/userdoc`: `docs/browser.md` ("Bookmark Images in Tor Mode"), `docs/whats-new.md` (v4.0.18 Bug Fixes). `tsc --noEmit` + `eslint` clean. **Not verified at runtime** — needs a configured `tor.exe` + Tor bookmarks file; the user's manual test. Task folder `doc/tasks/US-896-link-editor-tor-image-fetch/`. |
| US-893 | Remove the built-in Todo editor | — | **Goal:** retire the built-in `todo-view` editor now that the published **Todo board** (persephone-boards repo, content-host board built on EPIC-042/043/044) is the replacement — removing ~2,050 lines makes the core simpler to maintain. EPIC-044 explicitly deferred this as "a separate later task." **Removed (via `git rm` + edits):** the whole `src/renderer/editors/todo/` directory (8 files), `src/renderer/scripting/api-wrapper/TodoEditorFacade.ts`, `src/renderer/api/types/todo-editor.d.ts`, `assets/mcp-res-todo.md`; `todo-view` editor + `todo-panel` secondary-view registrations (`register-editors.ts`); the `todo-view` matcher + `.todo.json` from `SPECIALIZED_JSON_PATTERNS` (`editor-matchers.ts`); the `todo-view` construction branch (`PagesLifecycleModel.ts`); the "Todo" creatable item (`tools-editors-registry.ts`); `page.asTodo()` + imports (`PageWrapper.ts`); `ITodoEditor`/`asTodo` from the type surface (`page.d.ts`, `common.d.ts`); `TodoIcon` + `.todo.json` icon mapping (`LanguageIcon.tsx`, `language-icons.tsx`); `TraitTypeId.TodoItem` (`TraitRegistry.ts`); the `todo` MCP guide resource + `read_guide` enum entry + `create_page`/`execute_script`/scripting-guide mentions (`mcp-http-server.ts`); todo entries in `mcp-res-pages.md`/`mcp-res-scripting.md`; the `todo-view` QA test (`qa/mcp-test-create-page.md`). **Decisions (user-approved C1–C4):** C1 — `.todo.json` now falls back to **Monaco** (raw JSON) for users without the board (accepted regression; the board is the catalog replacement, requires Persephone ≥ 4.0.17); C2 — dropped `.todo.json` from `SPECIALIZED_JSON_PATTERNS` so **grid-json** is offered as a switch option; C3 — `.todo.json` shows the generic `JsonIcon` (an installed Todo board supplies its own icon via `editorPriority`); C4 — standalone task. **Follow-on (persephone-boards):** bumped the Todo board `editorPriority` 0 → 200 and published **todo v1.0.1** so the board becomes the default `.todo.json` editor and its icon shows in the Explorer tree (the built-in's `filePatternIcons` entry is gone). **`/review` clean** (0 concerns — every deletion matched by its registration-site removal; `git grep` across `src/`/`assets/` finds no live refs; `tsc --noEmit` clean). **`/document`:** `editors.md`, `scripting.md`, `folder-structure.md`, `secondary-views.md`, `trait-system.md` (replaced the canonical DnD reorder example `TodoItemView` → real `BrowserTabsPanel` code), `state-management.md`, `pages-architecture.md`, `overview.md`, and 3 diagrams; root `CLAUDE.md`/board docs verified clean. **`/userdoc`:** `docs/index.md`, `editors.md`, `tabs-and-navigation.md`, `scripting.md`, `mcp-setup.md`, `api/page.md`, `api/index.md`, `api/editors.md`, `boards.md`, `README.md`, + a `whats-new.md` **Breaking Changes** entry (v4.0.17) pointing users to install the Todo board. **`npm run lint` + `npm run typecheck` clean.** Note: `assets/editor-types/` regenerates its `todo-editor.d.ts` on next build. Task folder `doc/tasks/US-893-remove-builtin-todo/`. |
