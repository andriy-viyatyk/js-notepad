# Completed Tasks

Last 20 completed tasks, newest first. Older tasks are pruned.

| ID | Title | Epic | Notes |
|----|-------|------|-------|
| US-881 | Search-boards "Requires Persephone ≥ X" label uses the warning color | — | **Change:** on the Tools & Editors hub **Search boards** card, an incompatible board's *"Requires Persephone ≥ X"* line rendered in muted `color="light"`; changed to `color="warning"` (the theme amber/gold token) for visibility, matching the same label already shown in the Board Info version list. **Fix — 1 line:** `src/renderer/editors/tools-hub/SearchBoardsTab.tsx`. `/review` clean (named `TextColor` token, no hardcoded color). No doc impact (visual tweak). `tsc --noEmit` clean. No task folder. |
| US-880 | Tools & Editors hub page not restored (restart / drag-to-new-window) | — | **Symptom:** the Tools & Editors hub page **vanished** after an app restart, and dragging its tab to a new window opened an **empty page**. **Root cause:** `PagesPersistenceModel.restorePage` rebuilds no-host page editors only if their id is in `NO_HOST_EDITOR_IDS`; `tools-hub-view` was never added when the hub was introduced, so its persisted descriptor hit the `unrecognized editor descriptor` branch and returned `null` → the page's editor never rebuilt. Both symptoms share the `restorePage` path (drag-to-new-window transfers the page via the same restore). **Fix — 1 line:** add `"tools-hub-view"` to `NO_HOST_EDITOR_IDS` (`src/renderer/api/pages/PagesPersistenceModel.ts`); the hub now restores via the standard `createEditor` → `applyRestoreData` → `restore` path, preserving its selected tab. `/review` clean. `/document`: `editors.md` (`tools-hub-view` table row — added the tab-bar "+" entry point). `/userdoc`: `docs/whats-new.md` (v4.0.15 Bug Fixes), `docs/tabs-and-navigation.md`. `tsc --noEmit` clean. No task folder. |
| US-879 | Tab-bar "+" dropdown "Show All…" opens the Tools & Editors hub page | — | **Change:** the "+" new-page dropdown's **"Show All…"** item previously opened the **sidebar panel** (`app.window.openMenuBar("tools-editors")`); it now opens the full **Tools & Editors hub page** via `pagesModel.showToolsHubPage()` — the same page as the panel's "Open in new tab" button (includes the Search boards catalog). **Fix — 1 line:** `src/renderer/ui/tabs/PageTabs.tsx`. `/review` clean. `/document`: `editors.md` (`tools-hub-view` entry point). `/userdoc`: `docs/tabs-and-navigation.md` + `docs/editors.md` (corrected stale "panel"/"sidebar" wording → "hub page"), `docs/whats-new.md` (v4.0.15 Improvements). `tsc --noEmit` clean. No task folder. |
| US-878 | "+" install switch broken from a host-less built-in editor (Archive viewer for zip-based files) | — | **Symptom:** opening a `.xlsx`/`.docx`/`.pptx` (a ZIP → opens in the built-in **Archive** viewer) with a matching *published-but-uninstalled* board, then clicking the switch's **+** entry: Board Info showed **"No installable editor is published for this file type"**, the tab title collapsed to **"Install editor"**, the switch mislabeled the built-in peer as **"Text Editor"**, and clicking that did nothing. **Root cause:** the Archive editor is `hasContentHost: false`; `BoardInfoEditorModel.switchFrom` bailed early for a host-less source (`if (!trait) return`) **without capturing the file** — so `currentFileName()` fell back to the title, `recomputeMatches` matched no catalog board (`catalogBoardsForFile("Install editor")` → []), and `findCompatibleEditors` resolved the empty name to Monaco → "Text Editor". Switching back also hit Archive's base `switchFrom` (throws — Archive has no override). The toolbar "+" appearing at all proved compatibility (`useCatalogBoardsForFile` filters by `isCompatible`), so the contradiction pinned the missing file name. **Fix — 2 files:** (1) `src/renderer/editors/board-info/BoardInfoEditorModel.ts` — `switchFrom` now captures a host-less source's `oldEditor.filePath` into the `filePath` state field (+ sets the tab title via `fpBasename`), so the install page matches catalog editors and the switch keeps **Archive \| +**; (2) `src/renderer/api/pages/PageModel.ts` — `switchMainEditor`'s generic branch now **dispose-and-rebuilds** a real file editor from `filePath` when the source has no `contentHost` (Board Info target exempt — its tolerant `switchFrom` captures the path), instead of calling a base `switchFrom` that throws. Relies on Archive's existing `filePath` getter override (returns `archiveUrl`, US-876). New static import: `BOARD_INFO_EDITOR_ID` (dependency-free constant). **User-verified** (install tile shows Excel Viewer, tab keeps file name, switch stays Archive \| +, switch-back works). `/review` clean (0 concerns). `/document`: `editors.md` (Owner-Orchestrated Switching — host-less-source → file-editor rebuild). `/userdoc`: `docs/whats-new.md` (v4.0.15 Bug Fixes). `tsc --noEmit` clean. No task folder. |
| US-877 | Broken PDF viewer — blank document, CORS-blocked `safe-file://` fetch | — | **Symptom:** opening any `.pdf` showed the pdf.js viewer chrome (toolbar) but a **blank document**; console showed a CORS block on the PDF fetch (`Access to XMLHttpRequest at 'safe-file://…' from origin 'app-asset://pdfjs' … blocked by CORS policy: Cross origin requests are only supported for protocol schemes: **app-asset**, chrome, …, http, https`), then `net::ERR_FAILED` and pdf.js `ResponseException` *"Unexpected server response (0)"*. **Root cause:** the pdf.js viewer runs at origin `app-asset://pdfjs` (`PdfView.tsx:20-24`) and fetches the PDF **cross-origin** from `safe-file://`. Electron 43 / Chromium requires a custom scheme to be registered with **`corsEnabled: true`** to be a valid target of a cross-origin fetch — note `safe-file` is absent from the allowed-scheme list above, but `app-asset` is present. In `src/main/main-setup.ts` the `safe-file` scheme registration lacked `corsEnabled` while `app-asset` had it. **Regression origin:** US-821 ("enable CORS on the app-asset:// scheme (Electron 43 fix)", commit `ad145a2e`) added the flag to `app-asset` when the Electron 43 upgrade began enforcing custom-scheme CORS, but **missed `safe-file`** — the scheme pdf.js loads the PDF from. Pre-Electron-43 the fetch wasn't CORS-checked, so PDFs worked. **Fix — 1 line:** add `corsEnabled: true` to the `safe-file` scheme privileges in `main-setup.ts` (mirrors `app-asset`; scheme privileges register once at startup so a full restart — not HMR — is needed to verify). Main-process-only change; `/review` clean (0 concerns — `file-path` restriction is renderer-only). `/document`: no dev-doc impact (no doc describes the scheme privilege list; no new file/pattern). `/userdoc`: `docs/whats-new.md` (v4.0.15 Bug Fixes — the symptom was a user-visible regression shipped in 4.0.14). No task folder (one-line fix). Committed `bb37ffa5`. |
| US-876 | Editor-switch breaks for a simple custom-editor board sharing its file with a built-in editor | — | **Symptoms (found testing a local Excel-viewer board — simple board, `fileMasks: ["*.xlsx","*.xls"]`, `editorKind: "simple"`):** (1) switching from the Excel board to its built-in peer **Archive** (`.xlsx` is a ZIP) made the whole **editor-switch widget vanish** — no way back; (2) clicking **Board properties** showed the Board Info page with the switch reading **"Text Editor \| +"** instead of the file's real peers — again no way back to the board. **Root causes:** (1) `ArchiveEditor` never overrode `findCompatibleEditors()` (base `[]`), so `SwitchWidget`'s gate (`merged.length < 2 \|\| !merged.includes(model.editorId)`) hid the widget when Archive was active; compounded — Archive stores its path as `state.archiveUrl`, not `state.filePath`, so `model.filePath` was `undefined` and `PageModel.switchMainEditor`'s simple-board branch (`if (!filePath) return`, `:515`) silently no-op'd even the switch itself. (2) `openBoardInfo` only carries the originating file forward via a **content host** (`switchFrom`); a **simple** board holds no `CONTENT_HOST_TRAIT`, so Board Info's `currentFileName()` fell back to its own title (the board name) → `findCompatibleEditors()` resolved that to Monaco → "Text Editor \| +". **Fix — 3 files:** `src/renderer/editors/archive/ArchiveEditor.ts` (add `override get filePath()` = `state.archiveUrl`, `override findCompatibleEditors()` = `["archive-view"]` for a local path via `isPlainLocalPath`, else `[]` — the widget appends the board, plain `.zip` stays single-entry/hidden = no regression); `src/renderer/editors/board-info/open-board-info.ts` (when the outgoing editor is host-less, capture `old.filePath` and seed it into `BoardInfoEditorState`); `src/renderer/editors/board-info/BoardInfoEditorModel.ts` (new `filePath?` state field; `currentFileName()` prefers `host.filePath ?? state.filePath ?? titles`; base `filePath` getter then returns it so the widget shows **Archive \| Excel \| +**; `openBoard()` switches via `boardEditorId(root)` when a host **or** `filePath` exists so it returns to the file-viewing board; persisted in `getRestoreData`/`applyRestoreData`). **General, not `.xlsx`-specific** — affects any simple board whose built-in peer returns `[]`, and every simple board via Board-properties; a follow-up note flags that PDF/image/video built-ins would need the same `filePath`+`findCompatibleEditors` pattern if a future simple board targets them. **User-verified** after restart: Excel↔Archive both ways with the switch intact; Board properties shows **Archive \| Excel \| +** and Excel/Open board return to the board with the file. (The "Board properties did nothing" seen mid-testing was **dev-mode HMR staleness** — a full restart cleared it; committed code is correct.) `/review` clean (0 concerns — mirrors the `BoardEditorModel` `filePath`/`findCompatibleEditors` reference pattern). `/document`: `editors.md` (simple-board switch — peer must expose `filePath`+`findCompatibleEditors`, Archive does for ZIP-based files; Board Info — captures a host-less board's originating `filePath`). `/userdoc`: `docs/whats-new.md` (v4.0.15 Bug Fixes); `docs/boards.md`/`docs/editors.md` already described the intended behavior this fix delivers — no correction needed. `tsc --noEmit` + `eslint` clean. Task folder `doc/tasks/US-876-editor-switch-custom-editor-board/`. |
| US-875 | Editor switch — always render the "+" (Board Info) segment last | — | **Symptom:** opening a file's **Board properties** (which makes `BoardInfoEditorModel` the active editor) showed the install/switch "+" segment *between* the built-in editor and an installed board — e.g. `[Text Editor][+][DrawIO]` — instead of last. **Root cause:** `SwitchWidget` (`src/renderer/editors/base/PageToolbar.tsx`) builds its segment list as `options` (from `model.findCompatibleEditors()`) then appends trusted-board matches then the catalog "+" — normally putting "+" last. But `BoardInfoEditorModel.findCompatibleEditors()` returns `[builtin, BOARD_INFO_EDITOR_ID]`, so when Board Info is the active editor the "+" already sits inside `options` *before* the board segments get appended. **Fix — 1 file:** after assembling `merged`, if `BOARD_INFO_EDITOR_ID` is present but not last, splice it out and push it to the end (idempotent; unifies both entry paths — `options` vs catalog append). No effect on the already-correct two-segment case; the active-editor highlight is unaffected (`SegmentedControl` selects by `value`, not index). `/review` clean (0 concerns — pure list reorder, no color/`!`/import/Object-Model issues). `/document`: no dev-doc impact (`editors.md`'s Board Info prose — "reached from the `+` editor-switch entry" — stays accurate; no doc specifies segment order; over-documenting the ordering would violate the skill's simple-change rule). `/userdoc`: no impact (`docs/boards.md`/`whats-new.md` only illustrate the two-segment `Text \| +` case, which never regressed; the multi-segment edge case was never advertised). No task folder (small fix). Committed `678875e4`. |
| US-874 | Open an image data URL in the Drawing editor (`draw-view` target) | — | **Goal:** a general "open this image in the Drawing (Excalidraw) editor" capability — `openRawLink(imageDataUrl, { editor: "draw-view" })` opens an image `data:` URL as a **new, untitled, editable** Excalidraw drawing. Generalizes the built-in Mermaid/Image/SVG "Open in Drawing Editor" buttons for any caller (boards, scripts). **Design (settled with the user):** use the **real editor id** `draw-view` — **no second/pseudo-target** — with the **input type as the discriminator** (a `data:image/*` URL triggers the import; a real `.excalidraw`/JSON opens normally). Routed to `pagesModel.addDrawPage()` (a **fresh untitled page**), never binding the image as a content-host source — so Ctrl+S can never overwrite the source file (the hazard that ruled out "make the draw editor handle the image directly"). **Data-URL only by design:** the resolver does no file reads / no fetches; a caller with an http/file image converts it to a data URL first. **Fix — 2 files, renderer-only:** (1) `src/renderer/content/resolvers.ts` — new `openLink` subscription at the end of `registerResolvers()` (registered last → runs first, LIFO), `target === "draw-view"` + `data.url` starts `data:image/` → `addDrawPage(url, (title||"drawing")+".excalidraw")`, `handled = true` (short-circuits before the file resolver builds a pipe); errors via `ui.notify`. (2) `src/renderer/api/pages/PagesLifecycleModel.ts` — `addDrawPage` now derives the embedded image **MIME from the data URL prefix** (`/^data:([^;,]+)/`, fallback `image/png`) so SVG/JPEG embed correctly instead of always PNG. **No bridge/shim/channel/EventEndpoint changes** — `persephone.openRawLink(href, { editor })` already forwards the target string. **Verified live in the running app:** the drawio-viewer board's new "Open in Drawing Editor" button → a `drawing.excalidraw` page (editor `draw-view`, **no filePath** = untitled/unbound) with the diagram embedded and editable. `/review` clean (0 must-fix; flagged that `assets/board-template/CLAUDE.md` still lacked the note `mcp-res-boards.md` had → fixed in `/document`). `/document`: `content-pipeline.md` (new **Drawing-image resolver** bullet in the Layer-2 list), `assets/board-template/CLAUDE.md` (openRawLink `draw-view` image note; `assets/mcp-res-boards.md` already carried it). `/userdoc`: `docs/api/app.md` (documented the previously-undocumented `openRawLink` `options.editor` param + `md-view`/`draw-view` examples), `docs/whats-new.md` (v4.0.14). `tsc --noEmit` + `eslint` clean. **Consumer** (drawio-viewer board button + a `how-to/` integration recipe) lives in the separate `persephone-boards` repo. Task folder deleted at completion — this entry is the surviving evidence summary. |
| US-873 | "Open Terminal here" folder context-menu action | — | **Goal:** right-click a folder → open a terminal window rooted at that folder, across three surfaces: the Explorer **tree** (`TreeProviderViewModel.getFolderMenuItems`), the sidebar flat **folder list** (`CategoryViewModel.getFolderMenuItems`) — both gated by `supportsOsClipboard(provider)` (file-provider-only, so Mneme/archive/link folders are excluded), and the App bar **left-panel pinned folders** (`MenuBar.getMenuFolderContextMenu`, gated on `folder.path`). **Configurable terminal:** new `terminal.command` setting (`settings.ts`, mirrors `vlc-path`), **auto-detected on first use** (pwsh → powershell → cmd via `where`) and saved so the user can change it. New Settings → **Terminal** dropdown (`SettingsView.tsx` `TerminalSection`: Auto-detect / pwsh / powershell / cmd / wt, surfacing a custom value as its own item). **Launcher — `src/main/terminal-launcher.ts` (new, mirrors `vlc-launcher.ts`):** `detectTerminal()` + `openTerminalAt(dir, command)`. **Key fix:** launch via `cmd /c start "" /D <dir> <shell> <args>` — spawning a console shell directly with `detached:true` sets `DETACHED_PROCESS` (no console window → runs invisibly); `start` gives it a real window and decouples it. Arg mapping: pwsh/powershell `-NoExit`, cmd `/K`, wt `-d <dir>`. **Renderer helper `src/renderer/api/terminal.ts` (new):** reads the setting, detects+saves if empty, then launches; errors via `ui.notify`. IPC `openTerminal`/`detectTerminal` mirror `showItemInFolder`/`openInVlc`. `TerminalIcon` in `theme/icons.tsx`. Windows-only. **User-verified** the launch after the `cmd /c start` fix + main-process restart. `/review` clean (0 must-fix; applied a semicolon-consistency nit, left the onClick dynamic-import which matches `tree-context-menus.tsx`). `/document`: `CLAUDE.md` Key Files (terminal-launcher + terminal.ts rows) + `folder-structure.md` (main/ + renderer api/ listings). `/userdoc`: `docs/tabs-and-navigation.md` (three surfaces + Settings → Terminal), `docs/whats-new.md` (v4.0.14). `tsc --noEmit` + `eslint` clean. Task folder deleted at completion — this entry is the surviving evidence summary. |
| US-850 | Board editor switch on untitled (path-less) pages | — | **Symptom:** creating a new (untitled) Monaco page and renaming its tab to a content-host board's mask (e.g. `untitled.drawio`) did **not** surface that board's switch; and after force-switching, the switch vanished and you couldn't return to Monaco. **Root cause (three path-less gates):** (1) `SwitchWidget` (`PageToolbar.tsx`) fed only `filePath` to the board lookup, so a path-less page passed `""` and matched no board — unlike the built-in switch, which already falls back to the page title (`editorRegistry.findEditorsAccepting`: `filePath ?? host.title`); (2) `BoardContentEditorModel.findCompatibleEditors()` short-circuited to `[]` with no `filePath`, so once *on* the board its switch had nothing to offer and hid itself; (3) the base `BoardEditorModel.editorId` getter returned the plain `"board-view"` (not `board-editor:<root>`) when path-less, breaking both the widget highlight and — critically — `switchMainEditor`'s board-boundary detection (`parseBoardEditorId("board-view")` = null), so clicking Monaco never took the host-transfer path. **Fix — 2 files:** `SwitchWidget` now uses the page title as the file name when there is no path (only content-host boards qualify — a title-only page has no local path, and simple boards need a real file) and subscribes to `title` for live re-eval; `BoardContentEditorModel` adds an `editorId` override (always `board-editor:<root>` when a root is set — a content-host board is always a custom file editor; persistence still pins `"board-view"` via `getRestoreData`) and `findCompatibleEditors()` falls back to the host/page title so the built-in editor stays resolvable. **User-verified** the full round-trip: new page → rename to `.drawio` → drawio-viewer switch appears → switch to it → switch still shows → back to Monaco (host transfers, no data loss). `/review` clean (1 robustness suggestion applied — restore the `model.filePath` getter so a board's `sourceLink.filePath` merge isn't dropped). `/document`: `editors.md` (Content-Host Boards — title fallback + always-`board-editor:<root>` editorId), `CLAUDE.md` Key Files (BoardContentEditorModel row). `/userdoc`: `docs/whats-new.md` (v4.0.14), `docs/boards.md` (content-host "works on an untitled page"). `tsc --noEmit` + `eslint` clean. No task folder (small fix). |
| US-842 | Board iframe clipboard permission — boards can use `navigator.clipboard` | — | **Motivation:** the DrawIO viewer board's new "Copy PNG" action failed with a **Permissions Policy** violation (crbug.com/414348233) — a cross-origin iframe can't use `navigator.clipboard.write` unless the parent delegates the permission. **Root cause (not CSP):** the board `<iframe>` had no `allow` attribute, so Chromium's Permissions Policy blocked the Clipboard API in the (cross-origin) frame; the board CSP (`connect-src 'self'`) only governs remote network and was never the blocker. **Fix — single attribute in `src/renderer/editors/board/BoardWebview.tsx`:** added `allow="clipboard-read; clipboard-write"` to the board iframe. A board is a trusted local app (filesystem + process access via the bridge), so the clipboard is no additional risk; only remote network stays CSP-blocked. Enables the standard web Clipboard API for **all** trusted boards (still needs a secure context — `board://` is one — + a focused window + user gesture, per the browser). **Verified live:** after the fix the permissions-policy violation is gone; the drawio board's Copy PNG copies on a real click (user-confirmed the paste). `/document`: `assets/board-template/CLAUDE.md` + `assets/mcp-res-boards.md` (noted clipboard / browser-API availability to boards). `tsc --noEmit` clean. No task folder (single-attribute fix). |
| US-841 | Board scaffold — instruct the agent to rewrite `CLAUDE.md` into board-specific docs after implementation | — | **Motivation:** after a board is built, its scaffolded `CLAUDE.md` (the generic authoring guide) stayed unchanged, so a future agent asked to fix/extend the board had no board-specific context. **Change (committed `7e4c6908`):** added a top callout to `assets/board-template/CLAUDE.md` instructing the authoring agent to REPLACE the file with docs for the board it built (purpose / how it works / key files / run & test / gotchas), keeping a pointer to `read_guide("boards")` for the generic bridge reference; added the same step to the `assets/mcp-res-boards.md` develop lifecycle. **First applied** to the external DrawIO viewer board's own `CLAUDE.md` (rewritten from scaffold into real board notes). No task folder (small doc change). |
| US-835 | Modifier-free file drag — native OS drag-out + Move/Copy on drop | — | **Motivation:** US-833 shipped OS drag-out gated behind **Ctrl** because a plain drag had to stay an in-process HTML5 trait drag for internal drops to work — but Ctrl is hidden functionality most users won't discover. US-834 (which added `FileTreeProvider.importFiles`) removed the technical reason for the gate: it revealed that US-833's "Attempt 1" internal-drop failure was actually the **missing `importFiles`**, not a "native drag can't re-enter its own window" limitation. **Crux confirmed empirically first:** removing the gate alone, an internal folder-to-folder drag copied the file → proving a native `startDrag` dropped back inside the window re-enters via `GlobalEventService.captureDrop` as an `OsFile`/`FILE_LINK` drop. **Change — 2 files:** (1) `TreeProviderView.tsx` — dropped the `e.ctrlKey` guard in `handleOsDragStart` so **every** local-file-provider row drag is a native OS drag (no modifier); routed `onTraitDrop`'s `FILE_LINK` branch to `model.dropOsFilesInto` for file providers (`supportsOsClipboard`), keeping `importFiles` for byte-backed targets (Mneme). (2) `TreeProviderViewModel.tsx` — new `dropOsFilesInto`: on a drop into an Explorer folder shows a **Move / Copy / Cancel** dialog (Move offered only when items have a real `filePath`; byte-only items fall back to `importFiles`), then overwrite-collision confirm + progress via the shared `core/utils/copy-files.ts` `copyPathsInto`. **No cross-provider regression** — dropping an Explorer file onto a Link Editor collection still creates a link (identical `OsFile` payload to a Windows-Explorer drop, which already worked). **User-verified:** plain drag out to Explorer/Teams (no Ctrl), folder-to-folder drag shows Move/Copy and both operate, Windows-Explorer→folder shows the same dialog, overwrite prompt, same-parent drop is a no-op. `/review` clean (0 concerns). `/document`: `trait-system.md` (rewrote the OS drag-out section for the modifier-free model + `dropOsFilesInto` routing in the `FILE_LINK` branch); `CLAUDE.md` unchanged (no new file). `/userdoc`: `docs/tabs-and-navigation.md`, `docs/shortcuts.md` (`Ctrl+drag`→plain `Drag`), `docs/whats-new.md` (v4.0.14). `tsc --noEmit` + `eslint` clean. Task folder `doc/tasks/US-835-modifier-free-file-drag/`. |
| US-834 | Explorer file tree rejected file drops into folders (FileTreeProvider missing `importFiles`) | — | **Symptom:** dragging a file from Windows Explorer (or another app) onto a folder in Persephone's Explorer panel did nothing — no drop-highlight, no copy. **Investigation (recorded because it disproved several theories):** Persephone runs at **Medium** integrity, not elevated → ruled out UIPI/elevation; **SentinelOne** + MDE sensor present but log to their cloud consoles, not the local Windows Event Log → Event Log inconclusive; a temporary renderer drag-probe proved the drop **does** reach the app with a valid path (`getPathForFile="C:\…\README.md"`, `files=1`) → **not** an external/DLP block. Probing the tree drop chain then showed `canTraitDrop … importFiles=false`: **`FileTreeProvider` (the local-file provider) never implemented `importFiles`** — only the Link/Mneme providers had it — so `canTraitDrop` rejected every file drop and `acceptsFileDrop` (`writable && !!importFiles`) was false → no highlight. `trait-system.md` already *described* "File tree writes", so this was a genuine implementation gap, not a design choice. **Fix — `src/renderer/content/tree-providers/FileTreeProvider.ts`:** new `importFiles(items, targetCategory)` — path-backed items copied recursively via the shared `core/utils/copy-files.ts` `copyPathsInto` (same helper as clipboard paste; overwrite confirmation handled by the `TreeProviderViewModel.importFiles` caller), byte-only producers written via `nodefs`. One method restores both the drop-highlight and the copy-in. **User-verified:** file + folder drop into an Explorer folder copies (recursive), same-name prompts Overwrite. `/review` clean (0 concerns). `/document`: none — `trait-system.md` already documented File-tree file import (code now matches). `/userdoc`: `docs/tabs-and-navigation.md` (Explorer drop-in bullets), `docs/whats-new.md` (v4.0.14 Bug Fixes). `tsc --noEmit` + `eslint` clean. No task folder. |
| US-833 | Drag files OUT of the Explorer panel to Windows Explorer / Teams | — | **Goal:** drag a file out of the Explorer tree to Windows Explorer (copy) / Microsoft Teams (attach) while keeping internal drag working. **Three attempts — the platform reality was the whole problem:** (1) plain drag = native `webContents.startDrag` → clean export to Explorer + Teams, but a native drag **can't be dropped back onto the app's own windows**, so it broke internal same-window move / drag-into-editor. (2) HTML5 trait drag + `DownloadURL` file-promise on the same plain drag → internal preserved, but Explorer accepted it only via a "Save as" **download** dialog and **Teams rejected it** ("no drop" cursor) — Teams needs a real `CF_HDROP`, which only `startDrag` produces. (3, FINAL) **modifier-gated:** plain drag keeps the in-process HTML5 trait drag (internal move / drag-into-editor unchanged — and immune to the OS-drop block below); **Ctrl+drag** hands off to native `startDrag`. **Ctrl, not Alt** — Windows reads a held Alt during a drag as "create shortcut" (targets show "no drop"); Ctrl means "copy". **Environment finding:** on the tester's corporate laptop all OS file drops *into* Persephone are blocked (Explorer→app **and** VS Code→app) by endpoint security — independent of this feature, but it's why internal drag must stay in-process. **Impl:** new `src/main/os-drag-service.ts` (`startOsFileDrag` via `webContents.startDrag`; win32; shell icon via `app.getFileIcon` + 1×1 PNG fallback; single/multi `files`); IPC endpoint `startOsFileDrag` (api-types → controller → renderer api, mirrors `clipboardWriteFilePaths`); new UIKit `Tree.onDragStartOverride` first-chance `dragstart` hook (keeps UIKit Electron-free) + `canDragRow` enablement; `TreeProviderView` wires it for `supportsOsClipboard` file providers — on Ctrl (non-root) `preventDefault()` + `api.startOsFileDrag([href])`. **User-verified** both directions (internal move; Ctrl+drag → Explorer + Teams). Single file at a time (multi-file deferred). `/review` clean (0 concerns). `/document`: `CLAUDE.md` Key Files (os-drag-service) + `trait-system.md` (onDragStartOverride + Ctrl-drag export pattern). `/userdoc`: `docs/tabs-and-navigation.md`, `docs/shortcuts.md`, `docs/whats-new.md` (v4.0.14). `tsc --noEmit` + `eslint` clean. Task folder `doc/tasks/US-833-drag-files-to-os/`. |
| US-832 | "Open in New Tab" context-menu item for App-menu folders | — | **Motivation:** user-added folders in the App bar's left panel (MenuBar) could be opened in a new tab by double-click, but the right-click menu only had Remove Folder / Show in File Explorer. **Change — single file `src/renderer/ui/sidebar/MenuBar.tsx`:** added an **"Open in New Tab"** item (first in the user-folder branch of `getMenuFolderContextMenu`, `OpenFileIcon`) calling the existing `openFolderInTab(folder)` — the exact double-click action (`pagesModel.addEmptyPageWithNavPanel` + close menu). Committed earlier (`e772a6d7`). `/review` clean (0 concerns — reuses the existing method + icon in the correct non-static-folder branch). `/document`: no dev-doc impact (MenuBar already documented; no new Key File/pattern). `/userdoc`: `docs/tabs-and-navigation.md` (Left Panel bullets), `docs/whats-new.md` (v4.0.14). No task folder (small change). |
| US-830 | CI release build 404s downloading the Castlabs Electron binary | — | **Symptom:** the **first** GitHub Actions build after the Electron Forge → electron-builder migration (US-827) failed at the "Build and Publish" step. All prior steps (3 Rust builds, npm install, Python + Castlabs EVS setup) passed; electron-builder then 404'd fetching Electron: `.../releases/download/vv43.0.0+wvcus/electron-v43.0.0+wvcus-win32-x64.zip` — note the doubled **`vv`**. **Root cause:** `@electron/get` builds the download URL as `mirror + customDir`, and its default `customDir` is already `v${version}` (`v43.0.0+wvcus`); the `electronDownload.mirror` in `electron-builder.yml` ended in `v`, so the tag doubled to `vv…` → 404. Never surfaced under Forge (different downloader). **Fix — single line in `electron-builder.yml`:** dropped the trailing `v` from the mirror (`…/download/v` → `…/download/`), added an explanatory comment. Verified the corrected single-`v` URL returns HTTP 200. **Verified end-to-end:** re-ran the CI via `gh workflow run publish.yml --ref upcoming-v4.0.14` (workflow_dispatch draft build) — full pipeline green in ~11 min **including VMP signing**; downloaded + installed `persephone-setup-4.0.14.exe` from the draft release; **user confirmed the app runs and DRM video plays** (Widevine working in the built-in browser). `/review` + `/userdoc` N/A (release infrastructure, never shipped a user-visible bug — like the Rust exception). `/document`: added a "Test Build (draft, without releasing)" section to `doc/standards/release-process.md` documenting the `workflow_dispatch` draft-build flow. No task folder (single-line release-infra fix). |
| US-829 | Grid editor — toggling "First row is header" (or changing the CSV delimiter) blanked the grid | — | **Symptom:** in the CSV grid, checking "First row is header" in the CSV Options dialog left every cell empty and kept the column headers stuck as `0`,`1`,… instead of adopting the first row as header names (same for a delimiter change). Found during US-828 testing; **confirmed not a csv-parse 7 regression** (csv-parse 6 vs 7 verified byte-identical for our exact options before attributing cause) — a pre-existing bug present in v4.0.13 and earlier. **Root cause:** `reparseRows()` in `src/renderer/editors/grid/GridEditor.ts` guarded column re-derivation with `hasSavedColumns = this.state.get().columns.length > 0` to preserve user column customization on reload — but on a header/delimiter toggle the column *identity itself* changes (numeric ordinals ↔ header names), so the stale saved columns were wrongly kept. **Fix:** added an opt-in `resetColumns` param — `reparseRows(content, resetColumns = false)`; only the CSV delimiter/header-toggle subscription passes `true` to force re-derivation, all other call sites (switch/restore/host-content/decrypt) keep the default `false` so the "preserve saved columns" invariant is intact. Doc comment extended with the rationale. **User confirmed fixed.** `/review` clean (0 concerns — narrowly-scoped opt-in, no other caller's behavior changes). `/document`: no dev-doc impact (behavior-only fix; `GridEditor.ts` already a Key File). `/userdoc`: `docs/whats-new.md` (v4.0.14 Bug Fixes); `docs/grid-editor.md` verified already correct. No task folder (small fix). |
| US-813 | Built-in browser — fix Google sign-in ("browser may not be secure") via a `window.chrome` shim | — | **Symptom:** signing into Google in the built-in browser failed after entering the email with *"This browser or app may not be secure."* on `/v3/signin/rejected`. **Live diagnosis (driven over MCP `execute_script`, reading the real Google page through the webview's own `executeJavaScript` to avoid attaching a debugger that would confound the measurement):** ruled out, one by one — UA string already clean Chrome (no `Electron`/`persephone`); UA **Client Hints** carried no `Electron` brand (only `[Not_A Brand, Chromium]`), and adding a `Google Chrome` brand via CDP `Emulation.setUserAgentOverride` did **not** help; `navigator.webdriver === false` and no debugger attached in normal browsing. **Root cause (A/B-proven):** `window.chrome` was an empty `{}` (real Chrome populates `loadTimes`/`csi`/`app`); injecting a realistic `window.chrome` **at document-start** stopped the `/rejected` redirect, while injecting the identical object **after page load** did not — Google reads it during initial load, so the shim must exist in the page's **main world at document-start**. **Fix — single file `src/preload-webview.ts`:** the webview preload (isolated world, document-start) uses `contextBridge.executeInMainWorld({ func })` to define a minimal `window.chrome` (`loadTimes`/`csi`/`app`) in the page's main world before its first script — keeps `contextIsolation` **on** (a plain preload assignment is invisible to the page; a DOM `<script>` is blocked by Google's CSP), needs **no debugger**, and is guarded (never overrides real Chrome) + `try/catch` (no-op on Electron without the API). **Debugger unchanged:** verified `wc.debugger.attach` is called only from the on-demand CDP IPC handlers reached via `browser_*`/scripting `.cdp()` — never on page open, and the app sets no `--enable-automation`; the fix touches no CDP. **Verified end-to-end:** rebuilt preload picked up by a fresh tab (no app restart — preload-only change); dummy-email submit no longer hit `/rejected`; **user confirmed real Google sign-in now completes.** (Drafted as US-812 against `main`; renumbered to US-813 on `upcoming-v4.0.14`, whose US-812 was already the `search_tools` task.) `/review` clean (0 concerns). `/document`: `browser-editor.md` (new "Browser Compatibility Shim" section + preload Key-Files row). `/userdoc`: `docs/whats-new.md` (v4.0.14 Bug Fixes). `tsc --noEmit` + `eslint` clean. Task folder deleted at completion — this entry is the surviving evidence summary. |
| US-812 | `search_tools` discoverability — tokenize multi-word queries + index toolset-level keywords | — | **Motivation (two observed agent failures, 2026-07-08):** a natural multi-word query whose every word appeared in a registered tool's metadata returned **0 matches**, and terms present only in the manifest's **toolset-level** `keywords` never matched — so agents searching for existing tools wrongly concluded none existed. **Root causes (confirmed in code):** (1) `searchTools()` in `mcp-handler.ts` tested the whole query as ONE contiguous substring (`hay.includes(needle)`) — its comment even claimed toolset keywords were included when they weren't; (2) `RegisteredTool` (`api/tools/registered-tools.ts`) never carried the manifest's toolset-level `description`/`keywords`, so the corpus physically couldn't see them. **Fix:** `RegisteredTool` gained optional `toolsetDescription`/`toolsetKeywords` (populated in `refresh()`); the keyword branch now tokenizes the query on whitespace (dedupe, drop <2-char tokens), scores each tool by distinct terms matched over the expanded corpus (tool id/description/keywords + toolset name/description/keywords), returns ≥1-term matches sorted by score (stable sort keeps registry order on ties) with an additive `score` field per result. Empty-query listing and `select:<toolset>/<tool>` exact lookup byte-identical. Descriptor text updated in `mcp-http-server.ts` (tool + `query` param) and the agent guide `assets/mcp-res-tools.md` (query-forms bullet). **Verified live over MCP after restart** against 3 registered toolsets (5 tools): the originally-failing 4-word query now ranks the right tool first with `score: 4` (weak `score: 1` matches trail — ranked OR working as designed); a term existing only in toolset-level `keywords` and another only in the toolset-level `description` both matched all tools of that toolset; `select:`/single-word/empty-query regressions clean. `/review` clean (0 concerns). `/document`: no dev-doc impact (no doc described the old semantics; Key Files unchanged). `/userdoc`: `docs/agent-tools.md` (search_tools row → tokenized/ranked semantics), `docs/whats-new.md` (v4.0.14). `tsc --noEmit` + `eslint` clean. Task folder `doc/tasks/US-812-search-tools-multiword-match/`. |
| US-811 | "Add Folder" button at the bottom of the App bar's left panel | — | **Motivation:** in the App bar's slide-out MenuBar, adding a custom folder shortcut to the left (categories) panel was only discoverable via the right-click "Add Folder" context menu — not obvious to users. **Change — single file `src/renderer/ui/sidebar/MenuBar.tsx`:** added the `Button` UIKit primitive to the existing barrel import and rendered a centered "Add Folder" button (`FolderPlusIcon`, `title`, `onClick={model.addFolder}`) wrapped in a `Panel` (`justify="center"`, `paddingTop="sm"`) placed as the last child of the `menubar-categories` column Panel, directly after the folders `ListBox` — since the `ListBox` has `flex: "1 1 auto"`, the button pins to the bottom. Reuses the existing `model.addFolder` handler (native folder-select dialog → `menuFolders.add`), so no logic is duplicated; the right-click "Add Folder" context-menu entry remains. First shipped as a full-width `block` button, then changed per user request to a centered, content-width button. `/review` clean (0 concerns — sanctioned uikit-barrel import under the `src/renderer/ui/` chrome exception, valid Panel/Button props, matches file's `menubar-*` naming convention). `/document`: no dev-doc impact (no new files/patterns; `MenuBar.tsx` already listed in `overview.md`/`folder-structure.md`, not a new Key File). `/userdoc`: `docs/tabs-and-navigation.md` (Left Panel — Folder List, documented the button + retained context menu), `docs/whats-new.md` (v4.0.13 New Features). `eslint` clean. No task folder (small UI change). |
| US-810 | App-window automation target for `browser_*` tools (`pageId: "app"`) | — | **Motivation:** the Playwright-like `browser_*` MCP tools could drive browser pages and boards, but not Persephone's own UI — an agent couldn't see/click/type in the app itself to help with UI issues (dev debugging) or assist end users with the interface. **Design (board-precedent shaped):** new `IBrowserTarget` singleton `appTarget` in `src/renderer/automation/AppTargetModel.ts` — `cdp()`/`insertText()` + page-interaction commands real; navigation/tab methods throw with pointers to `list_pages`/`execute_script`; `focusWebview()` a deliberate **no-op** (JS-event input needs no OS focus, and avoids stealing the user's window focus). **Two findings made it simpler than first estimated:** (1) **no registration needed** — MCP commands already route to a specific window's renderer (`windowIndex` → `sendToRenderer`), and `CdpSession.send` is an `ipcRenderer.invoke` from that renderer, so in `cdp-service.ts` `event.sender` **is** the window to automate; a sentinel regKey `APP_WINDOW_CDP_KEY` (`"__app-window__"`, `src/ipc/api-types.ts`) short-circuits all three IPC handlers (`cdpAttach`/`cdpDetach`/`cdpSend`) to `event.sender`'s top-level session (detach is a no-op — debugger shared with boards; `sessionId` passed through so nested board-iframe `f1-…` refs still resolve); multi-window automatic. (2) **`input.ts` is context-agnostic** — it never uses the CDP Input domain (built on JS events), so click/type/press-key work on the app's own session unchanged. **Resolution (`getTarget` in `commands.ts`):** `pageId === "app"` returns `appTarget` as the **very first** check, before all fallback branches — explicit-only, never reachable by omission, so an agent aiming at a web page can't accidentally act on the app chrome. Reuses the existing `mcp.browser-tools.enabled` gate (no new setting/privilege — `execute_script` already grants full Node). **Snapshot shows only chrome + the active page** — inactive pages are `display:none` on their `PageManager` placeholders and Chromium's AX tree excludes hidden subtrees, so NO filtering code was needed (confirmed live). **Docs:** tool descriptions + `get_app_info` instructions (`mcp-http-server.ts`) document `pageId: "app"`; `assets/mcp-res-pages.md` new "Automating Persephone's Own UI" section. **Verified live over MCP after restart** (5 pages open, active = grid): snapshot = chrome + grid only (other 4 pages absent); tab-switch click → snapshot swapped to audio player, grid gone; type filtered the grid to "1 of 9 rows"; full-window screenshot; `browser_tabs list` → single `app` pseudo-tab; navigation/`tabs new` → clean JSON-RPC errors; no-`pageId` fallback correctly errored (never resolved to app). **Known limitation (not a regression):** synthetic `browser_press_key` Escape didn't close the app **Menu** — `MenuModel` handles Escape via a focus-scoped React `onKeyDown`, and a synthetic click doesn't move focus into the menu; same synthetic-input limitation already documented for browser/board targets (document-level Escape listeners like `PopoverModel` do close). `/review` clean (0 concerns). `/document`: `overview.md` (MCP bullet), `folder-structure.md` (automation listing), `browser-editor.md` (three target kinds + resolution case 0 + new "App-Window Target" subsection + Key Files), `CLAUDE.md` Key Files (adapter row + amended CDP-service row). `/userdoc`: `docs/mcp-setup.md` (new "Automating Persephone's own UI" subsection + `pageId` value), `docs/whats-new.md` (v4.0.13). `tsc --noEmit` + `eslint` clean. Task folder `doc/tasks/US-810-app-window-automation/` deleted at completion — this entry is the surviving evidence summary. |
| US-809 | MCP `get_page_content`/`get_active_page` — image blocks for image pages + hints for non-text pages | — | **Motivation (observed agent failure):** an agent asked to look at two Snip (image-view) pages called `get_page_content`, got `content: ""` with no signal, and fell back to DOM-scraping `<img>` tags via `execute_script` — fragile, works only for mounted pages. The proper path (`page.asImage().savePngToFile`) existed but was discoverable only via `read_guide("scripting")`. **Design — three-way priority in a shared `resolvePageContent(page)` (`mcp-handler.ts`):** (1) text host → `{ content }` unchanged (source text beats pixels for LLMs — Mermaid/SVG/HTML/Draw/Graph keep returning source); (2) `IImageExport` capability (duck-typed `exportPng`, NO static editor import — no chunk coupling; headless per its contract, so **background pages work**) → PNG → base64 `{ image: { data, mimeType } }`, with a **5 MB base64 cap** (user-approved) degrading to a `savePngToFile` hint, and try/catch degrading export failures to the default hint; (3) else `{ hint }` via `hintForEditor` — browser-view → `browser_*` tools (snapshot/screenshot), board-view → `read_guide("boards")`/board files, video/pdf → `list_pages → filePath`, default → `execute_script` + facades. In practice the image branch fires for image-view only (the one IImageExport editor without a text host) but capability detection covers future visual editors. `getPageContent`/`getActivePage` became async (dispatcher already awaited) and spread the payload — no misleading `""` shipped; `McpActivePage.content` now optional + `image`/`hint` fields. **Server (`mcp-http-server.ts`):** new `toPageContentResult()` mirrors the `browser_take_screenshot` precedent — `image` payload → `[text metadata block, MCP image block]`, text/hint → JSON text; both tool handlers rewired + descriptions updated. **Guides:** `mcp-res-pages.md` new "Reading Page Content" section; `mcp-res-scripting.md` cross-ref in the saving-images section (savePngToFile = disk / oversize path). **Verified live over MCP after restart:** background Snip → agent saw the PNG inline; active Snip via `get_active_page` → metadata + image; text page byte-identical; browser page → browser_* hint (test tab closed). qa/ docs checked — no `""` assertions to fix. `/review` clean (0 concerns; noted the try/catch covers mount-requiring exporters like HtmlEditor which in practice never reach the branch). `/document`: `overview.md` MCP §4 bullet (adaptive content reads). `/userdoc`: `docs/mcp-setup.md` (tool table rows), `docs/whats-new.md` (v4.0.13). `tsc --noEmit` + `eslint` clean. Task folder `doc/tasks/US-809-mcp-read-image-pages/` deleted at completion — this entry is the surviving evidence summary. |
| US-808 | Explorer tree — selection & focus visuals (VS Code-like) + keyboard actions (Ctrl+C/X/V, Delete, F2) + chevron-only expand + focus-preserving navigation | — | **Motivation:** the Explorer tree only highlighted the main-editor file (folders lost their highlight instantly), gave no visual cue whether the tree had focus, and had no keyboard actions — all deferred from US-807. **Folder-selection root cause:** clicking a folder navigates the main area to a CategoryEditor, and `ExplorerEditor.onMainEditorChanged` finds no `filePath` on it → `_selectAndReveal(null)` wiped the just-set selection ms after the click. **Selection (sticky, VS Code-style):** `TreeProviderViewModel` state gained `selectedValue` — set on click **and right-click**, adopted from `props.selectedHref` only when it changes to non-null (a null never clears; that null was exactly the folder-wipe path), seeded from `initialState`, persisted via `getState()`; adopt deferred by `queueMicrotask` (setProps runs during render). `ExplorerEditorModel`/`ExplorerSecondaryView` unchanged. **Focus visuals:** `TreeProviderView` passes `keyboardNav`; UIKit `Tree` root emits `data-keyboard-nav` and gains descendant styles under `&[data-keyboard-nav]:focus-within` — selected row = `color.background.treeSelection` bg + `color.text.selection` fg + 1px `color.border.active` outline (VS Code focused-item look; `focusBorder`); active (hover/cursor) row = outline only; unfocused tree keeps today's gray. Non-opted-in Tree consumers (Git refs, Boards, Tools, Archive, Mneme, Link) are pixel-identical. **New theme token** (user compared snips vs VS Code — accent `#0078d4` too bright): `color.background.treeSelection` (`--color-bg-tree-selection`) added to `color.ts` + **all 9 themes** = per-theme VS Code `list.activeSelectionBackground` (Default Dark `#04395e`, Solarized Dark `#005a6f`; other 7 already had the list value in their accent token). **Keyboard:** Tree's existing `keyboardNav` nav (arrows/Home/End/PageUp/Down/Enter) enabled; `TreeModel.onKeyDown` falls back to the **selected row** when `activeIndex` is null; new `TreeRef.focus()` (`focusRoot` via rootId); wrapper div's tab stop removed (Tree root is THE tab stop); Escape-from-search focuses the Tree. New `TreeProviderViewModel.onTreeKeyDown` (wired before Ctrl+F/Escape; inert when target is input/textarea/contentEditable): **Ctrl+C/X/V** → US-807 os-clipboard actions (file provider only; no Cut on root; paste target = selected folder / file's parent / root), **Delete** → existing `deleteItemAction` (always its confirm dialog), **F2** → existing `renameItem` dialog — both provider-capability-gated (`writable` + `deleteItem`/`rename`, never root, so they also work in Mneme/Link panels, matching their context menus); `renameItem`/`deleteItemAction` made public. **Chevron-only expand (user-directed):** folder-label click no longer toggles — `onItemClick`'s `toggleItem` removed (selects + navigates only); chevron + ArrowRight/Left toggle. Ripple: applies to all TreeProviderView panels; Enter selects/navigates. **Amendment — root-collapse guard:** with the label-toggle (and its root guard) gone, ArrowLeft could collapse the chevron-less root irrecoverably → new UIKit `Tree` prop `canCollapse?(source, level)` checked in `toggleAt` **only when collapsing** (expansion never blocked, so collapseAll's root re-expand still works); TreeProviderView passes `href !== rootPath`. **Focus-steal fix (testing feedback — blue flashed to gray on file click):** two steal paths — `navigatePageTo` fired `onFocus` on all 3 exits (→ TextChrome TC8 refocus) and Monaco/Grid mount-autofocus. New leaf `core/utils/focus-utils.ts` `isFocusInSidebar()` (activeElement inside `[data-name="secondary-views-container"]`); `navigatePageTo`'s 3 `onFocus` sends + `MonacoBody.handleMount` `ed.focus()` + `GridBody` mount-autofocus all guarded. Activation paths (`showPage` tab switch incl. keyboard-driven, page-close fallback, `openFile` new-tab) unconditional — "switch tab → type immediately" preserved; in-editor link navigation still focuses (focus was in the page region). **Deferred** (backlog): multi-select, dimming cut items. User-tested (selection, focus colors vs VS Code snips, keyboard, clipboard, Delete/F2, focus retention). `/review` clean (0 concerns). `/document`: `pages-architecture.md` (Step 9 focus rule), `folder-structure.md` (focus-utils), `CLAUDE.md` Key Files. `/userdoc`: `docs/tabs-and-navigation.md`, `docs/shortcuts.md` (Explorer tables), `docs/whats-new.md` (v4.0.13), `docs/mneme.md` (stale click-toggle line). `tsc --noEmit` + `eslint` clean. Task folder `doc/tasks/US-808-explorer-tree-selection/`. |
| US-807 | Explorer panel — Windows clipboard copy/paste of files (CF_HDROP interop) | — | **Motivation:** copy files in Windows Explorer → paste into a folder in Persephone's Explorer tree, and copy/cut in Persephone → paste in Windows Explorer. Electron's `clipboard` API can't do this (`readBuffer("FileNameW")` returns only the first file; `CF_HDROP` can't be written at all), so a native helper is required. **Decision (user-directed):** extend the existing Rust `snip-tool` crate with subcommands instead of adding a new Rust project — zero CI/installer changes since `persephone-snip.exe` already ships beside `persephone.exe`. **Rust (`snip-tool/`):** new `clipboard.rs` — `clipboard-read` prints `{"paths":[…],"dropEffect":"copy"\|"cut"\|"none"}` JSON (manual `DROPFILES` parse incl. legacy ANSI lists, `Preferred DropEffect` read, hand-encoded JSON escaping); `clipboard-write [--cut]` reads paths from stdin (one/line, avoids cmdline quoting limits), builds `CF_HDROP` + drop-effect DWORD (5=copy\|link, 2=move), **empty list ⇒ `EmptyClipboard` only** (used to consume a cut clipboard after paste, Explorer-style); `OpenClipboard` retried 10×50 ms; `main.rs` dispatches on `args().nth(1)` — no-arg snip path byte-for-byte unchanged; Cargo features `Win32_System_DataExchange` + `Win32_System_Memory` added (note: `HANDLE`=isize but `Global*` take `HGLOBAL`=`*mut c_void` → explicit casts). **Smoke-verified from a terminal:** write→read round-trip; PS 5.1 `Get-Clipboard -Format FileDropList` (independent consumer) sees our list; `--cut` reads back as `"cut"`; empty write clears. **Main+IPC:** new `clip-service.ts` (`readClipboardFiles`/`writeClipboardFiles`; degrades to empty result when the exe is missing — dev needs a one-time `cargo build --release`), `getSnipToolPath` exported from `snip-service.ts`, new `ipc/clipboard-ipc.ts` DTO, `Endpoint.clipboardReadFilePaths`/`clipboardWriteFilePaths` through api-types/controller (lazy import)/renderer api. **Renderer:** new `core/utils/copy-files.ts` — `copyPathsInto()` recursive copy/move on `app.fs` (folder-into-itself refusal, same-location skip, rename fast path + copy-then-delete fallback for cross-volume/occupied moves, pre-scanned file totals for progress, per-item error collection); new shared `components/tree-provider/os-clipboard.ts` (`supportsOsClipboard` ≡ `provider.type === "file"`, `copyPathToOsClipboard`, `pasteOsClipboardInto` — overwrite confirm **before** the progress overlay via `provider.list()` name clash, `ui.createProgress` "Copying N of M", cut-clipboard consumption after clean move). Menu items in **both** `TreeProviderViewModel` (tree) and `CategoryViewModel` (folder view — verified file-provider-backed via `CategoryEditor`'s sibling-host `treeProvider`): Cut/Copy on files, Cut/Copy/Paste on folders (no Cut on the explorer root, mirrors Rename/Delete gating), Paste on background (renders even without `mkdir`); Paste is always shown — click-time empty-clipboard check avoids spawning the exe per right-click. New `CutIcon` (scissors) in `theme/icons.tsx`. **Decisions:** context-menu ONLY — no Ctrl+C/X/V (tree selection/focus isn't rendered clearly enough; selection visuals + keyboard + multi-select tracked in backlog Technical Debt); single-item copy from Persephone (paste side handles any count); overwrite-confirm dialog (no "(2)" auto-rename); no script-API surface. Mneme/Link/Archive providers show none of it. User-tested both directions. `/review` clean (0 concerns). `/document`: `folder-structure.md` (snip-tool, clip-service, clipboard-ipc, os-clipboard, copy-files) + `CLAUDE.md` Key Files. `/userdoc`: `docs/tabs-and-navigation.md`, `docs/editors.md` (Folder View menus), `docs/whats-new.md` (v4.0.13 New Features). `tsc --noEmit` + `eslint` + `cargo build --release` clean. Task folder `doc/tasks/US-807-explorer-clipboard/`. |
| US-806 | Memory growth investigation — ~3 GB sessions (root cause: React dev performance tracks) + fixes | — | **Symptom:** the dev app (`npm start`) climbed to ~3 GB after extended browsing but restarted to ~400 MB with the same pages restored; terminal showed `MaxListenersExceededWarning: 11 destroyed listeners added to [WebContents]`. **Investigation (two live instrumented sessions, black-box first):** external per-process sampling (`Win32_Process.CommandLine` `--type=` mapping) attributed the growth to the **app renderer's native private memory** (main +~120 KB/nav, webview guest flat); an elimination ladder (bare webview with identical partition/preload/allowpopups = flat; direct `loadURL` = leaks; dropping `browser:register` = leaks; removing renderer `browser:event` handlers = **flat**; bogus-state-key notify without re-render = flat; plain-text-page churn = flat; 300-row grid churn = leaks) proved the unit of leakage is a **large-tree React re-render** (~100-400 KB retained each, GC-controlled), generic to the app, with the browser page as amplifier (6-10 state updates/nav through `BrowserEditorView`'s ~28-key selector + continuous title/audio events from media sites). Temp main-process instrumentation (`us806-debug.ts`: `getAppMetrics`/census/`Memory.getDOMCounters`/memory-infra tracing) + heap-snapshot histogram diff then pinned it: **`native:PerformanceMeasure` +10,736 per 100 renders** — React 19 **dev-build** "Components ⚛" performance tracks (~107 `performance.measure()` per large render, `detail.devtools` payloads) buffered **unboundedly**; native cost lands in malloc/PartitionAlloc/Oilpan so every JS-side counter looked flat. Prevention test (no-op `performance.measure`) cut steady-state growth to noise. Second bounded component: one-time V8 committed-heap expansion (~50 MB, plateaus). **Release build verified clean** (4.0.11: 400 renders → 0 measures, RSS plateaus ~235 MB) — the multi-GB leak is dev-only. **Fixes:** (1) `browser-service.ts` — `did-create-window` + `destroyed` routed through the tracked `on()` helper (re-registration on every `dom-ready` now removes them; `destroyed` runs full `unregisterWebview`) — verified live: counts constant across 15 navs, warning gone; (2) new `core/utils/performance-janitor.ts` (+ start in `renderer.tsx`) — clears measures/marks at >10k every 60 s, self-gating no-op in release; (3) `BrowserView.tsx` — `BrowserWebviewItem` memoized (comparator: model/partition/isActive/tab.id/URL-blankness) + `BlankPageLinks` memoized — verified: browser state churn no longer reconciles the webview subtree; (4) duplicate-webview-mount console-warning detector (one orphaned duplicate guest observed once, trigger unknown — detector makes recurrence self-identifying; hunt stays open). Temp instrumentation removed after verification. `/review` clean (0 concerns). `/document`: `browser-editor.md` (memoization section, tracked-`on()` pitfall, Key Files fixes) + `CLAUDE.md` Key Files. `/userdoc`: `docs/whats-new.md` (v4.0.12 Bug Fixes). `tsc --noEmit` + `eslint` clean. Task folder deleted at completion — this entry is the surviving evidence summary. |
| US-800 | Packaged build spawns child processes with a stripped environment | — | **Symptom:** from the installed build, child processes launched via `persephone.execute()` (boards/scripts) received a **~14-var** environment missing `APPDATA`, `LOCALAPPDATA`, `ProgramData`, `ProgramFiles*`, etc.; concrete break was `dotnet restore` failing (`NuGet.targets: Value cannot be null. (Parameter 'path1')`) because NuGet couldn't compute its config-folder paths. `npm start` was unaffected. **Investigation (empirically verified):** `execute()`/`command-runner.ts` correctly forwards `{ ...process.env, ... }` — not at fault; a Rust repro proved the launcher's `DETACHED_PROCESS \| CREATE_NEW_PROCESS_GROUP` spawn does **not** strip env (child inherited the full set); Persephone never mutates `process.env`. **Root cause:** the packaged **main process's own `process.env`** was already truncated at launch and faithfully inherited downstream. The reporter's taskbar pin targets `persephone.exe` directly (launcher bypassed), so the parent is `explorer.exe` — the ~14-var set (base machine vars present, profile/known-folder vars absent) is the signature of a **shell running with a degraded environment** (explorer restarted outside a normal logon). Confirmed by reading the PEB env blocks of live `explorer.exe`/`persephone.exe` (both healthy — 61 vars — after a reboot cleared the degraded state), so the failure is transient/non-deterministic; the fix is defensive recovery, not prevention. **Fix — new `src/main/windows-env.ts` (`reconstructWindowsEnv`, win32-only, backfill-only):** at the top of `setupMainProcess()` (before `initCommandRunner()` and mneme/tor/vlc/snip), backfills missing standard folder/system vars from **env-independent** sources — Electron `app.getPath('home'\|'appData'\|'temp')` (SHGetKnownFolderPath) + `SystemDrive`/`SystemRoot` — so all child spawns get parity even from a degraded shell; never overwrites a present var (no-op under `npm start`/normal launch). **Verified:** real module run against a simulated 14-var env → 18/18 assertions (all vars reconstructed incl. `LOCALAPPDATA` = `dirname(APPDATA)/Local`; pre-existing vars untouched). `/review` clean (0 must-fix). `/document`: `folder-structure.md` + `CLAUDE.md` Key Files. `/userdoc`: `docs/whats-new.md` (v4.0.12 Bug Fixes). `tsc --noEmit` + `eslint` clean. Task folder `doc/tasks/US-800-packaged-stripped-child-env/`. |
| US-799 | Keep board-spawned processes alive across navigation (`setBoardBusy`/`getBoardBusy`/`getJobs`) | — | Boards can declare themselves **busy** so their spawned processes (dev servers) survive the board's unload (page navigation, board reload); page/tab close, app quit, or busy=false + unload still kills them. New bridge API: `persephone.setBoardBusy(busy)` (shim → host-frame postMessage → `BoardEditorModel.setBusy` — renderer authoritative — → IPC mirror to main), `getBoardBusy()` (carried in the port-handshake init message), `getJobs()` (live jobs incl. previous board lifetimes — control-only handles: kill/stdin work, no output streaming; output while unloaded is dropped), and `execute(cmd, { name })` — named jobs as the stateless re-association key (board `localStorage` is in-memory by design: `appPartition = 'nopersist'`). Main (`board-bridge.ts`): job sinks keyed per owner (= `BoardEditorModel.id`, stable across mounts); `disposeBoardPort` keeps a busy owner's jobs; `reapBoardOwner` (model dispose = page close), non-busy dispose, `reapHost` (renderer crash), and will-quit reap everything; `command-runner.ts` stores `command`+`name` per job + `getJobsBySinkIds` query. Renderer: new `EditorModel.keepAliveOnNavigation()` hook — a busy board survives `setMainEditor` as an invisible ownership handle (no view, iframe destroyed) and is promoted back to main via the existing `matchesNavigationTarget` singleton reuse; `movePageOut` disposes keep-alive editors (cross-window move kills processes — documented limitation); session restore drops non-main board descriptors (busy is transient — no zombie handles). Boards panel shows a green “running” dot (`busy-boards.ts` reactive registry). Author reinit contract documented in `board-template/CLAUDE.md`, `mcp-res-boards.md`, `board-api.d.ts`. Design v2 (user-directed): keep only the processes, not a hidden iframe — no `Pages.tsx` keep-alive DOM, reload preserves processes, no compare-mode hazard. |
| US-797 | MCP server — fix unbounded session leak (idle reaper + cap) | — | **Symptom:** after Persephone ran a full day, Settings showed *"178 clients connected"* and climbing. **Root cause (verified against installed `@modelcontextprotocol/sdk` 1.27.1 source):** `mcp-http-server.ts` keeps a `sessions` map that grows on every `initialize` but only shrinks via `transport.onclose`, which fires **only** on an explicit HTTP `DELETE` or a manual `transport.close()`. SSE-stream disconnects are deliberately ignored by the SDK (the session is designed to outlive a single connection so a client can reconnect with the same `Mcp-Session-Id`). MCP clients (Claude Code restart, one-shot clients, reconnect after sleep) routinely terminate **without** sending `DELETE`, so their sessions — each a full `McpServer` + transport held in memory — leaked forever. No built-in idle timeout exists in the TS SDK (the .NET SDK's `IdleTrackingBackgroundService`/`IdleTimeout` is the canonical design this re-implements). **Fix — single file `src/main/mcp-http-server.ts`:** (1) the `Session` entry now carries `lastActivity`, bumped via a new `touchSession()` on every POST/GET/DELETE to a known session; (2) an idle reaper (`setInterval`, every `SESSION_SWEEP_MS`=60 s, `.unref()`'d so it never holds the process open) calls `transport.close()` on sessions idle past `SESSION_IDLE_MS`=30 min; (3) a `MAX_SESSIONS`=500 cap evicts the least-recently-active session on `initialize` as a burst backstop. All eviction paths funnel through `transport.close()` → existing `onclose` → `sessions.delete` + `broadcastMcpStatus` (each fire-and-forget `.catch()` deletes + broadcasts directly if a wedged transport throws); the sweep timer is cleared in `stopMcpHttpServer`. **Deliberately not done:** eviction on SSE-stream `close` — that breaks legitimate reconnect/resumability (typescript-sdk issue #1852 anti-pattern); well-behaved clients that send `DELETE` still leave instantly. **Research:** WebSearch/WebFetch over typescript-sdk source + issues #812/#1852 confirmed the SDK behavior and the app-level-reaper consensus; the official examples (`examples/legacy-routing/server.ts`) carry the same leak with no reaper. **Verified live:** after restart, `get_app_info` + `execute_script` (full main↔renderer round-trip) work; reconnect cleanly mints one session. `/review` 0 must-fix (1 suggestion applied — broadcast on the cap-eviction catch path for parity with the sweep). `/document`: `overview.md` MCP §4 gained a "Session lifecycle" bullet. `/userdoc`: none (purely internal; the existing "N clients connected" indicator is unchanged from the user's view, it just stays accurate now). `tsc --noEmit` + `eslint` clean. No task folder (small fix). |
| US-796 | Fix `board_refresh` appearing stale to automation (CDP frame resolution) | — | **Reported symptom:** the `board_refresh` MCP tool "looked like it doesn't work" — after editing a board's files and refreshing, follow-up reads showed pre-edit content. **Long misdiagnosis (worth recording):** all verification was via `browser_evaluate`/`browser_snapshot` (CDP), which were themselves **unreliable** here, so the bug was first chased on the **board-serving side** — a committed attempt (`cf27a82b`) switched `board-protocol-service.ts` from `net.fetch(file://)` to `fs.readFile` (Chromium ignores `Cache-Control: no-store` for the privileged `board://` scheme) and added a per-mount `?v=<boardId>` cache-buster + a `clearBoardCache` IPC. None of it fixed the symptom. **Actual root cause (confirmed once the user reported the *visible* board showed fresh content while CDP showed stale):** the board renders fine; the bug is in the **CDP read path**. `cdp-service.resolveBoardSession` matched the board frame by `board://<host>` **origin** via `.find()` first-match — ambiguous when the **same board is open in multiple tabs** (same origin) or when a remount's **pre-reload frame** briefly lingers, so automation attached to the wrong/old frame and reported stale DOM. Compounded by hidden (`display:none`) tabs and Persephone's lazy/postponed tab restore. **Fix (`8b520d16`):** reverted the speculative board-side changes (`board-protocol-service.ts` back to streaming `net.fetch`; dropped `clearBoardCache` + the async `reloadBoard`), **kept** the `?v=<boardId>` nonce on the iframe `src` — now doing double duty as a unique per-frame id — and threaded that nonce through `registerBoardFrame` (`BoardWebview` → `api.ts` → `api-types.ts` → `controller.ts` → `cdp-service.ts`). `resolveBoardSession` (and the screenshot clip selector) now resolve the **exact** frame whose URL carries this registration's nonce, falling back to origin-match when no nonce is known. **Verified live:** single-tab (edit → `board_refresh` → CDP reads fresh, matched the user's visual), and two-tab (refresh one → that tab reads its own fresh content, sibling reads its own — distinct nonces, no cross-frame contamination, even for a hidden refreshed tab). `/review` clean (0 must-fix). `/document`: `CLAUDE.md` Key Files (BoardWebview iframe `?v=` nonce + BoardTargetModel frame-by-nonce resolution). `/userdoc`: one `docs/whats-new.md` v4.0.10 Bug-Fixes entry (the reverted cache "fix" was introduced+reverted within the same cycle → omitted per release rules). `tsc --noEmit` + `eslint` clean. No task folder (debugging-driven fix). |
| US-795 | Hotfix — boards fail to load in release build ("bridge did not connect") | — | **Symptom:** in the v4.0.8 **installed** build, opening a board painted once but its `persephone` bridge never initialized; the board's `ui.log` showed *"board bridge did not connect (the board loaded but its script bridge never initialized)."* Dev (`npm start`) was unaffected. **Root cause (confirmed live via the release build's Persephone MCP):** the board↔host `MessagePort` handshake baked the host-renderer origin into the shim and required `event.origin === boot.hostOrigin` (`src/board-shim.ts`). The installed window loads from `file://`, whose origin serializes **inconsistently across boundaries** — the **main process** computes `boot.hostOrigin = new URL(file://…).origin` → **`"null"`** (Node URL), while the **board frame** sees the host's `event.origin` as **`"file://"`** (Chromium) — so the strict check never matched and the transferred port was dropped (the shim ran, `window.persephone` was defined, but `{kind:"connected"}` never reached main → handshake watchdog fired). Dev works only because the host is `http://localhost:<port>`, where both serializations agree. **Fix — `src/board-shim.ts`:** enforce the origin check **only for http(s) hosts** (`/^https?:\/\//`); for `file://`/opaque/unknown hosts drop it and rely on the load-bearing `event.source === window.parent` gate, and route the non-sensitive outbound host-frame pings (`board:interact` / `board:error`) to `"*"` (the host re-validates by board origin + source frame). **Verified:** packaged build (`npm run dist:zip` → `release/win-unpacked/persephone.exe`) — boards connect, log shows only `board loaded`. `tsc --noEmit` + `eslint` clean. Released as v4.0.9. No task folder (single-file hotfix). |
| US-794 | HTML Viewer — dismiss host menus/popovers on click inside the preview | — | **Motivation:** clicking inside the Browser webview or a Board iframe closes any open Persephone context menu / popover (both forward inner clicks to the host), but the HTML Viewer did not — a right-click menu stayed open when the user clicked into the preview. **Cause:** the HTML Viewer renders an `<iframe srcDoc=… sandbox="allow-scripts">`; the opaque-origin sandbox means inner clicks never bubble to the host document. **Change — single file `editors/html/HtmlBody.tsx`:** (1) extended the script already injected into the preview `srcDoc` (renamed `navigationBlockerScript` → `injectedScript`) with a **capture-phase `pointerdown`** listener that posts `{__persephone:"html:interact"}` to `window.parent` (targetOrigin `"*"`); (2) a host-side `window` `message` listener validates `e.source === iframeRef.current?.contentWindow` and turns `html:interact` into the same `document.body` `mousedown` the Browser/Board use to tear down overlays. Faithful mirror of the Board's `board:interact` pattern (`BoardWebview.tsx`); the one intentional difference — source-window validation instead of `e.origin` checking — is forced by the sandbox's opaque origin and is equally secure. `/review` clean (0 must-fix). `/document`: no architecture-doc impact (small editor-internal behavior; the parallel `board:interact` is likewise only in code comments). `/userdoc`: `docs/editors.md` (HTML Preview — "Menu dismiss on click"), `docs/whats-new.md` (v4.0.8 Improvements). `tsc --noEmit` + `eslint` clean. No task folder (small enhancement). |
| US-793 | "Open in Browser" page-tab context-menu item for HTML files | — | **Motivation:** clicking a local `.html` file in Explorer opens it in Monaco (expected), but the user sometimes needs to view it rendered in a browser; there was no quick way from the page tab. **Change — `editors/shared/editor-menu-items.tsx`:** new `openInBrowserMenuItems(filePath)` builder — returns an **"Open in Browser"** item (globe icon, own group via `startGroup`) only for `.html`/`.htm`/`.xhtml` files that are saved to disk, else `[]` (hidden); wired into `textFileMenuItems` after the file-path group. Fires the standard `openRawLink` pipeline with `target:"browser", browserMode:"internal"`. **Root-cause fix — `content/resolvers.ts`:** browser-routing previously lived **only inside the HTTP resolver** (gated by `isHttpUrl`), so `target:"browser"` was silently ignored for local files — the file resolver built a content pipe and the open-handler just activated the existing Monaco tab. Extracted the browser-routing block into a shared module-level **`openLinkInBrowser(data)`** helper (honors `browserPageId` / `browserMode` os-default·internal·profile·incognito / `link-open-behavior` fallback); the **HTTP resolver** now calls it, and the **file resolver** gained an early `data.target === "browser" || data.browserMode` branch that normalizes the path to a `file://` URL via the new `toFileUrl()` and delegates to the helper — so `target:"browser"` now works for local files everywhere, not just the page-tab menu. **`content/link-utils.ts`:** new `toFileUrl(pathOrUrl)` — path → `file://` via `url.pathToFileURL` (correct drive-letter/percent encoding), pass-through for anything already carrying a scheme; module-scope `require("url")` follows the `path-utils.ts` precedent (commented; `file-path.ts` exposes no `pathToFileURL`). `/review` 0 must-fix + suggestion applied (documented the `require("url")` reason). `/document`: `content-pipeline.md` (Layer 2 — file/HTTP resolver browser short-circuit + shared `openLinkInBrowser()`), `CLAUDE.md` Key Files (`openInBrowserMenuItems`). `/userdoc`: `docs/editors.md` (HTML Preview — "Open in Browser"), `docs/tabs-and-navigation.md` (HTML file tabs menu table), `docs/whats-new.md` (v4.0.8 Improvements). `tsc --noEmit` + `eslint` clean. No task folder (small enhancement). |
| US-792 | Mermaid viewer — "Convert to Excalidraw" (native editable shapes) | — | **Motivation:** the Mermaid viewer's "Open in Drawing Editor" button embeds the rendered diagram as a single flat **image** in Excalidraw (not editable per-element). The user wanted a way to convert into **individually-editable shapes**. **Library:** added `@excalidraw/mermaid-to-excalidraw@^2.2.2` — the official package powering Excalidraw's own "Mermaid to Excalidraw" insert (`@excalidraw/excalidraw@0.18` + `mermaid@11` were already deps; the library bundles its own mermaid). **Change — `editors/draw/drawExport.ts`:** new `buildExcalidrawJsonFromMermaid(source)` → **dynamic** `import("@excalidraw/mermaid-to-excalidraw")` (review must-fix: `drawExport.ts` is statically imported by the svg/graph/image editors, so a top-level import would bundle the library + its mermaid into all those chunks; dynamic import loads it only on conversion) → `parseMermaidToExcalidraw(source, { themeVariables: { fontSize: "16px" } })` → override the skeleton font to `FONT_FAMILY.Helvetica` *before* `convertToExcalidrawElements` (Excalidraw's default is the hand-drawn Excalifont, which the user disliked; setting it pre-conversion keeps text dimensions correct — covers standalone `text` + container/arrow `label.fontFamily`) → same `{ type: "excalidraw", … }` scene-JSON envelope the Draw editor parses. Returns `{ json, imageOnly }`. **`editors/mermaid/index.tsx`:** new **"Convert to Excalidraw (editable shapes)"** `IconButton` (after the existing "Open in Drawing Editor", `disabled={!svgUrl}`) + `onConvertToExcalidraw` handler reading the raw Mermaid source from `model.host`; opens a `draw-view` page. Only **flowchart / sequence / class** convert to real shapes — for other types the library auto-returns a single image element (`imageOnly`) and we `ui.notify` "opened as an image"; a genuine Mermaid parse failure (`try/catch`) falls back to `onOpenDraw()` (the existing rendered-SVG embed) + notify. The original "Open in Drawing Editor" button is unchanged. **Icon — `theme/language-icons.tsx`:** new `DrawOrangeIcon` (same pencil glyph as the cyan `DrawIcon`, tinted `color.misc.orange`); review suggestion applied — both now share a `makeDrawIcon(tint)` factory (was duplicated SVG). Orange chosen after the user found green too close to the cyan `DrawIcon`. **New token — `theme/color.ts` + all 9 `theme/themes/*.ts`:** `color.misc.orange` (`--color-misc-orange`) with palette-fitting values (monokai `#fd971f`, solarized `#cb4b16`, light themes `#d2691e`, dark themes `#e8833a`) + the doc-comment in `default-dark.ts`. `/review` 1 must-fix (static→dynamic import, applied) + 2 suggestions (icon factory applied; `Record<string,string>` theme-token typing noted as pre-existing). `/document`: no dev-doc impact (contained feature, follows the existing dynamic-import + color-token patterns; `drawExport.ts` not a Key File). `/userdoc`: `docs/editors.md` (Mermaid section — Convert vs. Open comparison table) + `docs/whats-new.md` (v4.0.8 New Features). `eslint` + `tsc --noEmit` clean. Task folder `doc/tasks/US-792-mermaid-convert-to-excalidraw/`. |
| US-791 | Colored `BoardColorIcon` for the Boards sidebar panel header | — | **Motivation:** the Explorer-sibling "Boards" sidebar panel showed its `BoardIcon` glyph in the default (monochrome) font color; the user wanted a colored header icon. **Change — `theme/icons.tsx`:** added `import color from "./color"` and a new exported **`BoardColorIcon`** — the same 4-panel dashboard layout as `BoardIcon`, but each `<path>` is `fill`ed with a distinct theme token (`color.misc.blue` top-left, `green` top-right, `yellow` bottom-left, `red` bottom-right) instead of a `currentColor` stroke; the `<g>` keeps `fill="none"` as a safety annotation (review suggestion — every path carries its own fill, so a future strokeless copy-paste can't fall back to black). Colors are CSS variables, so they re-tint with the active theme (no hardcoded hex — `StorybookIcon`'s fixed brand hex stays the only such exception). **`editors/register-editors.ts`:** the `boards` secondary-view registration now uses `createElement(BoardColorIcon)`; the import was swapped from `BoardIcon` (its only use in that file). `BoardIcon` is unchanged and still used by page tabs, tree item icons, `BoardGlyph`, the Create-Demo menu, etc. `/review` clean (0 must-fix; `fill="none"` suggestion applied, path-reorder suggestion declined — `BoardColorIcon`'s order is already the clean TL/TR/BL/BR reading order, and aligning would mean reordering the working `BoardIcon`). `/document`: no dev-doc impact (icon glyphs aren't enumerated in Key Files; no new pattern). `/userdoc`: `docs/whats-new.md` (v4.0.8 UI Polish). `eslint` clean. No task folder (small visual change). |
| US-790 | Folder View (CategoryView) — context-menu + footer parity with the Explorer tree | — | **Motivation:** opening a folder from the Explorer shows its contents in the folder-content page (`CategoryView`, `tree-category://`), but its right-click menu only had Copy Path / Rename / Delete / Open — missing the href-based items the Explorer tree already offers, with no central place that built them. **Investigation finding:** the "general handler keyed solely on the `ILink`" the user wanted **already exists** — the `app.events.linkContextMenu` channel + handlers in `content/tree-context-menus.tsx` (add "Open in New Tab/Window", "Show in File Explorer", "Open in Browser"/"Open in Rest Client" by href/isDirectory). The Explorer tree (`TreeProviderViewModel`) and the Categories list/tiles (`LinkItemList`/`LinkItemTiles`) fired it; `CategoryView` did not. **Change — `components/tree-provider/CategoryViewModel.tsx`:** (1) `onItemContextMenu` now sets `ctxEvent.target = item` and assigns `e.nativeEvent.contextMenuPromise = app.events.linkContextMenu.sendAsync(ctxEvent)` (direct-assignment form, matching the `LinkItemList` sibling — no Layer-3 IIFE since `CategoryView` has no `onContextMenu` prop), so the folder page gets the identical centrally-defined link items. (2) **New File / New Folder:** new `createNewFile`/`createNewFolder` (mirror `TreeProviderViewModel`, reload via `loadItems()`), an `onBackgroundContextMenu` that creates in the currently-viewed `this.props.category` (skipped when a folder is the right-click target — that folder's own menu carries them), and New File/New Folder on folder items (create *inside* via a `getItemListPath` helper = `category + "/" + title`); all gated on `provider.writable && provider.mkdir` (so read-only providers like archives show none). Wired in `CategoryView.tsx` via `CategoryViewRoot onContextMenu={model.onBackgroundContextMenu}`. (3) **Footer consolidation:** replaced the custom `.cv-footer` styled div (18px, 11px font, left-aligned) with a UIKit `Panel` mirroring the Monaco `EditorToolbar` footer (dark bg, top border, centered, `paddingX="sm"`) + a right-aligned `.cv-footer-count` label at `fontSize 13`; hardcoded `height={29}` because `EditorToolbar` is content-height-driven (Monaco's icon row is 29px, a text-only row ~20px) so the two footers stay aligned. `/review` 2 must-fix — C1 inline `style=` on the footer label → moved to a `.cv-footer-count` rule on the styled root (matches the file's class pattern); C2 (no Layer-3 IIFE) **declined** with rationale (consistent with the `LinkItemList` sibling; reviewer confirmed not a current bug) — + suggestion applied (`catch (err: any)` → `catch (err)` in the two new methods). `/document`: `context-menu.md` (noted `CategoryView` + Categories list/tiles fire the same `linkContextMenu` channel and the background New File/Folder). `/userdoc`: `docs/editors.md` (Folder View — context menu + New File/Folder + footer), `docs/whats-new.md` (v4.0.8 Improvements + UI Polish). `eslint` clean. No task folder (small fix). |
| US-789 | Header "snip" menu — quick screenshot to Image View | — | **Motivation:** the only screen-snip entry point was the Excalidraw editor's "Screen Snip" button, which always hides Persephone and inserts the result into the drawing. The user often just wants a quick screenshot (e.g. to paste into Teams) — frequently of Persephone's own content (an image or a web page open in the built-in browser) — without editing it. **Change:** a green horizontal three-dot ("…") trigger in the app header (`MainPage.tsx`, first child of `.status-indicators`, before the Mneme indicator) opens a `WithMenu` with two items: **Snip Screen** (hides Persephone, snips the desktop) and **Snip Persephone** (leaves every window visible so the user can snip Persephone's own content). Both capture via the existing snip pipeline and open the captured PNG in a new **Image View** page (copy / save / open-in-Excalidraw) by converting the returned data URL to a blob URL and calling `pagesModel.openImageInNewTab(blobUrl, "Snip")` — the same Image View the clipboard-paste path uses. **Why "Snip Persephone" works with no Rust changes:** the snip tool (`snip-tool/src/main.rs`) captures every monitor *first*, then shows the selection overlay over that frozen snapshot — so leaving Persephone visible captures it correctly. **Pipeline:** `startScreenSnip` gained a `hideWindows: boolean` param threaded through the IPC chain (`ipc/api-types.ts`, `ipc/renderer/api.ts`, `ipc/main/controller.ts` — `event`-first per the controller convention); `main/snip-service.ts` now gates `hideWindows()`/`showWindows()` on the flag (and skips the 200 ms repaint wait when not hiding). New Object Model method **`app.shell.startScreenSnip(hideWindows)`** (`api/shell/shell-calls.ts` + `shell/index.ts` + `types/shell.d.ts`) so the header routes through `app.*` instead of the raw IPC `api` (review must-fix C1). New horizontal **`MoreHorizIcon`** in `theme/icons.tsx`, rendered oversized (28px) and clipped to a 14px-tall `overflow:hidden` button so the dots read large without growing the header strip. `editors/draw/index.tsx` calls `startScreenSnip(true)` (unchanged behavior). The trigger is always rendered (Windows-only target; mirrors the unconditional Excalidraw button — review C2 documented); `runSnip` wraps its body in try/catch → `app.ui.notify` on failure (review C3); menu items lifted to module scope (review suggestion A). `/review` 2 must-fix (C1 raw-`api` import → routed via `app.shell`; C2 always-on → documented) + suggestions A/C applied, B declined (pre-existing hardcoded close-button color). `/document`: `CLAUDE.md` Key Files (`snip-service.ts` row) + `overview.md` (`app.shell` summary). `/userdoc`: `docs/whats-new.md` (v4.0.8 New Features), `docs/editors.md` (new "Screen Snip" section + Image Viewer & Drawing Editor cross-refs). `tsc --noEmit` + `eslint` clean. Task folder `doc/tasks/US-789-header-snip-menu/`. |
| US-788 | Git panel — keep the "Git (N)" modified-file count accurate while the panel is collapsed | — | **Motivation:** the merged Git secondary view header shows a `Git (N)` changed-file count (US-781/US-783) visible even when the panel is collapsed. But the visibility-aware refresh optimization (US-634/US-781) marked the **changes** submodel stale instead of reloading it when the panel wasn't the active (expanded) one, so on a working-tree change the collapsed header's count never updated until the panel was expanded. **Change — `GitTreeEditorModel.refresh()`:** now **always** calls `this.refreshChanges()` regardless of panel visibility, so each watcher-driven repo change reloads the staged/unstaged lists and the header count stays correct. The **refs** surface (Branches/Tags) keeps its visibility gate — nothing off-screen depends on its data — reloading only when visible (`isRefsVisible()`) and marking stale otherwise. Removed the now-dead `changes.stale` catch-up branches in `setGitPanelTab()`/`onPanelExpanded()` (changes is never stale now), the now-unused `isChangesVisible()` helper, and the unused `stale`/`markStale()`/`_stale` machinery on `GitChangesModel`. 2 src files (`GitTreeEditorModel.ts`, `GitChangesModel.ts`). `tsc --noEmit` + `eslint` clean. No task folder (small fix). |
