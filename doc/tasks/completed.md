# Completed Tasks

Last 20 completed tasks, newest first. Older tasks are pruned.

| ID | Title | Epic | Notes |
|----|-------|------|-------|
| US-589 | TextChrome — restore chrome for v4 editors (regression from US-559) | — | `isTextFileHost(host)` in `src/renderer/editors/base/TextChrome.tsx` discriminated `TextFileModel` from `NoteItemEditModel` by duck-typing on `setEditorToolbarRefFirst`. US-559 ("Strangler-fig retirement") removed that method from `TextFileModel` but left it on `NoteItemEditModel`, silently inverting the check: TRUE for the inner per-note fake host, FALSE for the real TextFileModel. Symptoms (all silent, all v4 editors — notebook, monaco, grid, markdown, html, todo, link, log-view, mermaid, svg, graph, draw, rest-client): footer toolbar gone, ScriptPanel never rendered, Run/Compare/ShowResources buttons gone, editor-overlay portal target never mounted (surfaced as "Expand note doesn't work" because `NotebookBody` portals `ExpandedNoteView` into `editor.host.editorOverlayRef` which was null). F5 still worked because it's handled in `handleRootKeyDown`, not gated on `textHost`. Fix: duck-type against `setEditorOverlayRef` instead (present on TextFileModel, absent on NoteItemEditModel) + comment explaining the regression for future maintainers. 1 file changed (commit `4ce4a1f`). |
| US-588 | Lint baseline — fix errors, then triage warnings | — | Reached the documented "sustainable lint baseline" goal: `npm run lint` → **0 errors / 0 warnings** (from 18 errors / 829 warnings at task start). **Phase 1** installed `eslint-plugin-react-hooks` + `eslint-import-resolver-typescript`, ignored `scratches/` + `assets/editor-types/`, fixed 3 small code issues (cleared all 18 errors). **Phase 2** mechanical pass: `import/no-duplicates` → 0 (5 files), `import/no-named-as-default` (clsx) → 0 (9 files), `@typescript-eslint/no-unused-vars` → 0 (12 file removals + 6 `_`-prefix renames + 1 documented suppression for `iconv-lite`); also added `argsIgnorePattern`/`varsIgnorePattern` config. **Phase 3** triage across `no-explicit-any` (162 → 0), `no-non-null-assertion` (~154 → 0), `react-hooks/exhaustive-deps`, `import/no-named-as-default-member`: split into 7 sub-phases (3g.9 → 3g.15) covering scripting facades, content pipes, state primitives, IPC, dialogs, browser-service, editors (graph/markdown/grid/notebook), uikit, ui shell. Strategy was `any` → `unknown` + type predicates and narrowing; only 2×2 load-bearing `any` retained with eslint-disable + comment (`IDialogViewData`/`IPopperViewData` — TS class-invariance forbids forward-referenced type-param defaults). Per-folder ESLint overrides for `**/*.story.{ts,tsx}` + AVGrid + grid editor. **Bonus closeout:** also cleared 19 pre-existing TSC errors across 3 files — `automation/commands.ts` (14 — added `isErrorResponse` type predicate to discriminate `IBrowserTarget \| McpResponse`), `notebook/note-editor/NoteItemEditModel.ts` (1 — annotated `pipe: IContentPipe \| null`), `scripting/worker/WorkerRunner.ts` (4 — `as unknown as never` casts on `ipcRenderer.on(WorkerChannel.x, ...)` matching the file's existing sendMessage pattern). Final state: `npm run lint` exit 0, `npx tsc --noEmit` exit 0. ~50 files touched across 9 commits. |
| US-587 | Post-EPIC-028 doc / type audit | — | Whats-new v4.0.1 announced changes that were never executed in code. **W1** unified `EditorView` into a single canonical site at `src/renderer/api/types/common.d.ts` (synced 3 missing members `rest-client`/`video-view`/`storybook-view`); `src/shared/types.ts` now re-exports instead of duplicating, so drift can't recur. Extended `EditorView` IntelliSense across the registry surface — `IEditorInfo.id`, `IEditorRegistry.getById/resolveId`, `ISwitchOptions.options/getOptionLabel`. **W2** added `IEditorInfo.hasContentHost: boolean` (the field whats-new promised but never shipped); `toEditorInfo()` now forwards it from the internal `EditorDefinition`. **W3** doc-only fixes — `docs/api/page.md` `asText()` Promise return types, `force?` parameter blurb, `type` row removal; `docs/scripting.md` facade-lifecycle paragraph aligned with `page.md`; `docs/whats-new.md` false breaking-change bullet replaced with positive "TypeScript improvements" entry, editor count 22 → 25. **Dev docs** — `doc/standards/editor-guide.md` Step 6 rewritten to describe the canonical-in-`api/types/` + re-export-from-`shared/types.ts` pattern; `doc/architecture/folder-structure.md` clarifies `assets/editor-types/` is generated by the Vite plugin (never hand-edit). 11 files changed. |
| US-586 | MCP Inspector — block relative-link clicks in resource markdown | — | MCP resources are keyed by URI, not by filesystem path, so a sibling reference like `[X](structure.md)` inside resource markdown had no anchoring base. The browser resolved it against the renderer origin (in dev: `http://localhost:5173/structure.md`), Vite answered with the SPA-fallback `index.html`, and Persephone opened a junk markdown page with stuck Text Editor/Preview toggles. Added `onClickCapture` to the resource-content `<Panel>` in `ResourceContentView.tsx`: relative hrefs (no scheme, no `#`, no `//`) are `preventDefault`'d and surfaced via `ui.notify("info")`; absolute schemes still flow through `openRawLink` as before. 1 file changed. |
| US-546 | Fix Explorer file tree jumping to top on file changes | — | `TreeProviderViewModel.buildTree` published an intermediate root-only tree (all expanded child folders had `items: undefined`) before async-reloading expanded children. When the shrunken tree was shorter than the container, `RenderGridModel.updateRenderInfo` reset `offsetRef.y` to 0 (innerSize < size branch). With `FileTreeProvider`'s 500ms debounced FS watch firing on every agent edit, the scroll position was wiped repeatedly. Made `buildTree` atomic: refresh all expanded paths' children inline (sorted by length so parents process first), then publish a single `state.update` with the fully-populated tree. The old tree remains visible during the awaits, so scroll position is preserved. 1 file changed. |
| US-541 | Fix UIKit `CategoryList` missing custom scrollbar styling | — | `CategoryList`'s `Root` styled.div had `overflow: auto` (intrinsic to the primitive) but never applied `className="scroll-container"`, so the native browser scrollbar appeared instead of the global VSCode-style fade-in scrollbar that the `.scroll-container` CSS rule paints. Surfaced during US-540 LinkEditor testing once the Hostnames panel started scrolling correctly. UIKit `Panel` auto-applies the class when its `overflow` prop is `"auto"`/`"scroll"` (see `Panel.tsx:403`); `Menu`'s `ListRoot` also hardcodes it. CategoryList's Root now hardcodes `className="scroll-container"` since its overflow is baked into the styled definition (same idiom as Menu). UIKit `Textarea` has the same gap (`overflow-y: auto` without the class) — flagged as a potential consistency follow-up. |
| US-540 | Fix LinkEditor left panels + PinnedLinksPanel scroll overflow into page chrome | — | Every offending panel used `<Panel flex={1} overflow=…>` which translates to `flex: 1 1 auto`. With `flex-basis: auto`, basis reads the panel content's intrinsic height (sum of all rows) so the panel computed to a huge height and Chrome's flex implementation didn't reliably shrink. In the standalone editor the parent's `overflow: hidden` clipped the visible result; in the browser blank-page host the page-level scrollbar appeared instead, dragging the URL bar out of view. Added `height={0}` per the documented `feedback_uikit_panel_height_zero` UIKit Panel pattern: outer `<Panel flex={1}>` in `LinkHostnamesPanel`, `LinkTagsPanel`, `LinkCategoryPanel` (left CollapsiblePanelStack children) AND the inner `pinned-links-list` Panel in `PinnedLinksPanel` (column-flex child of the outer panel-stretched-via-align-items wrapper). The PinnedLinksPanel outer Panel did NOT need the fix because it's stretched via `align-items: stretch` in its row-flex parent; `height={0}` there would collapse it. |
| US-528 | Fix browser URL bar mangling `file://` URLs into `https://file///…` | — | `BrowserEditorModel.navigate()` only special-cased `http://`, `https://`, and `about:`, so pasting `file:///D:/...` got prepended with `https://`. Replaced the hardcoded list with a generic `^[a-z][a-z0-9+.-]*://` scheme check so any well-formed scheme (`file`, `chrome`, `app-asset`, `safe-file`, …) passes through unchanged. |
| US-527 | Fix `http://localhost:<port>` link in markdown viewer replacing main window contents | — | `will-navigate` handler in `src/main/open-window.ts` allowed any `http://localhost*` URL with pathname `/` to navigate, intended only for the Vite dev server. Now allowed only when origin matches `MAIN_WINDOW_VITE_DEV_SERVER_URL`; other localhost links go through `eOpenUrl` like any external URL. |
| US-526 | Fix external https link ignoring active browser-page profile | — | Two bugs combined. (1) `resolvers.ts` `browserMode === "internal"` branch called `openUrlInBrowserTab` with `{ profileName: "" }`, hardcoding the search to profile-less pages instead of signalling "external reuse". Now passes `{ external: true }`. (2) `PagesLifecycleModel.openUrlInBrowserTab` matcher for `external === true` still constrained the reuse search to `browser-default-profile`; matcher now reuses any non-incognito/non-tor browser regardless of profile. New-page fall-through still uses `browser-default-profile`. |
| US-510 | Fix TreeProviderView chevron click also triggering row navigation | — | Custom `renderItem` wired `onChevronClick={ctx.toggleExpanded}` directly, but `ctx.toggleExpanded` has signature `() => void` and never stops propagation, so the click bubbled up to Tree's row `onClick` and fired `onItemClick`. Wrapped to call `e.stopPropagation()` first. |
| US-494 | Fix Open Tabs list — clicking current-window document page does not activate it | — | `OpenTabsList` was passing `mainEditor.state.id` (editor UUID) instead of `page.id` (page UUID), so `pagesModel.showPage()` lookup silently failed for any page with a `mainEditor`. List item now spreads the editor state then overrides `id` with the page id so `showPage(item.page.id)` resolves correctly. |
| US-493 | Fix Explorer panel not refreshing on `navigateUp` / `makeRoot` | — | `ExplorerSecondaryEditor` read `rootPath` from `model.state.get()` (non-reactive snapshot) instead of `model.state.use()` (subscribed). Switching to `model.state.use()` makes the rendered tree re-mount via `key={rootPath}` when `navigateUp` / `makeRoot` mutate the state. |
| US-545 | Markdown viewer — Azure DevOps `::: mermaid` container support | — | Markdown preview now renders Azure DevOps wiki `::: mermaid ... :::` fenced container blocks as diagrams. Added `preprocessFencedContainers()` regex pre-processor in `MarkdownBlock.tsx` that rewrites the containers into ```mermaid``` fences before ReactMarkdown sees the content, so the existing mermaid renderer handles them. Memoized via `useMemo(content)`. Scope limited to `mermaid`; other `:::` names pass through. 1 file changed. |
| US-425 | Fix infinite-loop when Persephone is the OS default browser | — | Fixed recursive launch loop caused by Persephone handling its own protocol URLs when registered as OS default browser. Released in v3.0.8. |
| US-424 | Links & Tags Panel — Enhancements and Bug Fixes | — | 5 items: (1) Save button in Links sidebar panel (visible when modified and not main editor). (2) SaveIcon updated to floppy-disk shape. (3) Tag editing in LinkTooltip — toggleable badges + inline input (Enter to add). Wired into LinkItemList and LinkTagsSecondaryEditor. (4) PageNavigator + Links panel bugs — first open event fix (`toggleNavigator` now fires `pageNavigatorToggled`), always-3-panels when main editor is Links, standalone mode shows Links always + Tags if data + Hostnames hidden, `addSecondaryEditor` bumps version on re-register, demote microtask no longer strips panels. (5) Archive `!` separator false-positive fix — `isArchivePath` now validates archive extension on left side. 10 files changed. |
| US-423 | Tags Panel — Player Integration & Tag-Based Navigation | — | Tags panel visible in sidebar when Player is main editor. Bottom resizable pane (Splitter + LinksList) shows links filtered by selected tag; "All" shows all items. Clicking opens via `openRawLink` with `sourceId: "link-tag"` + `selectedTag`. Player next-track/shuffle works within the selected tag's set. `LinksList` converted to `forwardRef` for scroll-into-view on auto-advance. Fixed `ContentViewModelHost` race condition (concurrent `acquire` calls now share the same vm). Fixed `RenderGridModel.setProps` to pass `{ all: true }` so cell cache invalidates on prop change. `LinkTreeProvider.getTagItems("")` returns all items. 8 files changed. |
| US-422 | Audio Player — Next Track & Shuffle Mode | — | Auto-plays next/random track from Explorer (filesystem folder) or Links (link collection category) panel when a track ends. Provider discovery via `sourceLink.sourceId` + duck-typed `treeProvider`. Navigation goes through full `openRawLink` pipeline. Shuffle bag (Fisher-Yates) stored in new `PageModel.getTransient/setTransient`. Shuffle setting persisted to `app.settings`. Links panel selection unified with `LinkViewModel.selectLink()` — panel click and auto-navigation both update the same state, so promote-to-main shows the correct highlight. 11 files changed. |
| US-421 | Custom Audio Controls Bar | — | Replaced native `<audio controls>` with custom overlay bar: play/pause, current time, seek (CSS gradient fill), duration, mute toggle. Bar fades out at rest and reappears on hover using CSS `opacity` + `pointer-events` via parent `:hover`. `isSeekingRef` suppresses timeupdate re-renders during drag. 3 files changed (1 new: `AudioControls.tsx`). |
| US-420 | Bars Audio Visualizer — Iterative Improvements | — | Bars rewritten: centered vertically + horizontally (symmetric growth from center), per-bar HSL rainbow colors with horizontal 3D gradient, OffscreenCanvas stamp optimization for particles, mini-bar particles emitted at peaks (top+bottom pairs), particle emission rate tuned. Added "No effect" mode with track info overlay (ID3 + filename fallback). Explorer panel now highlights video/audio files (`filePath` set in `newEditorModel`). File Explorer button added to Player toolbar. Circular effect trail fade fixed (destination-out compositing). 7 files changed. |
| US-420 | Audio Visualizer — effects + switcher UI + theme support | — | Added circular effect (3 RGB rings with cross-band modulation, OffscreenCanvas stamping), effect switcher buttons (bars/circular), dark/light theme support, selected effect persisted to settings, click-to-play/pause on visualizer. Removed wave/rings effects. 6 files changed. |
| US-419 | Audio Visualizer for Audio-Only Files in Video Player | — | Audio files (.mp3/.wav/.aac/.flac/.m4a) open in video editor with AudioPlayer component + spectrum visualizer. Web Audio API AnalyserNode feeds switchable effects (BarsEffect, CircularEffect). 11 files changed (7 new). |
| US-418 | Browser Cinema Mode — Expand `<video>`/`<iframe>` to Full Page | — | Preload script detects `<video>` and `<iframe>` elements via `MutationObserver`. On hover, injects a fixed-position expand button (built with DOM API — `innerHTML` blocked by Trusted Types CSP on sites like YouTube). Expand: resizes entire ancestor container chain to `100vw × 100vh` (not the video element itself — YouTube continuously overwrites its inline styles); hides sibling elements via `visibility: hidden`; dispatches `window.resize` to trigger site's own resize handlers. Collapse restores all saved styles. Native video controls enabled on expand; `MutationObserver` restores them if site removes the attribute. Button repositions on scroll. 1 file changed (`preload-webview.ts`). |
| US-395 | Links Editor Secondary Panel Enhancements | — | 3 enhancements: (1) Swap button to promote/demote LinksEditor between main and sidebar with panel save/restore. (2) Editable Target dropdown in Edit Link dialog. (3) Centralized link opening through openRawLink pipeline — all paths now respect ILink.target; browserMode/browserPageId metadata for browser routing; removed dead selectedBrowser code and BookmarksDrawer monkey-patch. ~20 files changed. |
| US-325 | Fix preload script error in HTML preview | — | Disabled `nodeIntegrationInSubFrames` in BrowserWindow config. Electron was injecting the preload script into sandboxed HTML preview iframes, causing `module not found: node:path` console errors. Nothing depends on Node.js in iframes — browser automation uses CDP, webviews have their own preload. 1 file changed. |
| US-392 | Paste Rich Text as Markdown in Monaco Editor | — | Ctrl+Shift+V in markdown/html pages reads clipboard HTML and pastes as converted Markdown (or raw HTML). Uses Monaco `addAction` keybinding + Turndown (dynamic import) for HTML→Markdown. Regular Ctrl+V unchanged. 2 files changed (1 new: `paste-rich-text.ts`). |
| US-393 | Interactive Resource Templates in MCP Inspector | — | Resource templates (parameterized URIs) are now interactive: clickable in sidebar, detail panel shows name/URI/description/MIME, `TextAreaField` per `{param}`, "Read Resource" expands template and calls `readResource`, results shown via `ResourceContentView`. Mutual exclusion with static resources. 2 files changed. |
| US-394 | ClaudeSession Helper Class for Scripting | — | Exposes `ai.ClaudeSession` global to scripts wrapping `@anthropic-ai/sdk`. Auto-manages message history, tool-call loop (maxToolRounds=20 safety limit), events (tool-call, tool-result, assistant-message), and toolChoice per send(). 3 new files + 6 modified. |
| — | Browser tab UX improvements | — | Dark favicon glow (drop-shadow filter), tab activation history (close returns to previous tab), auto tab grouping with visual left border indicator, new tab (+) appends at end. 4 files changed. |
| US-360 | View Actual DOM — include iframe content | — | Moved DOM collection to main process via `collectDom` IPC channel. Uses `WebFrameMain.framesInSubtree` to collect all frame DOMs (incl. cross-origin), cheerio to inject content inside `<iframe>` elements. Both "View Actual DOM" and "Show Resources" updated. 3 files changed. |
| US-361 | Adopt libarchive-wasm for multi-format archive support | EPIC-018 | Replaced jszip (read) with libarchive-wasm. Supports RAR, 7z, TAR (+gz/bz2/xz), cab, ISO. jszip kept for ZIP writes. Added `ITransformer.writable`; `ContentPipe.writable` checks transformers. Renamed Zip* → Archive* (classes, files, IDs). New helpers: `getArchiveExtension()`, `isZipBasedArchive()`. WASM loaded from `assets/libarchive/`. ~20 files changed. |
| US-359 | Links panel improvements | EPIC-018 | Rich tooltips with copy-as-JSON, "Edit Link" context menu, navigation fixes (pipe passthrough, target resolution), ILink.target + ILinkMetadata.fallbackTarget/title, DataUrlProvider for data: URLs, inline script/style extraction, language detection from title, deduplication. 14 files changed (1 new). |
| US-358 | HTML resource extraction | EPIC-018 | `extractHtmlResources()` utility using cheerio via `require()`. "Show Resources" toolbar button on HTML pages + Browser context menu. WebScraperIcon. 4 files (1 new utility + 3 modified). |
| US-357 | LinkEditor as secondary editor — fixes | EPIC-018 | 5 fixes: cache write order in openLinks(), secondary editor restore for pages without mainEditor, page-level cache cleanup on close, link highlight in Links panel, CategoryEditor discovery via treeProvider duck-typing. 5 files changed. |
| US-356 | Multi-file drop handler | EPIC-018 | Single file drop: unchanged. Single folder or multiple items: `expandDroppedPaths()` recursively enumerates folders into ILink items with categories, then `pagesModel.openLinks()` creates a link collection page. 1 file changed (`GlobalEventService.ts`). |
| US-355 | Standalone link collection page | EPIC-018 | Added `app.pages.openLinks((ILink \| string)[], title?)` API. Creates TextFileModel with .link.json content as Pattern A secondary editor (never mainEditor). Categories panel in sidebar; clicking a link navigates the page. Passes pageId in RawLinkEvent metadata for in-page navigation. 7 files changed. |
| US-353 | Replace CategoryTree with TreeProviderView in LinkCategoryPanel | EPIC-018 | Replaced CategoryTree with TreeProviderView + LinkTreeProvider. Added getLabel and rootLabel props to TreeProviderView. Added hasSubDirectories/hasItems to ILink for accurate expand arrow visibility. categoriesOnly prop controls showLinks. 7 files changed. |
| US-354 | Consolidate ILink drag-drop into LinkDragEvent | EPIC-018 | Replaced LINK_DRAG + LINK_CATEGORY_DRAG + tree-provider-item with single LINK_DRAG_TYPE and LinkDragEvent carrying ILink[] + sourceId. 9 files changed. |
| US-352 | Clean up and unify link actions | EPIC-018 | Unified double-click → open link. Removed separate "open" buttons. Deleted dead ItemTile.tsx. PinnedLinksPanel: selection highlight, TreeProviderItemIcon. 7 files changed, 1 deleted. |
| US-351 | Secondary editor registration | EPIC-018 | LinkEditor registers panels in PageNavigator via pageNavigatorToggled event. Panel expansion sync. Pattern B guard. Conditional inline panels. 4 files changed. |
| US-348 | LinkEditor refactoring — browser removal, context menus | EPIC-018 | Removed browser selector. Renamed treeProviderContextMenu → linkContextMenu. Registered HTTP link handler. Refactored context menus to event channel pattern. 6 files changed. |
| US-349 | CategoryView uses LinksList/LinksTiles | EPIC-018 | Replaced CategoryViewRow/ItemTile in CategoryView with shared LinksList/LinksTiles. No type mapping (ILink unification). 7 files changed. |
| US-350 | ILink type consolidation | EPIC-018 | Consolidated ITreeProviderItem + LinkItem into unified ILink type. Renamed name → title. ~15 files changed. |
| US-346 | Extract LinksList and LinksTiles | EPIC-018 | Extracted view-only LinksList and LinksTiles from LinkItemList/LinkItemTiles. Callback-based API. 2 new files, 2 refactored. |
| US-345 | Shared panel components | EPIC-018 | Extracted Categories/Tags/Hostnames panels into shared components. 6 new files in panels/. 8 files changed. |
| US-344 | LinkTreeProvider | EPIC-018 | ITreeProvider adapter over LinkViewModel. list() enumerates sub-categories + leaf links. 1 new file, 1 modified. |
| US-343 | Make folder editor provider-agnostic | EPIC-018 | CategoryEditor scans secondaryEditors[] for matching treeProvider by type+sourceUrl. PageModel notifies mainEditor on secondary changes. 10 files changed. |
| US-342 | Test in Explorer — fixes and adjustments | EPIC-018 | Toolbar layout fix, root collapse guard, click behavior, ".." parent navigation, first tile switch fix. 5 files changed. |
| US-341 | Rename CategoryEditor → ExplorerFolderEditor + view mode | EPIC-018 | Full rename. FolderViewModeService with hierarchical inheritance. Per-folder view mode persistence. 14 files changed. |
| US-340 | CategoryView tile modes | EPIC-018 | Tile rendering for all view modes. Responsive grid, view mode toggle, favicon preloading. 1 file changed. |
