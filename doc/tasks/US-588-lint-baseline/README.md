# US-588: Lint baseline — fix errors, then triage warnings

**Status:** Planned — phased
**Type:** Maintenance
**Started by:** Lint sweep during 2026-05-30 session — `npm run lint` reports 847 problems (18 errors, 829 warnings). User goal: fix what we can sustainably; downgrade or per-line-disable what's not worth fixing; don't let the baseline drift forever.

## Goal

Reach a sustainable lint baseline:

- **Zero errors** — CI/lint never has a hard failure.
- **Zero unreviewed warnings** — every remaining warning is either fixed, intentionally suppressed (with a comment explaining why), or its rule is disabled with a documented rationale.

This is **phased**. Each phase ends with a checkpoint where the user reviews progress before the next phase starts.

## Background

`.eslintrc.json` currently uses:
```json
"extends": [
  "eslint:recommended",
  "plugin:@typescript-eslint/eslint-recommended",
  "plugin:@typescript-eslint/recommended",
  "plugin:import/recommended",
  "plugin:import/electron",
  "plugin:import/typescript"
]
```

It's missing `plugin:react-hooks/recommended` (causing 7 "rule not found" errors) and `eslint-import-resolver-typescript` (causing 6 subpath-import errors). It also lints generated `assets/editor-types/*.d.ts` and scratchpad `scratches/*` files that shouldn't be linted.

Counts (full breakdown):

| Severity | Rule | Count |
|---|---|---|
| **error** | `react-hooks/exhaustive-deps` (rule not found) | 7 |
| **error** | `import/no-unresolved` | 6 |
| **error** | `@typescript-eslint/no-this-alias` | 2 |
| **error** | Parser error (scratches/) | 1 |
| **error** | `prefer-const` | 1 |
| **error** | `no-var` | 1 |
| warning | `@typescript-eslint/no-explicit-any` | 577 |
| warning | `@typescript-eslint/no-non-null-assertion` | 154 |
| warning | `@typescript-eslint/no-unused-vars` | 77 |
| warning | `react-hooks/exhaustive-deps` | 14 |
| warning | `import/no-named-as-default` | 10 |
| warning | `import/no-duplicates` | 10 |
| warning | other small | ~7 |

## Phase 1 — Fix all 18 errors

Each error here has a specific proposed fix. Land them all in one commit; re-run `npm run lint`; checkpoint.

### 1.1 Config fixes (resolves 14 of 18 errors at once)

**Install missing ESLint packages:**

```bash
npm install --save-dev eslint-plugin-react-hooks eslint-import-resolver-typescript
```

**Update `.eslintrc.json`:**

```json
{
  "env": { "browser": true, "es6": true, "node": true },
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/eslint-recommended",
    "plugin:@typescript-eslint/recommended",
    "plugin:import/recommended",
    "plugin:import/electron",
    "plugin:import/typescript",
    "plugin:react-hooks/recommended"
  ],
  "parser": "@typescript-eslint/parser",
  "settings": {
    "import/resolver": {
      "typescript": { "alwaysTryTypes": true },
      "node": true
    }
  },
  "ignorePatterns": [
    "scratches/",
    "assets/editor-types/",
    "out/",
    ".vite/",
    "dist/"
  ],
  "rules": {
    "@typescript-eslint/no-empty-interface": "off",
    "@typescript-eslint/no-var-requires": "off",
    "no-useless-escape": "off",
    "@typescript-eslint/no-empty-function": "off"
  },
  "overrides": [
    {
      "files": ["**/*.d.ts"],
      "rules": {
        "no-var": "off"
      }
    }
  ]
}
```

**What this resolves:**

| Errors fixed | How |
|---|---|
| 7× `react-hooks/exhaustive-deps` "rule not found" | Adding `plugin:react-hooks/recommended` registers the rule. Side effect: 14 real `exhaustive-deps` warnings become visible (already counted in the warning total; nothing new) — will be reviewed in Phase 3. |
| 6× `import/no-unresolved` (`@modelcontextprotocol/sdk/*`, `csv-{parse,stringify}/browser/esm/sync`, `@excalidraw/excalidraw/index.css`) | `eslint-import-resolver-typescript` honors `package.json#exports`. |
| 1× parser error in `scratches/graph-preprocessing-psevdo-code.ts` | `ignorePatterns` excludes scratchpad. |
| 1× `no-var` in `src/renderer/types/window.d.ts:6` | `declare global { var X }` is the canonical TypeScript pattern for globals. Override rule for `**/*.d.ts`. |
| Bonus: also stops linting `assets/editor-types/*.d.ts` (generated by Vite) and other build output. |

### 1.2 Real code fixes (4 errors remaining)

| File | Fix |
|---|---|
| `src/renderer/scripting/ScriptContext.ts:95` | `const context = this;` is unnecessary — every usage (`context.releaseList`, `context.outputFlags` on line 106; `context` on line 109) is inside arrow-function callbacks (`ensureFacade`, the `callableUi` Proxy handlers) that already capture `this` lexically. Delete the alias; replace `context.x` with `this.x`. |
| `src/renderer/scripting/ScriptContext.ts:139` | Same pattern: `const self = this;` inside `createCustomRequire()` followed by an arrow-function `req`. Delete the alias; replace `self.x` with `this.x` in the function body. |
| `src/renderer/editors/rest-client/parseClipboardRequest.ts:194` | `let normalized = ...` is never reassigned — `eslint --fix` will change to `const`. Auto-fix. |

### Phase 1 acceptance

- [ ] `npm run lint` reports **0 errors** (warnings count may have shifted — that's fine; they're reviewed in Phase 2/3).
- [ ] `.eslintrc.json` reformatted cleanly.
- [ ] `npm install` produces no peer-dependency warnings about the two new packages.
- [ ] Commit Phase 1 separately so it's reviewable.

## Phase 2 — Easy fixes

After Phase 1, re-run `npm run lint`. Target the low-risk, mechanical, small-count warnings.

### 2.1 Auto-fixable (`eslint --fix`)

Run `npx eslint --fix .` and review the diff. The lint summary says "1 error and 5 warnings potentially fixable" today — that number will shift after Phase 1; just take whatever `--fix` produces. Manual review of each diff before commit (no blind acceptance).

### 2.2 `import/no-duplicates` (10 occurrences)

Same module imported twice in one file — e.g., `import type { X } from "react"; import { useState } from "react";` could be merged. Often `--fix` handles these. The Tooltip.tsx file shows `'D:\...react\index.js' imported multiple times`. Mechanical merge; no behavior change.

### 2.3 `import/no-named-as-default` (10 occurrences)

All `clsx`-related: `import clsx from "clsx"` triggers this because `clsx` package exports `{ clsx }` as named but no default. The fix is `import { clsx } from "clsx"`. ~10 files; mechanical. Affected files (per Phase-0 audit): `HeaderCell.tsx`, `SelectColumn.tsx`, `FilterBar.tsx`, `FocusModel.tsx`, and ~6 others — re-grep after Phase 1 lands since file list may differ.

### 2.4 `@typescript-eslint/no-unused-vars` (77 occurrences)

Three sub-patterns. Triage by category:

- **Truly dead** — remove the declaration. Probably the majority.
- **Intentionally unused parameter** (e.g., a callback signature you don't need but the type requires) — prefix with `_` (the rule allows that by default; if it doesn't here, configure `"argsIgnorePattern": "^_"`).
- **Unused but kept for documentation/future** — prefix with `_` or add `// eslint-disable-next-line @typescript-eslint/no-unused-vars` with a comment saying why.

77 cases is too many to enumerate up front. Strategy: do them in a single pass, file by file, presenting a summary diff for review before commit.

### Phase 2 acceptance

- [ ] `import/no-duplicates`, `import/no-named-as-default` warnings → 0.
- [ ] `@typescript-eslint/no-unused-vars` warnings → 0 (each resolved as remove / `_` prefix / suppress-with-comment).
- [ ] All `eslint --fix` autofixes reviewed and accepted (or backed out).
- [ ] Commit Phase 2 separately.

## Phase 3 — Review & decide per category

After Phase 1 + 2, the remaining warnings will be roughly:

| Estimated count | Rule | Likely disposition |
|---|---|---|
| ~577 | `@typescript-eslint/no-explicit-any` | Sub-categorize first (see 3.1). |
| ~154 | `@typescript-eslint/no-non-null-assertion` | Sub-categorize (see 3.2). |
| 14 | `react-hooks/exhaustive-deps` | Review each — these are real dependency-array bugs or false positives. |
| Few | other | One-off. |

### 3.1 `no-explicit-any` triage approach

Break down by area before deciding. Probable sub-buckets (verify counts after Phase 1):

| Area | Likely action |
|---|---|
| `src/renderer/uikit/AVGrid/*` (~70+) | AVGrid is a generic data-grid wrapper — `any` is somewhat by design for column accessors / cell values. Likely: per-file `/* eslint-disable @typescript-eslint/no-explicit-any */` with a one-line rationale comment at top. |
| `*.story.tsx` / `*.story.ts` (~15) | Storybook-style demo files. Disable rule via `.eslintrc.json` `overrides` for `**/*.story.{ts,tsx}`. |
| `src/renderer/uikit/Tooltip/`, `RenderGrid/`, etc. (small counts each) | Case-by-case — many will become real `unknown` or generic types. |
| Application code (non-UIKit) | Default to fix — replace `any` with a real type or `unknown` + narrowing. |

Plan: produce a per-subdirectory count summary, then discuss each bucket with the user before editing.

### 3.2 `no-non-null-assertion` triage approach

Break down: many `!` uses are after a check that the type system can't see (`if (x) { use(x!) }` patterns). Some are real laziness. Some are inside generic adapter code where narrowing isn't worth the ceremony.

Plan: same as 3.1 — sub-categorize by area, then discuss.

### 3.3 `react-hooks/exhaustive-deps` (14 real warnings, surfaced by Phase 1)

Each is potentially a real bug — missing dependency means a stale closure. Walk through each individually; for each one:

- If safe to add the dep: add it (auto-fix if `--fix` handles it).
- If adding the dep would cause unwanted re-renders / infinite loops: use `// eslint-disable-next-line react-hooks/exhaustive-deps` with a comment explaining why.

### Phase 3 acceptance

- [ ] Every remaining warning is either fixed, suppressed with a comment, or its rule is overridden with documented rationale.
- [ ] `npm run lint` is **0 errors / 0 warnings** OR every warning is one we've explicitly chosen to live with (suppression comment in place).
- [ ] If a rule is disabled in `.eslintrc.json`, the config has a comment explaining why.
- [ ] Commit Phase 3 in one or more reviewable chunks (suggested split: 3.1, 3.2, 3.3 separately).

## Concerns / Open questions

1. **Will Phase 1 surface new warnings?** Adding `plugin:react-hooks/recommended` activates ~14 `exhaustive-deps` warnings that today appear as "rule not found" errors. Net effect on the counter: -7 errors, no new warnings beyond what's already counted. Confirmed.

2. **What about the per-file `clsx` import — is the named-import form actually correct?** `clsx`'s `package.json` typically exports both a default and named — `import clsx from "clsx"` works at runtime but the linter prefers `import { clsx }` because of the named-as-default convention. Worth a quick smoke test (`import { clsx } from "clsx"; clsx("a", "b")`) before bulk-rewriting. If the runtime shape differs, fall back to per-file disable.

3. **Do we want CI enforcement?** Today `npm run lint` is run by hand. If we want this baseline to stick, a pre-commit hook or CI step would enforce zero errors. Out of scope for US-588 unless explicitly requested — flag for backlog.

4. **`react-hooks/recommended` includes other rules besides `exhaustive-deps` (e.g., `rules-of-hooks`).** These may surface additional errors after Phase 1. If so, fold the fixes into Phase 1 or split into a sub-phase.

5. **Should warnings ever become errors?** Some projects elevate selected warnings to errors after baseline cleanup, to prevent regression. Not in initial scope but worth discussing after Phase 3.

## Acceptance criteria (overall)

- [ ] Phase 1 complete and committed.
- [ ] Phase 2 complete and committed.
- [ ] Phase 3 complete and committed.
- [ ] `npm run lint` final state: 0 errors. Warnings either 0 or a small, documented set the user has explicitly accepted.
- [ ] `.eslintrc.json` is the documented source of truth for what's enforced and what's not.

## Out of scope

- CI enforcement (pre-commit hooks, GitHub Actions). Worth a follow-up task.
- Lint configuration for `qa/`, `scripts/`, `launcher/`, or `snip-tool/` directories beyond what `ignorePatterns` already covers.
- Reformatting / Prettier integration. Style is not enforced; that's a separate decision.
